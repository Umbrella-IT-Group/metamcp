# Umbrella IT Group fork of MetaMCP

This is a maintained downstream fork of [`metatool-ai/metamcp`](https://github.com/metatool-ai/metamcp). Upstream has been effectively unmaintained since 2026-02-08 (no merges in 88+ days as of fork creation). We carry community PRs that upstream isn't reviewing, plus our own targeted fixes, on the `umbrella` branch.

## Why fork

1. **OAuth refresh tokens.** Upstream advertises `refresh_token` in `/.well-known/oauth-authorization-server` but the token endpoint hard-rejects that grant. Result: Claude.ai's custom MCP connectors disconnect every 60 minutes (the hardcoded access-token TTL) and force re-authentication. Cherry-picked from upstream PR #276.
2. **Long-lived configurable TTLs.** Upstream hardcodes 1-hour access tokens. We make access + refresh + better-auth-session lifetimes env-var configurable, with defaults tuned for max connectivity (24h / 365d / 30d).
3. **Session lifecycle fixes.** Upstream PR #283 (ours), #260, #282 — sessions getting stuck in stale states.
4. **Subprocess cleanup.** Upstream PR #273 — child processes of terminated servers leak, eventually OOM the host.
5. **Per-server header forwarding.** Upstream PR #256 — flexibility for cross-tenant routing.

The MIT license permits this fork-and-maintain pattern explicitly.

## Upstream revival on `ai-dev` and convergence (2026-06 / decision 2026-07-21)

After ~4 months dark (last `main` merge 2026-02-08), upstream `metatool-ai/metamcp` revived in June 2026 on a new default branch, **`ai-dev`** — **135 commits ahead of `upstream/main`** (`git rev-list --count upstream/main..upstream/ai-dev` = 135, verified 2026-07-21). The author's note (`recent-updates.md` on `ai-dev`) frames it as the "forward ongoing dev branch which contains ai agent changes" and asks consumers to "test before you build the image based on this branch." That same README banner links this fork directly: *"There is also a community maintained fork (ty a lot!): https://github.com/Umbrella-IT-Group/metamcp"*.

### What converged upstream

The revival wave merged a batch of community work, including several of **our** contributions and several community PRs we had already cherry-picked. Verified via `gh pr view <n> --repo metatool-ai/metamcp` on 2026-07-21:

| Upstream PR | Author | Upstream state | Relationship to our line |
|---|---|---|---|
| [#286](https://github.com/metatool-ai/metamcp/pull/286) | `Umbrella-IT-Group` (ours) | **MERGED** 2026-06-14 (`3b4f36d`) | Better-auth session env-var configurability — our authored upstream PR. Mirrors fork PR #1. |
| [#293](https://github.com/metatool-ai/metamcp/pull/293) | `Exploitacious` (ours) | **MERGED** 2026-06-14 (`b73829d`) | Session-lost + transport-lost recovery detectors — our authored upstream PR. Backs fork PRs #12/#13. |
| [#283](https://github.com/metatool-ai/metamcp/pull/283) | `UmbrellaITSolutions` (ours) | **MERGED** 2026-06-14 (`e36dca3`) | Re-init backend session on HTTP 404 — our authored upstream PR (fork commit `1f1c937`). |
| [#276](https://github.com/metatool-ai/metamcp/pull/276) | `loris-av` | **MERGED** 2026-06-14 (`8b66c1d`) | OAuth `refresh_token` grant — community PR we cherry-picked (fork PR #1 base). |
| [#273](https://github.com/metatool-ai/metamcp/pull/273) | `alpha-pet` | **MERGED** 2026-06-14 (`e53eb58`) | Subprocess-leak fix — community PR we cherry-picked. |
| [#260](https://github.com/metatool-ai/metamcp/pull/260) | `BTForIT` | **MERGED** 2026-06-14 (`f3cd11c`) | Idle-based session timestamps + per-server cap — community PR we cherry-picked. |
| [#282](https://github.com/metatool-ai/metamcp/pull/282) | `loris-av` | **MERGED** 2026-06-14 (`1629c43`) | `SESSION_LIFETIME` env for session cleanup — companion to #276, in our line. |
| [#310](https://github.com/metatool-ai/metamcp/pull/310) | `Exploitacious` (ours) | **CLOSED, not merged** (`gh pr view 310` → `mergedAt: None`) | List-handler recovery cascade (ports fork PR #28). Superseded once #293 landed; the cascade **content is nonetheless present on `ai-dev`** (a `Merge pull request #310` commit `19e6b1f` landed the branch, but the PR against `main` reads CLOSED). Nothing lost — the content also ships in `umbrella` (fork PR #28). Local/remote branch `upstream-pr/list-handler-recovery` pruned 2026-07-21. |

Also merged upstream but **deliberately not adopted here**: [#256](https://github.com/metatool-ai/metamcp/pull/256) `BenjaminAronsson` per-server client header forwarding (MERGED 2026-06-14). It enables multi-tenant cross-tenant routing; this fork's posture is single-tenant with shared static per-server service credentials, so we decline it by design (previously logged as "DEFERRED" in the cherry-pick log — now reclassified DECLINED).

### Decision: stay on frozen `main`, no rebase onto `ai-dev` (operator, 2026-07-21)

We are **not** rebasing `umbrella` onto `ai-dev`, and `main` stays frozen as the pre-revival upstream mirror. Rationale:

- Our line has diverged structurally in ways a blanket rebase would fight — zod v4 + MCP SDK 1.29 (fork PR #64), the RBAC layer (#68), the M365 delegated-token broker (#44), and the declined #256 header-forwarding path that `ai-dev` carries.
- The patches that converged upstream (#286/#293/#283 and the community PRs #276/#273/#260/#282) already ship in `umbrella`; taking them again from `ai-dev` buys nothing and would drag in `ai-dev`'s experimental "ai agent changes" that upstream itself says to test first.
- Therefore the converged patches remain **carried as deltas by choice**. The cherry-pick log rows for them stay as-is; this section is the record that they *also* exist upstream, so a future maintainer knows they are safe to drop only if we ever choose to track `ai-dev`.

Cadence going forward: cherry-pick individual community fixes from `ai-dev` on their own merits (see the 2026-07-21 incoming-brings rows in the cherry-pick log below), rather than adopting the branch wholesale.

## Branch model

- `main` — mirrors upstream `metatool-ai/metamcp:main`. **Never** push our changes here directly. `git pull --rebase upstream main` periodically to track.
- `umbrella` — our integration line. All deployable work lives here. Default branch on this fork.
- `feature/<short-name>` — short-lived per-PR branches off `umbrella`. Merge with squash. Delete after merge.

`umbrella` is the hardened, deployable line — it is what gets built into `ghcr.io/umbrella-it-group/metamcp:latest`.

## Remotes

| Remote | URL | What it tracks |
|---|---|---|
| `origin` | `git@github.com:Umbrella-IT-Group/metamcp.git` | This fork. Push to `umbrella` and feature branches here. |
| `upstream` | `https://github.com/metatool-ai/metamcp.git` | Canonical upstream. Read-only — fetch for rebase comparison and to grab new community PRs. |

Other forks worth watching when looking for community work to cherry-pick (add as remotes only when actively reviewing — don't keep them around as noise):

- `cjam28/metamcp` — heavy OAuth-discovery polish (35 commits ahead of upstream as of 2026-04-30). `userinfo` real-identity, public-URL `/oauth/register`, spec-strict discovery metadata, `[TRACE-OAUTH]` logging.
- `Janhouse/metamcp` — dependency line (zod 3→4, MCP SDK 1.16→1.29). No longer a cherry-pick candidate for this line: the same dependency bump shipped via our own migration (PR #64, merged 2026-07-13 — see the cherry-pick log below for verification detail), not a cherry-pick of Janhouse's commit. Their migration was studied for breaking-change patterns only; our fork had diverged enough (e.g. it never type-checked its backend) that it never hit the pgEnum / session-recovery-hydration issues our own migration had to solve. Still worth a glance for whatever they pick up past this line next.

## Cherry-pick log

Track every commit we pull in. When upstream merges the same patch, we drop ours during the next rebase and replace with the upstream sha so history converges if upstream ever revives.

| Date | Source | Upstream PR | Commit on `umbrella` | Notes |
|---|---|---|---|---|
| 2026-05-07 | `loris-av` | [#276](https://github.com/metatool-ai/metamcp/pull/276) | `f446ce3`, `69e4b4f`, `138668a` (squashed into PR #1 on `umbrella`) | OAuth refresh-token grant + MAX_TOTAL_CONNECTIONS + SESSION_LIFETIME env vars. Umbrella patch on top: env-configurable TTLs (24h access / 365d refresh / 30d session). |
| 2026-05-07 | `UmbrellaITSolutions` | [#283](https://github.com/metatool-ai/metamcp/pull/283) | `1f1c937` | Re-init backend session on HTTP 404 "Session not found". Our PR. |
| 2026-05-07 | `tremlin` | [#273](https://github.com/metatool-ai/metamcp/pull/273) | `dcbe7bb`, `b63d709` | Subprocess leak fix — addresses upstream issues #128/#162 OOM cluster. Includes graceful SIGTERM + concurrency-safe creating-idle-sessions guard. |
| 2026-05-07 | `BTForIT` | [#260](https://github.com/metatool-ai/metamcp/pull/260) | `7a0535a`, `7289173`, `0cc901c`, `909ab61` | Per-server connection cap, cold-start warmup, idle-based session timestamps, admin reset/shutdown API. The `cee1356` per-server-cap commit conflicted with #273's concurrency guard — resolved by running cap check before entering critical section. |
| **DECLINED** | `BenjaminAronsson` | [#256](https://github.com/metatool-ai/metamcp/pull/256) | — | Per-server client header forwarding (12 commits). MERGED upstream 2026-06-14. **Reclassified DEFERRED → DECLINED-by-design 2026-07-21:** it enables multi-tenant per-client credential pass-through; this fork is single-tenant with shared static per-server service creds and would never forward a caller's `Authorization` upstream. Collides with 17 of our files (hottest recovery/proxy/pool). Revisit only if we ever go multi-tenant with per-client upstream creds. |
| 2026-07-21 | `bobbyhyam` | [#312](https://github.com/metatool-ai/metamcp/pull/312) | fork PR [#78](https://github.com/Umbrella-IT-Group/metamcp/pull/78) | **Persist the `uv`/`uvx` cache across container recreates** — named `uv_cache` Docker volume + `Dockerfile` dir pre-create/chown so `uvx` STDIO servers don't re-download packages on every Watchtower image swap. Byte-faithful port of upstream `7e3d252`/`ad68628`; touches `Dockerfile` + this repo's `docker-compose.yml` only (the prod compose fragment in `Umbrella-MCP-Server/metamcp/compose.fragment.yml` is a follow-up to actually mount the volume in prod). Correctly dropped `225f552`, which belongs to upstream #311 (self-heal), not #312. Reviewed approve-with-nits (docker build not verified end-to-end — slow base-image pull; deploy-time check). |
| 2026-07-21 | `elvus` | [#258](https://github.com/metatool-ai/metamcp/pull/258) slice | fork PR [#77](https://github.com/Umbrella-IT-Group/metamcp/pull/77) | **Rate-limiter enforcement fix (slice only)** — `SlidingWindowRateLimiting` checked `limiter.has(key)` against a map keyed by `namespace_uuid`, so it silently recreated the limiter every call and the client rate limit never enforced; fixed to `has(namespace_uuid)` in `lib/rate-limit.ts`. **Gated behind `enable_client_max_rate` (schema default `false`)** — a prod no-op today (we enable no per-client limits), so this is a latent-bug fix + defense-in-depth. Dropped all of #258's dep bumps (already on SDK 1.29 / zod v4), the devcontainer/postgres-expose change, and the unrelated middleware refactor. Added a red-green vitest (upstream shipped none). Reviewed approve-with-nits. |
| 2026-07-21 | `raphaelbarreiros` | [#300](https://github.com/metatool-ai/metamcp/pull/300) / [#301](https://github.com/metatool-ai/metamcp/pull/301) slices | fork PR [#79](https://github.com/Umbrella-IT-Group/metamcp/pull/79) | **OAuth hardening (slices)** — backend OAuth *client* path (gateway → OAuth-protected backend server; distinct from the gateway's own OAuth *server*). From #300: atomic `oauth_sessions` `onConflictDoUpdate` upsert (closes the concurrent token-exchange race). From #301: a per-flow `state` CSRF nonce persisted to a new `oauth_sessions.expected_state` column (migration **`0023_*`**, idempotent `ADD COLUMN IF NOT EXISTS`) and validated **server-authoritatively** via a new `oauth.validateState` tRPC procedure — our fork has no backend `exchangeToken` (token exchange is client-side via SDK 1.29 `auth()`), so the check moved to the callback; fails **closed**. **DROPPED** #300's zod hunk removing `refresh_token` from `OAuthAccessTokenSchema` (would revert our headline refresh feature + break the build) and **SKIPPED** #301's `client.ts` post-auth retry loop (collides with our ~700-line M365-mint client path). +15 vitest, red-green proven. Reviewed **approve**. PR OPEN — held for a maintainer deploy decision (touches the Claude.ai OAuth handshake path). **Rebase now REQUIRED before merge (2026-07-29):** #84 landed migration `0023_*` (`when` 1785196800000) and #85 landed `0024_*` (`when` 1785283200000) — this PR's `0023_oauth_expected_state` must be renumbered to `0025_*` AND its `_journal.json` entry's `when` bumped STRICTLY above 1785283200000, per the migration-ordering note in the #84 row (a mechanical rename alone is SILENTLY SKIPPED by drizzle). Exact instructions posted as a PR comment 2026-07-29. **Resolved same-day by `04d9f85`** (2026-07-29), which renamed `0023_oauth_expected_state.sql` to `0025_*` and bumped its `_journal.json` `when` above 1785283200000; PR #79 was reconfirmed `mergeStateStatus: CLEAN` against `umbrella` on 2026-08-13 and remains OPEN pending the operator prod-deploy go. |

## Our own patches (no upstream PR)

These are Umbrella-specific deltas. Each one should eventually be either upstreamed or replaced by an upstream alternative.

| Date | Fork PR | Description | Files | Upstream candidate? |
|---|---|---|---|---|
| 2026-05-07 | [#1](https://github.com/Umbrella-IT-Group/metamcp/pull/1) | Make access + refresh TTLs env-configurable; bump defaults to 24h / 365d | `apps/backend/src/routers/oauth/token.ts` | Yes — wait for upstream PR #276 to merge first, then file the env-var overlay. |
| 2026-05-07 | [#1](https://github.com/Umbrella-IT-Group/metamcp/pull/1) | Make better-auth session lifetime env-configurable; bump defaults to 30d / 7d | `apps/backend/src/auth.ts` | Yes — already filed as [metatool-ai/metamcp#286](https://github.com/metatool-ai/metamcp/pull/286). |
| 2026-05-07 | [#3](https://github.com/Umbrella-IT-Group/metamcp/pull/3) | Drop arm64 cross-build from CI | `.github/workflows/umbrella-build.yml` | No — Umbrella-specific deploy target. |
| 2026-05-07 | [#4](https://github.com/Umbrella-IT-Group/metamcp/pull/4) | Add `0014_oauth_refresh_token` entry to drizzle journal that PR #276 cherry-pick missed | `apps/backend/drizzle/meta/_journal.json` | Yes — once upstream PR #276 merges, journal needs this entry. |
| 2026-05-07 | [#5](https://github.com/Umbrella-IT-Group/metamcp/pull/5) | Make `0014_oauth_refresh_token.sql` migration idempotent (`ADD COLUMN IF NOT EXISTS`) | `apps/backend/drizzle/0014_oauth_refresh_token.sql` | Yes — defends any deployer from hash-mismatch crash-loop. |
| 2026-05-07 | [#6](https://github.com/Umbrella-IT-Group/metamcp/pull/6) | `/health/upstream` rollup endpoint with per-backend-MCP reachability | `apps/backend/src/index.ts` | Yes — clean addition, useful for any operator. |
| 2026-05-07 | [#7](https://github.com/Umbrella-IT-Group/metamcp/pull/7) | Replace MetaMCP brand with Umbrella IT Group wordmark on sidebar + browser-tab metadata | `apps/frontend/app/[locale]/(sidebar)/layout.tsx`, `apps/frontend/app/layout.tsx`, `apps/frontend/public/umbrella-wordmark.png` | No — Umbrella-specific. |
| 2026-05-07 | [#8](https://github.com/Umbrella-IT-Group/metamcp/pull/8) | Proxy `/health/*` paths to backend (was 404 on `/health/upstream` because rewrite was exact-match only) | `apps/frontend/next.config.js` | Yes — pairs with #6 if that's upstreamed. |
| 2026-05-07 | [#9](https://github.com/Umbrella-IT-Group/metamcp/pull/9) | Copy `apps/frontend/public/` in Dockerfile (Next.js 15 standalone doesn't auto-copy) + use square `Umbrella Bug` brandmark | `Dockerfile`, `apps/frontend/app/[locale]/(sidebar)/layout.tsx`, `apps/frontend/public/umbrella-bug.png`, `apps/frontend/public/umbrella-logo-full.png` | Dockerfile fix yes, sidebar text no. |
| 2026-05-08 | [#11](https://github.com/Umbrella-IT-Group/metamcp/pull/11) | OAuth cleanup TypeError fix: `oauth.repo.ts:147` `isNotNull(refresh_token).not()` → `isNull(refresh_token)`. The `.not()` chain isn't valid on a Drizzle `SQL` predicate; the cleanup task throws every tick, expired access tokens with no refresh token accumulate forever, and the leaked rows trip `MAX_CONNECTIONS_PER_SERVER` for downstream consumers (see `Umbrella-MCP-Server` PR #124 for the cap-bump companion fix). Diagnosed via third-agent break-glass on `mcp-host-prod` 2026-05-08; intent confirmed by the on-line-133 comment "(or refresh token is null)". | `apps/backend/src/db/repositories/oauth.repo.ts` | Yes — straight bug fix, should be upstreamed (regardless of whether upstream's session lifecycle differs, this code path exists upstream and is broken there too). |
| 2026-05-08 | [#12](https://github.com/Umbrella-IT-Group/metamcp/pull/12) | Hardens PR #283's session-lost recovery — extends `isBackendSessionLostError` to walk `.cause` chains, accept non-Error throwables, inspect `.code` on object inputs, and JSON-stringify nested envelopes; also engages the same recovery path on the dynamic-find `tools/list` failure in `metamcp-proxy.ts`. Production observation: between an `mcp-autotask` Watchtower restart at 2026-05-08T20:58Z and a manual MetaMCP restart at 21:15Z, 138 `-32600 "Session not found"` events fired without the existing detector tripping, despite the rendered string clearly containing all three matched substrings. Root cause not reproducible against synthetic test fixtures, so the fix takes the defensive path: broaden the detector against every plausible wrap shape + close the dynamic-find gap that silently logged-and-continued. 9 new tests cover the new shapes (cause-walk, plain envelope object, custom toString, circular cause, etc.). Existing 5 tests still pass. | `apps/backend/src/lib/metamcp/session-error.ts`, `apps/backend/src/lib/metamcp/session-error.test.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts` | Yes — pairs with PR #283 (`1f1c937`); both should land upstream as a unit. |
| 2026-05-14 | [#13](https://github.com/Umbrella-IT-Group/metamcp/pull/13) | Broadens PR #12's session-lost recovery to also handle the SDK transport-lost (`"Not connected"`) envelope. Adds `isBackendTransportLostError` (sibling detector, same hardening posture) + `isRecoverableBackendError` (union predicate). Recovery callsites in `metamcp-proxy.ts` switched to the union. Production observation: 2026-05-14T08:00–08:02Z six Tara n8n execs failed with `"Not connected"` 500 during the Bravo[autotask] punch list rollout (#137–#142); 2026-05-14T14:32Z Captain's Claude.ai connector hit `-32603 "Not connected"` on every call — gateway alive (curl `/` → 307), backend container UP, cached pool entry dead. Operator escalation 2026-05-14T16:54Z: "It's not acceptable for having MCP servers down or needing reboots after small change applications." Retires the `sudo docker restart metamcp` workaround. 14 new tests on transport-lost + 4 on the union predicate, 29 total in the file. | `apps/backend/src/lib/metamcp/session-error.ts`, `apps/backend/src/lib/metamcp/session-error.test.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts` | Yes — bundled with PR #12's surface, filed upstream as [metatool-ai/metamcp#293](https://github.com/metatool-ai/metamcp/pull/293). |
| 2026-05-14 | [#15](https://github.com/Umbrella-IT-Group/metamcp/pull/15) | **Lazy session recovery** — closes the metamcp-restart cascade that PR #12/#13 don't address. When metamcp itself restarts, the in-memory `sessionManager` is empty, so every consumer's cached `Mcp-Session-Id` returns 404 "Session not found" until manual re-init. This PR persists session metadata (sessionId, namespace, endpoint, SHA-256 auth principal, init params) to `mcp_sessions` on init; on subsequent requests where the in-memory transport is missing, the router queries the table, re-validates the auth principal in constant time, rebuilds the transport, and replays the request. Cross-namespace replay defense: the stored namespace + endpoint must match the request's. Auth-method discriminator: api_key vs oauth sessions can't be swapped. Pruner reaps rows older than `MCP_SESSION_TTL_DAYS` (default 7) on boot + every `MCP_SESSION_PRUNER_INTERVAL_MS` (default 24h). New file `lib/metamcp/session-auth.ts` (hashing + constant-time compare), new repo `db/repositories/mcp-sessions.repo.ts`, new schema table + migration `0015_mcp_sessions.sql`. 17 new tests: 10 on the auth helpers, 7 on the repository contract. Operator quote 2026-05-14T17:24Z: "Option 1 would be perfect. Let's build it." Closes the documented stale-session workaround for good — Streamable-HTTP metamcp restarts are now transparent to consumers. Merged 2026-05-14T17:54Z (`f0f636f`). | `apps/backend/src/db/schema.ts`, `apps/backend/drizzle/0015_mcp_sessions.sql`, `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/repositories/mcp-sessions.repo.ts`, `apps/backend/src/lib/metamcp/session-auth.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `turbo.json` | Yes — files cleanly on top of `main` (no dependency on PR #283); will mirror upstream after fork merge. |
| 2026-05-14 | [#16](https://github.com/Umbrella-IT-Group/metamcp/pull/16) | **Cascade `invalidateServerConnection` across every session for the same `serverUuid`** — closes the recovery-doesn't-recover gap surfaced at 2026-05-14T17:29Z when PR #13's detector fired correctly (`"Backend connection lost ..."` logged) but `getSession`'s cap-reuse branch handed the recovery path a stale `ConnectedClient` cached under a sibling session's slot for the same `serverUuid`. Rewrites `invalidateServerConnection` to iterate every `activeSessions[*][serverUuid]` entry, await per-client `cleanup()` (with per-client try/catch so one failure can't strand siblings), drop the idle slot + `creatingIdleSessions` guard. 7 new vitest cases prove the cascade, the no-touch-other-serverUuids guarantee, idle/creating cleanup, defensive cleanup-throws posture, and the no-op-when-empty case. `vitest.config.ts` `@/` alias added so tests can import `@/utils/logger`. Merged 2026-05-14T17:53Z (`9bdd177`). | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `apps/backend/vitest.config.ts` | Yes — surface change is contained to `mcp-server-pool`; mirror after the matching upstream `metamcp-proxy.ts` recovery wiring lands (companion to upstream PR #293). |
| 2026-05-14 | [#17](https://github.com/Umbrella-IT-Group/metamcp/pull/17) | **OpenAPI bridge recovery cascade** — closes the PR #15 post-deploy regression at 2026-05-14T18:30Z where the autotask MCP container restart (PR #143 Watchtower pull) produced repeated `Error POSTing to endpoint (HTTP 404) ... Session not found` events with **no** recovery firing on the OpenAPI consumer path. Root cause: `routers/public-metamcp/openapi/handlers.ts` has its **own fork** of `createOriginalCallToolHandler` + `createOriginalListToolsHandler` that pre-dates PR #13/#15/#16's recovery wiring in `metamcp-proxy.ts`. The OpenAPI fork only logged-and-rethrew on tools/call failure and logged-and-swallowed on tools/list failure. Tara and every other OpenAPI consumer (registry-sync worker, n8n httpRequest node, external integrations) hit this handler. This PR mirrors the Streamable-HTTP path's invalidate-and-retry cascade into both OpenAPI handlers, with `OpenAPI bridge:` log-prefix so operators can split this recovery from the Streamable-HTTP one when investigating. 4 new vitest cases cover the success retry, non-recoverable pass-through, fresh-session-also-fails, and re-acquire-returns-null defensive paths. Operator escalation 2026-05-14T18:31Z: post-deploy traffic stuck behind this gap. | `apps/backend/src/routers/public-metamcp/openapi/handlers.ts`, `apps/backend/src/routers/public-metamcp/openapi/handlers.test.ts` | Yes — straight bug fix to a code path that exists upstream and has the same gap. Pairs with upstream PR #293's detector contribution. |
| 2026-05-15 | [#18](https://github.com/Umbrella-IT-Group/metamcp/pull/18) | **Clean 404 response on stale sessionId — drop info leak + reinit-hint header** — server-side half of a CIPP MCP sub-agent session-stickiness fix (Captain's validation agent run, 2026-05-15, hit `404 Session not found` on every sub-agent tool call). Two problems with the prior response on both GET and POST `/:endpoint_name/mcp` handlers: (1) `available_sessions: sessionManager.getSessionIds()` leaked every live session UUID to the caller with zero diagnostic value; (2) no reinit signal, though the MCP Streamable HTTP spec requires the client to start a new session on a 404. Fix: drop `available_sessions` from the body, add an `Mcp-Session-Reinitialize-Required: true` header, and a plain-language body pointing at re-`initialize`. Lazy-recovery path (`recoverPersistedSession`) untouched. Client-side reinit (harness auto-retry on 404) is explicitly out of scope — that's Anthropic-side, closed-source; this ships only the server's half of the contract (this is the "already documented for PR #18" gap the #23/#24 rows above reference — the Anthropic MCP connector never adopted the client half, which is why those two PRs had to work around it at the session-persistence layer instead). Admin-side 404 paths (`mcp-proxy/metamcp.ts`, `mcp-proxy/server.ts`) intentionally not touched — different consumers. No new tests — the handler depends on auth middleware + `sessionManager` + the pool + `recoverPersistedSession`; mocking all four for a drop-a-field/add-a-header change was judged not worth it. `pnpm check-types` clean; `eslint --max-warnings 0` on the changed file clean. Merged 2026-05-15T17:10Z (`a5c077a`). | `apps/backend/src/routers/public-metamcp/streamable-http.ts` | Yes — spec-conformance fix (404 body/header shape) to a path that exists upstream unchanged. |
| 2026-05-21 | [#19](https://github.com/Umbrella-IT-Group/metamcp/pull/19) | **`tools/list_changed` propagation gateway → client** — closes the diagnosed gap where MetaMCP gateway never forwards backend `notifications/tools/list_changed` upstream and never advertises the `listChanged: true` capability on its own `Server`. Research-agent finding (parent context): "Zero `setNotificationHandler` calls anywhere in `apps/backend/src` for the list-changed schema; upstream `Server` capabilities declared `tools: {}` with no `listChanged`." Result before this PR: clients (Claude Code, Claude.ai custom connectors) NEVER learn about new/removed tools without a manual reconnect cycle. Surface: (a) `client.ts` registers a `ToolListChangedNotificationSchema` handler that fans out to a per-`ConnectedClient` `Set<ListChangedSubscriber>`; (b) `metamcp-proxy.ts` flips upstream capability to `tools: { listChanged: true }`, attaches a subscriber at every `getSession` callsite (originalListTools, dynamic-find, and both recovery paths from PR #12/#13/#16) that clears `toolsSyncCache` + emits upstream via `server.notification()`, detaches subscribers on `server.onclose`; (c) `mcp-server-pool.ts` `invalidateServerConnection` snapshots `listChangedSubscribers` BEFORE running cleanup and fires them — this is the watchtower-restart cycle-breaker (backend FastMCP doesn't emit on its own startup, so the invalidation path is the only signal consumers get that tools have moved). Subscriber dedupe is per-upstream-Server (Set semantics) so re-registration on recovery is idempotent. 5 new vitest cases on the fan-out dispatcher (`client.test.ts`) + 4 new cases on the pool's invalidation-fires-subscribers path (extends `mcp-server-pool.test.ts`). Backend FastMCP emit is intentionally out of scope — separate follow-up PR. | `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/client.test.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts` | Yes — capability declaration is spec-conformance, propagation is bug-fix to a path upstream has the same gap on. Pairs with backend-emit follow-up. |
| 2026-05-21 | [#23](https://github.com/Umbrella-IT-Group/metamcp/pull/23) | **Hybrid `boot_id` + `capability_hash` check on lazy-recovery (narrows PR #22)** — PR #22's blanket boot_id refusal forced a client re-initialize on every metamcp restart. That triggered a real-world Anthropic-side gap: the Anthropic MCP connector doesn't honor the spec's "HTTP 404 → start new session" requirement (already documented for PR #18); it wraps the 404 + `Mcp-Session-Reinitialize-Required: true` response as `-32600 "Anthropic Proxy: Invalid content from server"`. Result before this PR: every metamcp restart broke claude.ai sessions until manual `/mcp reconnect`, even for capability-neutral restarts (OAuth fixes, dep bumps, transport tweaks, lint sweeps). This PR refines the refusal to a two-step check — refuse only when stored `gateway_boot_id` differs from the current process AND stored `capability_hash` differs from the current process. Two metamcp processes built from the same source declare identical capabilities (`new Server({...})` is baked at construction), so their SHA-256 hashes match and recovery is safe; only true capability changes (the case PR #22 was actually trying to catch — PR #19's `tools: { listChanged: true }` addition) trigger the refusal. New file `lib/metamcp/gateway-capabilities.ts` is the single source of truth — same frozen object is declared on the upstream `Server` AND hashed into `GATEWAY_CAPABILITY_HASH`, so future capability changes auto-shift the hash without manual bumps. New migration `0017_mcp_session_capability_hash.sql` (idempotent `ADD COLUMN IF NOT EXISTS`, nullable). Combined predicate `shouldRefuseRecovery` encodes the full truth table with backward-compat handling for pre-PR-22 rows (null/null → allow) and PR #22-only rows (non-null boot_id, null hash — conservative refusal when boot_id also differs). 18 vitest cases (was 4) cover hash determinism, boot+hash interactions, and null branches; repo tests round-trip the new column. | `apps/backend/src/lib/metamcp/gateway-capabilities.ts`, `apps/backend/src/lib/metamcp/gateway-boot-id.ts`, `apps/backend/src/lib/metamcp/gateway-boot-id.test.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/drizzle/0017_mcp_session_capability_hash.sql`, `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/mcp-sessions.repo.ts`, `apps/backend/src/db/repositories/mcp-sessions.repo.test.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `UMBRELLA_FORK.md` | Yes — pairs with PR #15 + #22 (lazy-recovery + boot_id). Upstream should land all three together. |
| 2026-05-21 | [#22](https://github.com/Umbrella-IT-Group/metamcp/pull/22) | **`boot_id` check on lazy-recovery** — closes the capability-cache-mismatch bug introduced by PR #15. PR #19 added `tools: { listChanged: true }` to the gateway's advertised capabilities, but `initialize` negotiates capabilities once per session and PR #15's lazy-recovery hands clients a transport whose negotiated set was frozen against the pre-upgrade process — clients with cached `listChanged: false` silently ignore the new `notifications/tools/list_changed` we emit, leaving stale tool surfaces indefinitely. Today's session diagnosed this on the autotask namespace: `DELETE FROM mcp_sessions WHERE namespace_uuid='<autotask>'` nuked 140 stale sessions as a manual workaround, not durable across the next capability-bumping upgrade. This PR stamps a process-lifetime UUID (`GATEWAY_BOOT_ID`, `randomUUID()` at module load) onto every persisted session row at init; on recovery, the router refuses any row whose stamp differs from the current process's id — capability changes within a single process lifetime are impossible (capabilities are baked into `new Server({...})`), so a match is sufficient to skip the forced re-init. Null stamps (pre-PR-22 rows) are allowed through; pruner reaps them within `MCP_SESSION_TTL_DAYS`. New migration `0016_mcp_session_boot_id.sql` (idempotent `ADD COLUMN IF NOT EXISTS`, nullable for backwards-compat), new module `lib/metamcp/gateway-boot-id.ts` (constant + pure `shouldRefuseRecoveryForBootIdMismatch` predicate). 4 new vitest cases on the predicate + constant, extended repo tests pin the new column in `PersistedMcpSession`. | `apps/backend/src/lib/metamcp/gateway-boot-id.ts`, `apps/backend/src/lib/metamcp/gateway-boot-id.test.ts`, `apps/backend/drizzle/0016_mcp_session_boot_id.sql`, `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/mcp-sessions.repo.ts`, `apps/backend/src/db/repositories/mcp-sessions.repo.test.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `UMBRELLA_FORK.md` | Yes — pairs with PR #15 (lazy-recovery). Upstream should land both together. |
| 2026-05-21 | [#24](https://github.com/Umbrella-IT-Group/metamcp/pull/24) | **Auto-nuke pre-deploy sessions on first boot after capability change** — closes the wedged-session footgun left over by the Anthropic-client 404 gap (already documented for PR #18 + PR #23). PR #23's `shouldRefuseRecovery` correctly refuses lazy-recovery for sessions whose negotiated capability set is stale, but the Anthropic MCP connector wraps the resulting 404 + `Mcp-Session-Reinitialize-Required` response as `-32600 "Anthropic Proxy: Invalid content from server"` rather than honouring the spec's "start a new session on 404" requirement. Result: claude.ai sessions WEDGE indefinitely after a capability-changing deploy until the persisted row is manually `DELETE`d. This PR automates the cleanup. On metamcp boot — after migrations, before `app.listen()` — `autoNukeStaleSessions` does a one-shot scan of `mcp_sessions` for any row whose `capability_hash` is NULL (pre-PR-23 row) or differs from the current process's `GATEWAY_CAPABILITY_HASH`. If found, every non-matching row is deleted in a single statement. The client's NEXT request after the restart then hits the new-session POST path on the streamable-http router instead of the lazy-recovery refuse path, surfacing the Anthropic 404 mishandling at most once rather than indefinitely. Idempotent — re-running on a clean table is a no-op. Env-gated via `MCP_AUTO_NUKE_ON_CAPABILITY_CHANGE` (default true; set false for forensic-debug boots). DB errors are logged + swallowed inside the helper so a transient postgres issue doesn't crash boot. Per-namespace counts + the hash values found (current + each prior) are logged for forensic correlation; no token material is included. 9 new vitest cases cover empty-table, only-current-hash, single-old-hash, mixed null+old+current, only-null-hash, env-knob-disabled, env-knob-various-spellings, and DB-error-swallowed. Counterpart to IDEAS-side entry in COWORK for the upstream Anthropic-client fix. | `apps/backend/src/lib/metamcp/session-auto-nuke.ts`, `apps/backend/src/lib/metamcp/session-auto-nuke.test.ts`, `apps/backend/src/index.ts`, `turbo.json`, `UMBRELLA_FORK.md` | No — workaround for an Anthropic-client bug. Remove once Anthropic ships the spec-compliant 404 handling and the `Mcp-Session-Reinitialize-Required` header is honored. |
| 2026-05-21 | [#20](https://github.com/Umbrella-IT-Group/metamcp/pull/20) | **HTTP/SSE backend transport-drop detection + exponential reconnect backoff + circuit-breaker reset on recovery** — closes the diagnosed gap where HTTP/SSE backends had no `onclose`/`onerror` parity with STDIO's `ProcessManagedStdioTransport.onprocesscrash`. Watchtower-driven container restarts left the gateway's pooled `ConnectedClient` pointing at a dead socket; the next request hitting that pool entry tripped PR #13/#16's recovery cascade, but idle pool entries (no traffic) sat dead until manual reconnect. Surface: (a) `client.ts` attaches `transport.onclose`/`onerror` on `SSEClientTransport` and `StreamableHTTPClientTransport` instances, plumbs a new `onTransportDrop` callback through `connectMetaMcpClient` (sibling to `onProcessCrash`), tracks a `closing` flag so caller-driven `cleanup()` suppresses the drop callback during normal shutdown, and chains existing handlers so SDK-internal bookkeeping still runs; (b) replaces the fixed-5s `waitFor` reconnect sleep with a documented exponential schedule 1s / 2s / 4s / 8s / 16s / 30s-cap plus ±250ms jitter (`computeReconnectBackoffMs`), env-configurable via `MCP_RECONNECT_BACKOFF_INITIAL_MS` / `_MAX_MS` / `_MULTIPLIER`; (c) `mcp-server-pool.ts` wires `onTransportDrop` to a private `handleTransportDrop` that cascade-invalidates every pool slot for the affected serverUuid (reuses PR #16's path, so PR #19's `listChangedSubscribers` fan-out fires for free), and conditionally calls `serverErrorTracker.resetServerAttempts(uuid)` when the most recent success was within `MCP_RECOVERY_RESET_THRESHOLD_MS` (default 5 min) — transient watchtower bounces don't accumulate the sticky circuit breaker, but sustained failure clusters preserve their count; (d) `server-error-tracker.ts` adds a `markSuccess` method (intent-distinct sibling to `resetServerAttempts`) called on every successful connect. 5 new vitest cases on `handleTransportDrop` (cascade, list_changed synergy with PR #19, within-threshold reset, beyond-threshold no-reset, never-succeeded no-reset) + 2 new cases on the backoff schedule (documented schedule, jitter envelope). **Stacks on PR #19** — uses `listChangedSubscribers` infrastructure introduced there. | `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/client.test.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `apps/backend/src/lib/metamcp/server-error-tracker.ts`, `turbo.json` | Yes — STDIO `onprocesscrash` already exists upstream; this is the missing HTTP/SSE parity. File together with PR #19 if both land on upstream as a single propagation+reconnect surface. |
| 2026-05-27 | [#27](https://github.com/Umbrella-IT-Group/metamcp/pull/27) | **Hydrate lazy-recovered StreamableHTTP transports — the fix that makes PR #15/#22/#23/#24 actually work.** Root-caused the `-32600 "Anthropic Proxy: Invalid content from server"` connector wedge to OUR side, not Anthropic's. Break-glass investigation on `mcp-host-prod` 2026-05-27 (container booted 20:52:34Z on PR #26): every one of 314 `mcp_sessions` rows carried the current `capability_hash` (`e35d609e…`, verified == locally-computed SHA-256 of the frozen `GATEWAY_CAPABILITIES`), so `shouldRefuseRecovery` was returning *allow* and `autoNukeStaleSessions` was deleting nothing — the entire PR #22/#23/#24 capability machinery never fired. Recovery was "succeeding" and then 400-ing on the first real request. Cause: PR #15's `recoverPersistedSession` rebuilds a `StreamableHTTPServerTransport` and `server.connect()`s it, but never replays the MCP `initialize` handshake. In `@modelcontextprotocol/sdk` 1.16.0 the transport flips `_initialized=true` + assigns `sessionId` ONLY inside the initialize-request branch (`server/streamableHttp.js:337-338`); a reconnecting client already initialized in the dead process and won't send `initialize` again. So the rebuilt transport's first request hits `validateSession` → `400 {-32000 "Bad Request: Server not initialized"}` (`streamableHttp.js:442`), which the Anthropic connector relays as `-32600`. Explains the observed asymmetry exactly: `/mcp` reconnect reuses the cached `Mcp-Session-Id` → same dead transport → wedge persists; a full client refresh drops the id → `initialize` with no `Mcp-Session-Id` → the new-session POST branch runs the handshake → works (confirmed by fresh boot-id `3e2057d9` rows minted 21:02–21:09Z on refresh). The recovery tests never caught it because they mock the transport and skip the real `_initialized` gate. Fix: new side-effect-free module `lib/metamcp/transport-recovery-hydration.ts` — `hydrateRecoveredTransport(transport, sessionId)` restores `_initialized`/`sessionId` after `connect()` (guarded: returns false + refuses recovery if the SDK field shape changed), plus `assertRecoveryHydrationContract()` run at router module load to fail loud in boot logs on an SDK-internals rename. Makes the boot_id/capability_hash machinery finally mean something: hash matches → transparent recovery that *works*; hash differs → refuse + reinit. 6 new vitest cases drive the REAL SDK transport (no mock) — reproduce the un-hydrated 400, prove hydration clears it, prove a mismatched session id still 404s, prove the SDK-shape-change refusal, and the boot contract. SSE (different transport, no `_initialized` gate) + the OpenAPI bridge (recovers the backend client, not a client-facing transport) are unaffected — scoped to streamable-http. | `apps/backend/src/lib/metamcp/transport-recovery-hydration.ts`, `apps/backend/src/lib/metamcp/transport-recovery-hydration.test.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `UMBRELLA_FORK.md` | Yes — the lazy-recovery transport-init gap exists upstream too; the keystone of the #15→#27 recovery line. File upstream with PR #15/#22/#23. |
| 2026-05-27 | [#26](https://github.com/Umbrella-IT-Group/metamcp/pull/26) | **LRU eviction at the global connection cap** — closes the deadlock that PRs #16 + #20 don't cover. `maxTotalConnections` (default 100) was a HARD refuse. Under persistent sessions (`sessionLifetime === null`, the default) `cleanupExpiredSessions` no-ops AND `cleanupSession` RECYCLES active connections back into the idle pool rather than destroying them, so `getTotalConnectionCount` (idle + active + pending) climbs to the cap and stays there. Once full, `canCreateConnection` refused EVERY new connection — including the idle-session recreation a backend needs after its container restarts. PRs #16/#20 cascade-invalidate the *affected* server's slots, but a globally-full pool still locks out that server's *re-creation* (`createNewConnection` → `Skipping connection … connection limit reached`), so the pool deadlocks until a manual `docker restart metamcp`. Observed 2026-05-27: `mcp-autotask` wedged 8+ min after a Watchtower redeploy (Umbrella-MCP-Server PR #182) — `metamcp` up 5 days, `mcp-autotask` healthy on the new image, but every autotask tool returned `-32603` "backend session loss / Unknown tool". Fix: new `evictOneForCapacity(forServerUuid)` frees ONE slot by DESTROYING (not recycling — `cleanupSession` would recycle and free nothing) the least-valuable connection: an idle session first (prefers a server other than the one being admitted), else the oldest-touched active connection; `createNewConnection` evicts-and-rechecks at the cap instead of hard-refusing. Makes the cap a soft LRU bound. 5 new vitest cases (idle-evict + other-server preference, sole-idle fallback, oldest-active when no idle, never-evict-the-admitted-server, empty-pool no-op); 21 total in the file. | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `UMBRELLA_FORK.md` | Yes — the hard-refuse deadlock exists upstream too; straight robustness fix. Pairs with PRs #16/#20 as the global-cap complement to their per-server cascade. |
| 2026-06-11 | [#28](https://github.com/Umbrella-IT-Group/metamcp/pull/28) | **Invalidate-and-retry cascade in all four aggregate list handlers** — closes the silent-degradation gap behind incident 2026-06-11 (Umbrella-MCP-Server#229): autotask/sales/itglue/cipp namespaces served 0 tools for ~24h while every backend was healthy. Watchtower swapped backend containers Jun 10 → pooled clients died; idle StreamableHTTP transports hold no socket so PR #20's drop detectors never fired; the next `tools/list` reused the zombie via the cap-reuse branch, the per-server fetch threw, and the handler's catch **logged-and-continued** — no invalidation, no retry, zombie pooled forever, `tools/list completed in 3-8ms, returning 0 tools` as a *successful* response until manual `docker restart metamcp`. `tools/call` + dynamic-find have had the cascade since PR #13/#16, the OpenAPI bridge since 2026-05-14; the four aggregate list handlers (tools/prompts/resources/templates) were the remaining gap. Fix: shared `requestWithSessionRecovery` helper (invalidate → fresh getSession → re-register PR #19 subscriber → retry once), wired into all four handlers with client-map repointing; error-level `DEGRADED for namespace …` tripwire whenever a server is excluded from an aggregate response; `No session for` upgraded console.log → logger.error with reason classes. 6 new vitest cases on the helper; 139 total. | `apps/backend/src/lib/metamcp/list-handler-recovery.ts`, `apps/backend/src/lib/metamcp/list-handler-recovery.test.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `UMBRELLA_FORK.md` | Yes — same swallow exists upstream in all four handlers; pairs with upstream PR #293's detectors. |

| 2026-06-11 | [#29](https://github.com/Umbrella-IT-Group/metamcp/pull/29) | **Periodic health sweep over ACTIVE pooled connections** — the 60s health check pinged idle sessions only; active connections were never health-checked. Idle StreamableHTTP transports hold no socket between requests, so a Watchtower swap of the backend container kills every pooled client without firing PR #20's onclose/onerror detectors; at the per-server cap every slot is active, the idle loop sees nothing, the cap blocks idle recreation, and getSession's cap-reuse branch hands zombies out blind indefinitely (incident 2026-06-11). `checkActiveSessionHealth` pings every distinct active ConnectedClient (cap-reuse shares one client across sessionIds — pinged once); two consecutive failed sweeps trigger the PR #16 cascade + error reset + idle rebuild. Worst-case zombie lifetime drops from infinity to ~2 min with zero traffic. Env kill-switch `MCP_ACTIVE_HEALTH_CHECK`. Also fixes `checkIdleSessionHealth`'s early return on an empty idle map, which blinded the entire health check (recreation loop included) exactly when the pool was at its sickest. 7 new vitest cases. | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `turbo.json` | Yes — upstream has the same blind spot; pairs with PR #20's transport-drop parity. |
| 2026-06-11 | [#30](https://github.com/Umbrella-IT-Group/metamcp/pull/30) | **Half-open probe for ERROR-gated servers** — the ERROR circuit breaker (DB `error_status`) was sticky forever: the only request-path reset (cold-start warmup) requires the ENTIRE pool empty, which never happens while other namespaces hold live connections; an ERROR-gated backend stayed excluded until admin reset or process restart (the unkillable `No session for: autotask` loop, incident 2026-06-11). The health timer's recreation loop now resets the gate and warms an idle session every `MCP_ERROR_PROBE_INTERVAL_MS` (default 5 min, <=0 disables). Backend back -> heals hands-free; still down -> STDIO crash-counting re-trips the breaker, HTTP/SSE waits for the next probe. Note: a still-down backend's error_status flaps ERROR->NONE in the admin UI at probe cadence; the warn-level probe log line is the source of truth during sustained outages. 5 new vitest cases; 151 suite-wide. | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `turbo.json` | Yes — standard breaker semantics upstream lacks; lands with #29. |

| 2026-06-11 | [#32](https://github.com/Umbrella-IT-Group/metamcp/pull/32) | **tsc-clean + latent-bug + logging cleanup** — (1) `metamcp-server-pool.ts` `isSessionExpired` lacked the null-lifetime guard its two sibling implementations have: under the default persistent-session config (`sessionLifetime === null`), `age > null` coerces to `age > 0` and every session reads expired at 1ms old. Currently uncalled (the cleanup timer uses its own guarded copy) — defused before anything wires it. (2) `DatabaseNamespaceServerSchema` missing `error_status` (repo selects + serializer reads it; type didn't know). (3) `admin.ts` handler param annotations broke Express overload resolution. (4) test-file private-ctor casts consolidated. Net: `tsc --noEmit` 7 errors → 0. (5) hot-path `[DEBUG-TOOLS] console.log` → `logger.debug` (level-gated) and pool cascade `console.error/warn` → logger; legacy `MAX_TOTAL_CONNECTIONS`/`MAX_CONNECTIONS_PER_SERVER` declared in turbo.json. | `apps/backend/src/lib/metamcp/metamcp-server-pool.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `apps/backend/src/routers/public-metamcp/admin.ts`, `packages/zod-types/src/namespaces.zod.ts`, `turbo.json`, `UMBRELLA_FORK.md` | isSessionExpired guard + zod field yes; rest housekeeping. |
| 2026-06-12 | [#34](https://github.com/Umbrella-IT-Group/metamcp/pull/34) | **/health/upstream truthfulness for down HTTP/SSE backends** — `reachable` was literally `!in_error`, and HTTP/SSE backends never trip the ERROR breaker (crash counting is STDIO-only): a hard-down HTTP backend read `reachable: true, healthy: true` forever, blinding the external uptime probe added 2026-06-11. The pool now stamps `lastConnectFailureAt[serverUuid]` at the `createNewConnection` chokepoint (covers cold start, sweep rebuilds, half-open probes — `connectMetaMcpClient` resolves undefined on every failure mode) and clears it in `markServerSuccess`; `getPoolStatus` exposes failure/success stamps + active-sweep ping-failure counts. `/health/upstream`: `reachable = !in_error && !(0 connections && failure stamp)`, aggregate `healthy = unreachable === 0` (supersedes errored-only), new `unreachable_servers` count + per-server `last_connect_failure_at` / `last_connect_success_at` / `ping_failures` ISO fields. Zero-connections-no-stamp stays healthy (lazy cold-start). 4 new vitest cases; 41 in file. | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `apps/backend/src/index.ts`, `UMBRELLA_FORK.md` | Health endpoint is fork-only; the pool telemetry (failure stamps) is upstreamable alongside #310's pool surface. |
| 2026-06-12 | [#35](https://github.com/Umbrella-IT-Group/metamcp/pull/35) | **OpenAPI bridge DEGRADED-tripwire parity** — the bridge got the invalidate-and-retry cascade 2026-05-14, but kept two silent-failure residues PR #28 fixed on the Streamable-HTTP path: `getSession` returning null was a bare `return` (no log, server vanished from the catalog silently — the same shape as incident #229's 0-tool responses), and per-server failures never rolled up into the namespace-level `DEGRADED for namespace` error line, so OpenAPI-path degradation (Tara, n8n httpRequest, registry-sync) was invisible to the Grafana alert `crit-metamcp-namespace-degraded`. Fix: null-session now logs error + counts as failed; outer catch counts too; aggregate tripwire emits the same log shape as metamcp-proxy.ts with an `(OpenAPI bridge)` suffix to split the consumer paths. 3 new vitest cases (null-session, non-recoverable failure, healthy-silent); 7 in file. | `apps/backend/src/routers/public-metamcp/openapi/handlers.ts`, `apps/backend/src/routers/public-metamcp/openapi/handlers.test.ts`, `UMBRELLA_FORK.md` | Yes — same gap upstream; natural follow-on to metatool-ai#310. |
| 2026-06-12 | [#36](https://github.com/Umbrella-IT-Group/metamcp/pull/36) | **Namespace-pool silent-failure fixes (2026-06-12 audit F1-F4)** — read-only audit of `metamcp-server-pool.ts` for incident-#229 bug classes found: **(F1, critical)** `createIdleServer` (blocking variant) had no in-flight guard and overwrote the idle slot unconditionally; every tRPC config edit fires `invalidateIdleServer` + `invalidateOpenApiSessions` concurrently, both reached `createNewServer`, and the loser's backend ConnectedClients leaked forever under a temp `idle_<ns>_<ts>` sessionId (persistent sessions never expire) — repeated admin edits ratchet the backend pool toward cap exhaustion, the #229 end state via the admin-edit route. Fixed with the mcp-server-pool pattern: `creatingIdleServers` guard, post-await re-check, loser cleanup, plus a per-namespace `idleServerGenerations` counter (bumped by invalidate/cleanup/cleanupAll) so in-flight creations straddling an invalidation discard their pre-change-config result. **(F2, critical)** `invalidateOpenApiSessions` cleaned the namespace instance but never the backend-pool sessions keyed `openapi_<ns>` — the id the OpenAPI request path actually uses — so after a server config change the OpenAPI surface kept executing against stale pre-change clients indefinitely. Now cleans that layer explicitly. **(F3, moderate)** `startup.ts` warmed idle servers with `includeInactiveServers=true` while conversion never checks the flavor and every consumer asks `false`: the first public session per namespace after each boot served INACTIVE backends' tools for its whole life. Startup now warms `false`. **(F4, moderate)** `cleanupSession` deleted maps only after two unguarded awaits; one throw stranded a permanent zombie. Maps drop first; each layer cleans in its own try/catch. New test file, 8 cases; 159 suite-wide. Audit's verified-clean list + minor findings (F5-F7) recorded in PR body. | `apps/backend/src/lib/metamcp/metamcp-server-pool.ts`, `apps/backend/src/lib/metamcp/metamcp-server-pool.test.ts`, `apps/backend/src/lib/startup.ts`, `UMBRELLA_FORK.md` | Yes — all four exist upstream; F1/F2 are straight bug fixes. |

| 2026-06-27 | [#37](https://github.com/Umbrella-IT-Group/metamcp/pull/37) | **Default new namespaces/endpoints/servers to public; endpoints default OAuth-on** — Phase 1 of the naming+topology refactor (plan of record: `Umbrella-MCP-Server/docs/refactor-naming-topology-plan.md` D5), picked as the standalone zero-consumer-blast-radius entry point. Ownership default flips from the creating user to `null` (Everyone) when omitted, at both the frontend `defaultValues`/`reset()` call sites (`namespaces/page.tsx` ×3, `endpoints/page.tsx` ×3, `mcp-servers/page.tsx` ×1) and the backend fallback (`namespaces.impl.ts`/`endpoints.impl.ts`/`mcp-servers.impl.ts`, `?? userId` → `?? null`); the Private option stays in the form selector and editing an existing record is untouched. New endpoints also default `enable_oauth` to `true` (create-form defaults/resets + both Zod schemas' `.default(true)`; the edit form still reads the endpoint's **stored** value so existing endpoints keep rendering their real state). No DB migration — `user_id` is already nullable with no DB default, and `enable_oauth`'s DB-level default is never exercised since the app always sends the value explicitly; existing rows are untouched. `pnpm check-types` clean; backend `tsc --noEmit` 0 errors. Merged 2026-06-27T17:15Z (`4c77662`). | `apps/backend/src/trpc/endpoints.impl.ts`, `apps/backend/src/trpc/mcp-servers.impl.ts`, `apps/backend/src/trpc/namespaces.impl.ts`, `apps/frontend/app/[locale]/(sidebar)/endpoints/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/mcp-servers/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/namespaces/page.tsx`, `apps/frontend/components/edit-endpoint.tsx`, `packages/zod-types/src/endpoints.zod.ts` | No — Umbrella-specific default-visibility posture; upstream has no equivalent policy stance to adopt. |

| 2026-06-29 | [#38](https://github.com/Umbrella-IT-Group/metamcp/pull/38) | **Activity-rich Live Logs + prune deleted-server pool zombies** — the Live Logs view only ever wrote connection errors to the in-memory store (for HTTP backends the sole writer was `client.ts`'s connect-failure path, attributed to a literal `"client"` with the error detail hidden in the UI). Combined with a deleted/renamed server whose `serverParamsCache` entry the 60s health sweep kept reviving (the `endpoints`→`ninja` rename left an orphan — ~297 failed reconnects in 40 min, observed 2026-06-29), the view was nothing but `Error connecting to MetaMCP client (attempt N/3)` with no server name; all real activity (connections, tool calls) went only to stdout→Loki. Surface: (a) `log-store` gains an event `category` (connection/tool_call/server/system) + `serverUuid`/`toolName`/`durationMs` and a structured `record()` entry point (`addLog()` retained as a `category=server` back-compat wrapper); ring buffer 1000→2000. (b) `client.ts` connect-failure now names the server + emits connection-established and transport-drop events. (c) implements the previously-stubbed `createAuditingMiddleware()` — every proxied `tools/call` logs a `tool_call` event (tool, server, duration, ok/fail), wired **outermost** so denied calls are captured too. (d) **root-cause:** `mcp-server-pool.checkIdleSessionHealth` reconciles against `mcpServersRepository.findAll()` at the top of the 60s sweep and prunes pool state (idle session, params cache, error gate, probe/ping counters) for any uuid no longer in the registry — deleted/renamed servers can no longer zombie-reconnect; guarded so the unit-test repo mock / a transient DB blip can't take the sweep down. (e) frontend Live Logs: category color-tags, per-category + errors/warnings-only filters, tool/duration, un-hidden error detail. Contract: `MetaMcpLogEntry` zod schema extended; `GetLogs` limit max 1000→2000. backend vitest 166/166. | `apps/backend/src/lib/metamcp/log-store.ts`, `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/metamcp-middleware/auditing.functional.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/frontend/app/[locale]/(sidebar)/live-logs/page.tsx`, `apps/frontend/lib/stores/logs-store.ts`, `packages/zod-types/src/logs.zod.ts`, `UMBRELLA_FORK.md` | Pool-prune yes (registry reconciliation is a robustness fix upstream lacks). Activity logging + Live Logs UI are fork-shaped (upstream's store is connection-error-only), but the `category` schema + `tool_call` auditing middleware are upstreamable. |

| 2026-06-29 | [#39](https://github.com/Umbrella-IT-Group/metamcp/pull/39) | **Consumer identity ("who") in Live Logs** — tool_call events + a new client-session event now show the authenticated caller (api-key name like "Tara connector", or OAuth user email). Identity is resolved at the endpoint auth layer (`authReq.apiKeyUuid`/`oauthUserId` → `api_keys.name` / `users.email`, cached) and threaded to the audit middleware WITHOUT touching the incident-sensitive pool/proxy/createServer. (a) `session-client-registry.ts` — pure, DB-free in-memory sessionId→identity map; the audit middleware imports ONLY this (keeps its module graph DB-free for unit tests). Streamable-HTTP keys it per-consumer sessionId (race-free) and refreshes on every request, so lazy-recovered sessions repopulate it post-restart. (b) `consumer-identity-resolver.ts` — router-only DB resolver (cached per method:id). (c) OpenAPI bridge is stateless (one deterministic `openapi_<ns>` sessionId shared across consumers), so its identity rides the per-call `MetaMCPHandlerContext.clientName` instead of the shared registry key — no cross-consumer race. (d) `log-store`/zod gain a `clientName` field + a new `client` category (client→gateway session open, distinct from gateway→backend `connection`). (e) frontend renders `← <name>` per row + a CLIENT category tag/filter. backend vitest 166/166; pool/proxy/createServer signatures untouched. | `apps/backend/src/lib/metamcp/session-client-registry.ts`, `apps/backend/src/lib/metamcp/consumer-identity-resolver.ts`, `apps/backend/src/lib/metamcp/log-store.ts`, `apps/backend/src/lib/metamcp/metamcp-middleware/auditing.functional.ts`, `apps/backend/src/lib/metamcp/metamcp-middleware/functional-middleware.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/backend/src/routers/public-metamcp/openapi/handlers.ts`, `apps/backend/src/routers/public-metamcp/openapi/tool-execution.ts`, `apps/frontend/app/[locale]/(sidebar)/live-logs/page.tsx`, `packages/zod-types/src/logs.zod.ts`, `UMBRELLA_FORK.md` | Partly — the `clientName`/category schema + audit-identity pattern are upstreamable; the api-key/OAuth resolution is fork-shaped (depends on our auth middleware's authReq fields). |

| 2026-06-29 | [#40](https://github.com/Umbrella-IT-Group/metamcp/pull/40) | **Fix: Streamable-HTTP tool_call consumer attribution** (follow-up to #39). #39's sessionId-keyed registry missed because the namespace pool pre-warms idle servers under a placeholder `idle_<ns>_<ts>` sessionId and converts them on use WITHOUT rebuilding the handler context — so the proxy's `context.sessionId` is the placeholder, not the real session, and the registry lookup returned nothing (Streamable-HTTP tool_calls logged with no consumer; the CLIENT session event + OpenAPI tool_calls were unaffected — they don't read the registry). Verified live: CLIENT showed `← Umbrella Hermes`, the tool_call was blank. Fix: expose the proxy `handlerContext` on `MetaMcpServerInstance` and stamp `clientName` onto it right after `getServer` (new-session + lazy-recovery paths); handler closures read `context.clientName` by reference so the late set is seen by the audit middleware. Drops the now-redundant `session-client-registry.ts`; `ClientIdentity` moves into the resolver. tsc clean, vitest 166/166. | `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/lib/metamcp/metamcp-server-pool.ts`, `apps/backend/src/lib/metamcp/metamcp-middleware/auditing.functional.ts`, `apps/backend/src/lib/metamcp/consumer-identity-resolver.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts` (removes `session-client-registry.ts`), `UMBRELLA_FORK.md` | Pairs with #39 — same upstreamability. |
| 2026-06-30 | [#41](https://github.com/Umbrella-IT-Group/metamcp/pull/41) | **CI: path-filter the umbrella build, scope the buildx cache, add manual GHCR retention** — `umbrella-build.yml` fired a full amd64 image build+push on every push/PR to `umbrella` regardless of what changed, including docs-only edits (this file included) that `.dockerignore` already excludes from the build context. Adds a `paths:` allowlist (cross-checked against every `Dockerfile` COPY + `turbo.json`'s globalDependencies) to both the `push:` and `pull_request:` triggers so non-build changes no-op. Scopes `cache-from`/`cache-to` to `scope=metamcp-umbrella` so this pipeline's GHA cache stops competing with `docker-publish.yml`'s tag-triggered builds. New `ghcr-cleanup.yml` (`workflow_dispatch` only, dry-run default, `keep-n-tagged: 20`, excludes `latest`) gives manual retention over the `metamcp` package without any schedule that could race the `:latest` pointer `Umbrella-MCP-Server/metamcp/compose.fragment.yml` pulls. | `.github/workflows/umbrella-build.yml`, `.github/workflows/ghcr-cleanup.yml`, `UMBRELLA_FORK.md` | No — Umbrella-specific CI/infra housekeeping. |
| 2026-07-01 | [#42](https://github.com/Umbrella-IT-Group/metamcp/pull/42) | **CI: drop nonexistent `metamcp-docker-per-mcp` from GHCR cleanup** — corrects PR #41's `ghcr-cleanup.yml`: that package was never published to GHCR, so listing it made the cleanup job 404 after successfully pruning `metamcp`. Restricts `packages:` to the one real package. | `.github/workflows/ghcr-cleanup.yml` | No — Umbrella-specific CI/infra housekeeping. |
| 2026-07-14 | [#73](https://github.com/Umbrella-IT-Group/metamcp/pull/73) | **Merged 2026-07-14T23:28Z (`76e65ef`), squash, after a review-fix round.** **Observability — console WARN/ERROR mirroring, log rotation, truthful health pool config (Track C2)** — three verified prod defects from the 2026-07-14 incident. **(1)** `logger.ts` console mirroring mirrored ONLY `level==="INFO"` for the `info` mode, so prod (LOG_LEVEL=info) showed a clean `docker logs` while `app.log` carried 278 cap-refusal WARNs; rewritten to threshold semantics (`SEVERITY_RANK` + `CONSOLE_FLOOR`) so each mode mirrors its own level AND everything more severe — `info` now carries WARN+ERROR, `errors-only`/`all`/`none` unchanged, line format preserved. **(2)** No in-container rotation (app.log grew 98MB/25h); the logger now rolls `<name>` to `<name>.1` (single generation, prior `.1` overwritten) on any write past `LOG_MAX_SIZE_MB` (env, default 50) and reopens fresh — dependency-free, crash-safe (rotation failure swallows + one console line + counter reset, never throws into the logging path), fd owned via `openSync` so `renameSync` never races the stream's async open, counter seeded from on-disk size across restarts. **(3)** `/health/upstream` never surfaced the global cap; the pool block now reports effective per-server cap, `max_total_connections`, and `total` via a new read-only `McpServerPool.getPoolConfig()` (pool's own fields, not a re-parse). **Flagged out of scope:** `getInstance()` hardcodes the singleton's caps (`100`/`5`), bypassing the constructor's env-parse — so `MAX_CONNECTIONS_PER_SERVER=50` has no effect and the pool truthfully enforces 5; making the pool *honor* the env is a pool-behavior change, not this observability lane. 14 new tests (logger mode matrix + rotation + pool-config getter); backend vitest 311 green, `tsc --noEmit` clean, `pnpm build` green. `LOG_MAX_SIZE_MB` added to `turbo.json` globalEnv. | `apps/backend/src/utils/logger.ts`, `apps/backend/src/utils/logger.test.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool-config.test.ts`, `apps/backend/src/index.ts`, `turbo.json`, `UMBRELLA_FORK.md` | Partly — the threshold mirroring + size-based rotation are straight robustness fixes to code that exists upstream unchanged; the `/health/upstream` pool block is fork-only (endpoint is fork PR #6/#34). |
| 2026-07-14 | [#74](https://github.com/Umbrella-IT-Group/metamcp/pull/74) | **Merged 2026-07-14T23:30Z (`42a9fed`), squash; retargeted to `umbrella` after #73. Verified against a running image: `/health/upstream` reports the configured `max_total_connections` / `max_connections_per_server` — the first time the env caps have ever been in force.** **Pool caps honor MAX_TOTAL_CONNECTIONS / MAX_CONNECTIONS_PER_SERVER env — getInstance hardcoded 100/5 since 806c2b2** — independent audit confirmed `McpServerPool.getInstance()` has unconditionally passed `maxTotalConnections=100` (hardcoded, not even a parameter) and defaulted `maxConnectionsPerServer` to a literal `5` since commit `806c2b2`, so the constructor's own `MAX_TOTAL_CONNECTIONS`/`MAX_CONNECTIONS_PER_SERVER` env-parse defaults never ran — verified via `git show 806c2b2`, the commit that introduced the hardcoding (previously `getInstance` passed no cap arguments at all, letting the constructor's env-parse govern correctly). Two silent-config-drift classes trace to this: a raised `MAX_TOTAL_CONNECTIONS` never takes effect (the pool keeps enforcing 100), and a raised `MAX_CONNECTIONS_PER_SERVER` likewise never takes effect (the pool keeps enforcing 5) — this is the root cause PR #73's `/health/upstream` truthfulness fix made *visible* (and explicitly flagged out of its own scope). Fix: `getInstance()`'s three params are now optional with no default of their own, so an omitted argument falls through to the constructor's env-parse instead of silently overriding it; explicit arguments (the override path tests need) still win. Adds `McpServerPool.resetInstanceForTests()` (test-only; production never calls it) so a singleton can be exercised against more than one env configuration per test process — without it, `getInstance()` itself (the actual prod path) was untestable, which is why PR #73's `mcp-server-pool-config.test.ts` could only exercise the raw constructor (reworded there to stop overclaiming once this gap was found). 4 new tests in `mcp-server-pool-getinstance-env.test.ts`: the silent-override regression (zero-arg `getInstance()` reflects env — the real prod call shape), defaults unchanged when env unset, explicit args still win, reset seam proven by identity. Verified red-before-green (reverted locally, all 4 failed, restored). backend vitest 319/319 green (315 inherited + 4 new), `tsc --noEmit` clean, `pnpm build` green. | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool-getinstance-env.test.ts`, `UMBRELLA_FORK.md` | Yes — the hardcoded-singleton-caps bug and the `resetInstanceForTests()` test seam pattern both exist upstream too (upstream's `getInstance()` has the identical shape); straight robustness fix. |
| 2026-08-04 | [#88](https://github.com/Umbrella-IT-Group/metamcp/pull/88) | **OAuth client create/list/delete UI** — there was NO OAuth-client management surface at all (the app's `frontend.oauth` router manages OAuth *sessions* — this gateway's outbound creds to upstream MCP servers — not the *clients* that authenticate INTO it; the two are different tables and opposite directions of trust). Before this, minting a client for a Claude connector meant a hand-rolled `POST /oauth/register` curl. #88 builds the whole create/list/delete admin surface on a dedicated `/oauth-clients` sidebar page (under API Keys). DCR's validation + credential minting were lifted out of the express `/oauth/register` handler into a shared `client-registration.ts` that BOTH the REST endpoint and the new `frontend.oauthClients` tRPC router call — no reimplementation, DCR's validation order + RFC 7591 error bodies unchanged (the one delta: the 201 now echoes the normalized `contacts` value it actually persists instead of the raw input). All three verbs are `adminProcedure` (no per-user ownership model exists for clients). The secret is returned exactly once on create; `OAuthClientListItemSchema` carries only `has_client_secret`, so the list surface structurally cannot leak it — enforced by the tRPC `.output()` contract AND the serializer, two independent layers, both mutation-verified. Fixed two real bugs the work surfaced: `createAppRouter` enumerates sub-routers explicitly, so wiring `createFrontendRouter` alone left `oauthClients` unreachable on the wire; and `OAuthClientSchema.updated_at` was `.optional()` against a NOT NULL column. Gates: 520 backend tests (+28, incl. mutation-checked secret-leak + admin-gating), backend lint returned to the exact 32-warning baseline, `tsc --noEmit` clean both apps, `pnpm build` green. Note: `client_secret` is stored PLAINTEXT — pre-existing (DCR already did; `hashClientSecret` in `utils.ts` has no call site), not worsened by this PR. | `apps/frontend/app/[locale]/(sidebar)/oauth-clients/page.tsx`, `apps/backend/src/routers/oauth/client-registration.ts`, `apps/backend/src/routers/oauth/registration.ts`, `apps/backend/src/routers/oauth/oauth-clients.impl.ts`, `apps/backend/src/db/repositories/oauth.repo.ts`, `packages/zod-types/src/oauth.zod.ts`, `packages/trpc/src/router.ts` + router wiring + tests | Yes — upstream has the identical gap (client creation is DCR-only, no UI); an admin-gated create/list/delete surface reusing the existing DCR validator is a general capability. |
| 2026-08-04 | [#87](https://github.com/Umbrella-IT-Group/metamcp/pull/87) | **White-label branding — env-configurable product name, org name, logo, description** — the three (four, counting `<meta description>`) hardcoded Umbrella surfaces become deployment config so one fork image serves any deployment. Canonical vars `NEXT_PUBLIC_BRANDING_{PRODUCT_NAME,ORG_NAME,LOGO_PATH,DESCRIPTION}`; `docker-entrypoint.sh` promotes the short `BRANDING_*` aliases into them before either server process starts (the `NEXT_PUBLIC_` prefix is forced by next-runtime-env, which only republishes that prefix to the browser, and the sidebar brand is a client component — promoting in the entrypoint rather than compose covers `docker run`/k8s too and makes a server-render vs client-hydration brand mismatch impossible). Root layout's static `metadata` becomes `generateMetadata()` so the title resolves per request; every route already renders dynamically via `<PublicEnvScript />`'s `unstable_noStore()`, so a rebrand is a container restart, never an image rebuild. Custom logo is a file mounted under `apps/frontend/public/` (a subdirectory — mounting `public/` itself would shadow the bundled assets); verified empirically that Next enumerates `public/` once at server start (present-at-start → 200, added-after-start → 404), so the mount must exist before the container starts. `resolveLogoPath()` accepts an app-served absolute path only — a host `next/image` doesn't accept THROWS in `next dev` (that check is compiled out of prod: `image-loader.js` wraps it in `NODE_ENV !== 'production'`, Next's own comment says "this should only error in development") and in production instead yields a silently broken logo behind an opaque optimizer `400 "url" parameter is not allowed`; declaring a remote host would also mean editing `next.config.js`, i.e. the image rebuild this feature exists to avoid. So a bad value falls back to the bundled asset with a `console.warn` (proven end to end: HTTP 200, bundled asset, warning logged). The check validates SHAPE, not sanitization — any `/`-leading url is served internally by the optimizer, so a malformed one is an internal 400/404, and the value is operator-supplied container config regardless. **Zero-config parity proven by diffing the rendered `/en` document before vs after with no branding env set** (build-id + chunk hashes normalized): the only non-build-noise delta in 32KB is one `<img>` tag — `alt` now tracks the org name and `w-10` → `w-auto` so a non-square logo isn't squashed; same asset, same srcSet, same 40x40 rendered box; title/description/heading byte-identical. Same build artifact renders the override with env set and the defaults with env cleared — no rebuild between. Adds vitest to `apps/frontend` (first frontend test lane; config mirrors `apps/backend/vitest.config.ts`, lockfile delta 3 lines since vitest was already resolved for the backend) + a turbo `test` task + root `pnpm test`. 13 new tests, verified red-before-green by mutation (disabling the logo guard fails 4; changing a default fails the parity pin; restore → 13/13). Gates: frontend 13/13 + backend 492/492 green, `check-types` 2/2 clean, `pnpm build` 4/4 green, lint **21 warnings vs the 22-warning `umbrella` baseline** (adds none, incidentally clears a pre-existing `prettier/prettier` warning on the brand heading), `prettier --check` clean. NOT configurable, deliberately: `apps/frontend/app/icon.png` (the favicon) is a Next 15 special file resolved at build time, so a deployment wanting its own favicon still replaces that file; and the i18n locale strings still say **MetaMCP** in ~15 places (`navigation.json`, `endpoints.json`, `namespaces.json`, `logs.json`, `api-keys.json`, `common.json`) — that is upstream's brand rather than Umbrella's, so it is outside this change's stated scope, but it means "white-label" is true of these four surfaces and not yet of the whole UI. | `apps/frontend/lib/branding.ts`, `apps/frontend/lib/branding.test.ts`, `apps/frontend/vitest.config.ts`, `apps/frontend/app/layout.tsx`, `apps/frontend/app/[locale]/(sidebar)/layout.tsx`, `apps/frontend/package.json`, `docker-entrypoint.sh`, `docker-compose.yml`, `example.env`, `turbo.json`, `package.json`, `UMBRELLA_FORK.md` | Yes — upstream hardcodes its own "MetaMCP" branding in exactly these files; the env-with-current-defaults shape is a drop-in white-label for any deployer and changes nothing for someone who sets no vars. |
| 2026-08-13 | [#91](https://github.com/Umbrella-IT-Group/metamcp/pull/91) | **Manual remediation of the Dependabot backlog Dependabot itself could not act on — clears 29 of 31 open alerts (15 of 16 HIGH)** — Dependabot returned `security_update_not_possible` with an EMPTY conflicting-dependencies field on these for 11+ days, for two reasons it has no lever against. (1) *Transitive under an exact-pinned parent* — `hono`, `@hono/node-server` and `ip-address` have no manifest entry of their own; their only parent is `@modelcontextprotocol/sdk`, pinned exact at `1.29.0` in both apps, and Dependabot's transitive path needs a manifest range to widen. The SDK's own ranges (`hono ^4.11.4`, `@hono/node-server ^1.19.9`, `express-rate-limit ^8.2.1` → `ip-address ^10.2.0`) already permit every patched version, so the lockfile was frozen below them, not blocked by a real conflict. (2) *Already governed by `pnpm.overrides`* — `js-yaml` and `brace-expansion` sat under existing floors, which Dependabot cannot edit at all. New floors: `hono@4 ~4.12.34`, `@hono/node-server@1 ~1.19.15`, `ip-address@10 ~10.3.1`. Raised floors: `js-yaml@4 ^4.2.0`→`^4.3.1`, `brace-expansion@1 ^1.1.13`→`^1.1.18`, `brace-expansion@2 ^2.0.3`→`^2.1.4`. Direct bumps folded in so the wave costs one deploy: nanoid (backend), postcss + next (frontend). Closes #90, #89, #83. **The new floors use `~`, not `^`, and that ceiling is the point** — the patch line of each advisory clears every open alert on that package, so this ships the security fix without also taking a minor on the gateway's HTTP bridge (`^` would float hono 4.12 → 4.13 under the live proxy). The cost is that a future advisory first patched in a MINOR will not be picked up by `pnpm install` — it stalls silently behind the tilde. **Review trigger: widen the floor deliberately (`~` → `^`, or bump the tilde base) when an advisory lands whose first patched version is a minor rather than a patch, or when a future `@modelcontextprotocol/sdk` bump requires newer `hono` / `@hono/node-server` / `ip-address` than the tilde admits.** Pre-merge fold-ins from review, so the fleet spends one prod deploy instead of two: `tar@7 ^7.5.16`→`^7.5.21` (alert #105, resolves 7.5.22 — `^` kept because raising an existing floor keeps its operator, and tar is build-time under `@tailwindcss/oxide`, off the request path, so the minor-float risk that motivates `~` does not apply); new `fast-uri@3 ~3.1.5` (alerts #95/#113, 2 HIGH, chain `@modelcontextprotocol/sdk` → `ajv@8.20.0` → `fast-uri`, resolves 3.1.5 — `~` per the policy above, same exact-pinned-parent shape as the hono floors). **The `postcss@<8.5.10` override was slated for deletion as inert and is NOT inert — verify before deleting it.** The claim was that postcss resolves 8.5.26 monorepo-wide so the selector matches nothing; in fact `next@15.5.23` depends on `postcss` pinned EXACT at `8.4.31`, and this override is what rewrites it up. Deleting it reintroduced `postcss@8.4.31` into the lockfile on the first install (caught by the lockfile drift audit), which is below both open postcss advisories (#104 needs 8.5.18, #112 needs 8.5.23) — a silent security regression. Kept and raised instead: `postcss@<8.5.10 ^8.5.10`→`postcss@<8.5.23 ^8.5.23`, which fixes the real complaint (the floor sat below the advisory minimum, so it read as stale) while holding the resolution at 8.5.26. Benign side effect, pre-existing in kind: pnpm rewrites recorded optional-peer ranges to the override value, so `postcss-load-config@6.0.1` (upstream `>=8.0.9`) and `tsup@8.5.0` now read `^8.5.23` in the lockfile. Deliberately NOT bumped: `sharp` (alert #94, the one remaining HIGH — needs a MINOR bump of a native module, gets its own reviewed lane) and `@modelcontextprotocol/sdk`. Still open after merge: #94 `sharp` HIGH, #92 `body-parser` LOW (needs 2.3.0, resolves 2.2.2, transitive under express). Gates: `pnpm install --frozen-lockfile` exit 0, `pnpm build` 4/4 tasks, `pnpm check-types` 2/2, `pnpm test` 48 files / 520 backend + 1 file / 13 frontend all passing with 0 turbo cache hits. `pnpm lint` still fails on the pre-existing frontend `--max-warnings 0` warnings (28-line warning fingerprint diffed byte-identical against the pre-change baseline via stash; ungated in `umbrella-build.yml` by its own comment). | `package.json`, `pnpm-lock.yaml`, `apps/backend/package.json`, `apps/frontend/package.json`, `UMBRELLA_FORK.md` | No — dependency hygiene on our line. |
| 2026-08-13 | [#92](https://github.com/Umbrella-IT-Group/metamcp/pull/92) | **OAuth grant success logging — the instrument for the daily-disconnect root cause** — token.ts logged nothing on successful grants, so whether Claude clients ever call `grant_type=refresh_token` (vs. re-running the full auth-code flow after each access-token expiry) was unprovable from server logs; that was the load-bearing unknown in the 2026-08-13 disconnect investigation. Adds one `logger.info` per issued token on BOTH grant paths: grant type, client id, user, access-token last-4, `rotated=true` on refresh redemption; `client_name` only on the auth-code path where the client row is already loaded (the refresh path deliberately skips a DB round-trip for logging — every refresh chain starts with an auth-code grant, so the id-to-name mapping is already in the log). **Injection-safe by review:** `client_name` arrives verbatim from the UNAUTHENTICATED `/oauth/register` DCR endpoint, and naive quoting let a hostile name containing a newline forge whole `[oauth] token issued` lines in this very log; fixed with `JSON.stringify` + 100-char clamp — benign output stays byte-identical, and a regression test proves single-line output (mutation-verified: fails red under naive quoting). 7 tests total, including a secret-leak guard asserting no whole credential appears across EVERY logger call the request produced (mutation-verified: logging the full token fails 4 tests). Also closed the stale #79 "rebase REQUIRED" note in this ledger (resolved same-day 2026-07-29 by `04d9f85`, reconfirmed CLEAN 2026-08-13). Pairs with Umbrella-MCP-Server #488 (deployed access TTL 24h→30d): after ~48h of real traffic these lines settle whether clients refresh or re-auth. | `apps/backend/src/routers/oauth/token.ts`, `apps/backend/src/routers/oauth/token.test.ts`, `UMBRELLA_FORK.md` | Yes — upstream logs only token failures; injection-safe success-path grant observability is a general gap. |
| 2026-08-13 | [#94](https://github.com/Umbrella-IT-Group/metamcp/pull/94) | **Spec session termination toward backends on pool teardown — the session-abandonment leak (Phase 0).** Every pool teardown site calls `client.cleanup()`, which did `transport.close()` + `client.close()`; the SDK's `Protocol.close()` only closes the LOCAL transport and never emits the MCP spec's `DELETE` with `Mcp-Session-Id`, so backends never learned a session was gone and kept its session object forever. Measured on prod 2026-08-13: **1027 sessions created / 0 terminated** against a single backend, ~1.17MB retained each, one backend OOM-cycling roughly every 4 days; all 17 backend vendor MCP servers affected. The SDK has shipped `StreamableHTTPClientTransport.terminateSession()` the whole time (pinned SDK 1.29.0), but its only call site in this repo was the Inspector DELETE route (`routers/mcp-proxy/server.ts`), never the production pool. **Correction (2026-08-13, see the mcp-proxy admin-gate row below):** this row originally called that DELETE route "admin-only". It was not. It carried `betterAuthMcpMiddleware` and nothing else — a session-cookie check with no role check — so it was reachable by any member-role account until the admin gate landed. The pool-teardown finding is unaffected; only the access-control description was wrong. Fix lands at the ONE `cleanup()` choke point in `client.ts` that all 14 pool teardown sites in `mcp-server-pool.ts` funnel through, rather than at the 14 callers. **Ordering is load-bearing** — `close()` aborts the transport's abort controller, so a DELETE issued after it could never reach the wire; terminate, then close. **Fail-open by design:** teardown runs on latency-sensitive paths (idle sweep, cap eviction, `invalidateServerConnection` cascade), so a hung/unreachable/mid-restart backend must never stall pool eviction — the call is bounded by a 2s `Promise.race` and every failure becomes exactly one `logger.warn` naming the server plus the error before `close()` proceeds (nothing swallowed silently, nothing rethrown into teardown); the termination promise also carries a sink `.catch()` so a rejection arriving after the timeout won the race can't surface as an unhandled rejection. STDIO and SSE carry no HTTP session and no `terminateSession` method, so they short-circuit on the `instanceof` gate. Double-termination is safe by the SDK's own guard (`if (!this._sessionId) return;` plus `_sessionId` cleared after a successful DELETE), so a double teardown of one `ConnectedClient` — reachable as both an idle and an active slot during an invalidation cascade — sends one DELETE, not two; a FAILED DELETE deliberately leaves the session id, though the retry only actually fires in the double-cleanup cascade — on ordinary paths every pool caller drops the `ConnectedClient` immediately after `cleanup()`, so a failed DELETE there is an accepted orphan (status quo ante, fail-open by design). 5 new tests on a surface that had ZERO client-face DELETE coverage (server-face had 8+): ordering, reject path, hang/timeout path, STDIO skip, double-teardown dedupe (mutation-verified: dropping the call, or moving it after `close()`, turns 4 of the 5 red). **Known follow-ups from review (accepted, not blocking):** (1) the m365 backend's teardown DELETE rides the M365 injected fetch, which strips transport-level Authorization outside a request-scoped ALS context — timer-driven teardowns (idle sweep, health check, shutdown) may therefore send an unauthenticated DELETE to m365 and orphan that one server's session (fail-open makes this exactly the pre-fix status quo for m365 only; fix is letting teardown bypass the injected fetch); (2) `SESSION_TERMINATE_TIMEOUT_MS` (2s) is a compile-time const — under a steady warn-stream from one backend, raising it costs a code change + image build + GitOps deploy, deliberate because env-tunables here require turbo.json globalEnv + README registration; (3) crash-cascade latency: the 2s bound is per client, and `invalidateServerConnection` tears down sequentially, so a hung-but-accepting backend adds up to 2s x N active sessions to crash recovery — accepted for the leak fix, revisit if recovery latency ever matters. | `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/client-connect.test.ts`, `UMBRELLA_FORK.md` | Yes — upstream `metatool-ai/metamcp` has the identical abandonment bug; `Protocol.close()` never sends the spec DELETE there either. |
| 2026-08-13 | [#95](https://github.com/Umbrella-IT-Group/metamcp/pull/95) | **`@modelcontextprotocol/sdk` 1.29.0 → 1.30.0 currency bump (Phase 1) — exact pin, both apps.** Follows the PR #64 pattern (that row is the last SDK migration) but is a patch-grade bump, not a migration: `LATEST_PROTOCOL_VERSION` and `SUPPORTED_PROTOCOL_VERSIONS` are unchanged, and an audit the same day diff-verified 1.29→1.30 as non-breaking for every SDK file this fork touches. What 1.30.0 buys: (a) **content-type correctness** — the `mediaType` parser no longer mis-reads a `Content-Type` carrying parameters (charset, boundary), which is what a proxied backend or an intermediary can add; (b) **a stdio ReadBuffer 10MB cap in the SDK — which this fork does NOT receive**: review caught that the pool's STDIO path is `ProcessManagedStdioTransport`, which uses the fork's own VENDORED, uncapped `ReadBuffer` (`lib/stdio-transport/shared.ts`), not the SDK's — so a runaway STDIO backend can still balloon the vendored buffer unbounded. Porting the 10MB cap into the vendored copy is a named follow-up in "Deferred / future work"; (c) **SSE keep-alive availability** (see below). Also in the delta but inert for us: a zod-compat internal refactor, `_resumableStreams`, and better multi-issue validation messages. **Lockfile drift audited and surgical** — `git diff -U0 -- pnpm-lock.yaml` is 16 lines, all four hunks the SDK itself (two importer specifiers, the packages-block resolution/integrity, the snapshot key); ZERO transitives moved. The `pnpm.overrides` tilde floors from PR #91 still satisfy the SDK's declared ranges without widening: SDK 1.30.0 asks `@hono/node-server` at `^1.19.9` or `^2.0.5` and `hono` at `^4.11.4`, our `~1.19.15` / `~4.12.34` resolve to `1.19.17` / `4.12.34`, and neither line appears in the lockfile diff. **The fork's one SDK-private dependency was re-verified against the INSTALLED 1.30.0, not assumed:** `lib/metamcp/transport-recovery-hydration.ts` reaches into `_webStandardTransport._initialized` / `.sessionId`, and in the shipped 1.30.0 build the Node wrapper still constructs `_webStandardTransport` in its constructor, the inner transport still initializes `_initialized = false` there, still assigns `sessionId` + `_initialized = true` only in the initialize branch, and `validateSession` still gates on `!this._initialized`. Executing that file's own `assertRecoveryHydrationContract()` probe against the installed build returns `true`, so the boot-time contract check passes rather than logging a STARTUP CONTRACT VIOLATION and refusing every lazy recovery. **SSE keep-alive decision: nothing to wire — it is ON BY DEFAULT, not opt-in.** `keepAliveMs` is a public option on `StreamableHTTPServerTransportOptions` (an alias of the web-standard options type) that defaults to `15000`, so all four `new StreamableHTTPServerTransport(...)` sites in this fork inherit a 15s `: keepalive` comment frame per SSE stream with no code change. Explicitly restating the SDK default at four call sites would be noise, so instead a fail-loud tripwire pins the contract (`routers/public-metamcp/sdk-sse-keepalive.test.ts`, same posture as `assertRecoveryHydrationContract`); it is a REAL tripwire, verified red against the old build (`_keepAliveMs` is `undefined` at 1.29.0, `15000` at 1.30.0). That default **narrows but does not close** the half-open-TCP blind spot documented on `dispatchTracked`: keep-alive writes push bytes at a dead peer so the kernel's retransmit timeout eventually errors the socket and settles the pending dispatch, but that chain is reasoned from SDK source rather than runtime-verified against a real half-open connection, and it fires on `tcp_retries2` timescale (order of 15 min) rather than a heartbeat-miss threshold. The blind-spot comments in `streamable-http.ts` and `public-session-sweeper.ts` are corrected in this PR to say exactly that — they previously asserted the transport does no app-level heartbeat, which this bump makes false. Residual follow-up unchanged: `SO_KEEPALIVE`, or a fork-side missed-heartbeat threshold on our own clock. Gates (pnpm 10.12.0, matching the Dockerfile): `pnpm install --frozen-lockfile` exit 0 ("Lockfile is up to date"); `pnpm build` exit 0, 4/4 tasks; `pnpm check-types` exit 0, 2/2; backend `tsc --noEmit` exit 0, zero output; backend vitest **534 passed, 50 files** (532/49 on the base commit — the +2/+1 is this PR's tripwire, no test lost); `pnpm lint` fails on `frontend#lint` with a fingerprint IDENTICAL to the base commit (22 frontend warnings, 0 backend, `--max-warnings 0`), the known pre-existing failure recorded in the #64 row (note: turbo aborts backend:lint when frontend#lint fails, so "0 backend warnings" in a repo-level run is an artifact of the abort, not a measurement — run `eslint .` in apps/backend directly for the real backend figure; it was clean when run directly). Keep-alive site count: four SERVING construction sites plus the contract probe in transport-recovery-hydration.ts. | `apps/backend/package.json`, `apps/frontend/package.json`, `pnpm-lock.yaml`, `apps/backend/src/lib/metamcp/transport-recovery-hydration.ts`, `apps/backend/src/lib/metamcp/transport-recovery-hydration.test.ts`, `apps/backend/src/routers/public-metamcp/sdk-sse-keepalive.test.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/backend/src/routers/public-metamcp/public-session-sweeper.ts`, `UMBRELLA_FORK.md` | No — dependency currency on our own pin. |
| 2026-08-13 | [#96](https://github.com/Umbrella-IT-Group/metamcp/pull/96) | **`frontend.apiKeys.list` returned public keys' RAW plaintext value to every member — a critical credential-disclosure defect, now masked.** `serializeApiKeyList` emitted `key: apiKey.key` verbatim, and `findAccessibleToUser` deliberately returns the caller's own keys PLUS every public ('everyone') key, so the leak was not scoped to what a user owns: any self-registered member could open the API Keys page and read gateway-wide API keys in plaintext. A single member-role call to this procedure is enough to recover every public key verbatim. The admin cross-user view (`serializeAdminApiKeyList`, PR #85) had already been prefix-masked for this reason; the member-facing list was left raw on the argument that a public key "exists to be handed to every consumer anyway" — wrong, because distributing a key to its intended consumers out-of-band is not the same as every member being able to read it on demand, and member enrollment was therefore the entire exploit chain. Fix masks at both layers between the DB row and the wire: the serializer now emits `key_prefix` (first 10 chars + `…`, via a shared `keyPrefix` helper the admin serializer also uses so the two can never drift), and `ListApiKeysResponseSchema` drops `key` entirely so a regressed serializer would still be stripped at the tRPC `.output()` boundary. Field RENAMED rather than masked in place so any consumer needing a usable secret fails to compile instead of silently shipping a truncated key. Two frontend consumers followed: the API Keys table's reveal + copy-full controls are gone (nothing left to reveal; full values now exist exactly once, in the mint dialog) with a note pointing members at requesting an endpoint-scoped key, and the endpoints list's "copy URL with API key" items always emit the `YOUR_API_KEY` placeholder — they previously substituted the caller's first active key, which for a member is whatever public key exists, making them a second distribution channel for the same secret. Five tests pin it, including a regex sweep of the whole serialized response for any `sk_mt_` token longer than a prefix. | `apps/backend/src/db/serializers/api-keys.serializer.ts`, `apps/backend/src/trpc/api-keys.impl.ts`, `apps/backend/src/trpc/api-keys.impl.test.ts`, `packages/zod-types/src/api-keys.zod.ts`, `apps/frontend/app/[locale]/(sidebar)/api-keys/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/endpoints/endpoints-list.tsx`, `UMBRELLA_FORK.md` | Yes — upstream has the identical serializer and the identical exposure. |
| 2026-08-13 | [#99](https://github.com/Umbrella-IT-Group/metamcp/pull/99) | **Admin-gate the whole `/mcp-proxy` surface (member-role RCE) + `frontend.oauth.get` upstream-credential leak.** Two confirmed member-role findings, one PR. **(A) Remote code execution as the gateway user, reachable by any member.** `GET /mcp-proxy/server/stdio?transportType=STDIO&command=…&args=…&env=…` reads the command, its args and its env straight off the QUERY STRING (`routers/mcp-proxy/server.ts` `createTransport`), resolves them through `findActualExecutable`, and hands them to `ProcessManagedStdioTransport.start` -> `spawn()` merged over the backend's FULL `process.env` (`lib/stdio-transport/process-managed-transport.ts`) — so anything the gateway process can read (DATABASE_URL, BETTER_AUTH_SECRET, M365_TOKEN_KEK, every backend credential) is in the child's environment. The only gate on that router was `betterAuthMcpMiddleware`, which validates a session cookie, sets `req.user` and `req.session`, and performs NO role check whatsoever — read the file end to end: there is no `role`, `isAdmin` or `requireAdmin` in it. Since RBAC landed (fork PR #68) every member-role account on the gateway therefore had arbitrary command execution on the host, and `POST /mcp` (same `createTransport`) plus `GET /sse` were the same hole by other verbs. Fix: a new `requireAdminMcpMiddleware` mounted on the PARENT router in `routers/mcp-proxy.ts`, immediately after `betterAuthMcpMiddleware`, covering every route under `/mcp-proxy` — both sub-routers, every verb, and anything added later. The two sub-routers previously applied `betterAuthMcpMiddleware` themselves; that is removed in favour of one parent-level pair, because a per-sub-router gate is one forgotten `use()` from a hole, and because double-mounting would double the per-request get-session DB read. **Why `req.user.role` is trustworthy and the gate is real, not decorative:** `role` is a better-auth `additionalFields` entry declared `input: false` in `apps/backend/src/auth.ts`, so no client can supply it at sign-up or user-update — it comes from the DB column default plus deliberate admin action; no `cookieCache` is configured on the session, so `GET /api/auth/get-session` re-reads the user row every request and a demotion is immediate; and it is the SAME field from the SAME call that `adminProcedure`/`requireAdmin` (`packages/trpc/src/trpc.ts`) has gated the product's administrative tRPC surface on since PR #68, plus what the live admin UI reads (`app/[locale]/(sidebar)/api-keys/page.tsx` `authClient.getSession()` -> `user.role`). The test is positive (`role === "admin"` passes), so a missing user, a missing role, an unknown role or any future payload-shape change all land on 403 — fail-closed by construction, pinned by tests for absent/unknown/wrong-case roles. Anonymous callers still get 401 from the session check, never 403, proving the ordering. **(B) `frontend.oauth.get` handed members upstream OAuth secrets.** It was `protectedProcedure` (`packages/trpc/src/routers/frontend/oauth.ts`) over an impl (`apps/backend/src/trpc/oauth.impl.ts`) that takes only an `mcp_server_uuid` with no ownership and no role check, returning `tokens` (access_token + refresh_token), `client_information` (client_id + client_secret) and `code_verifier` UNREDACTED (`db/serializers/oauth-sessions.serializer.ts` passes all three through verbatim) — i.e. any member could enumerate server UUIDs and read the gateway's credentials for every upstream MCP server. Now `adminProcedure`, matching its sibling `upsert`, which has been admin-gated since the 2026-07-14 MINOR sweep and writes the very same rows. **Correction to the PR #94 row above:** that row described the Inspector's `DELETE /mcp-proxy/server/mcp` as "admin-only". It was not — session-only, no role check — until this PR; the #94 pool-teardown finding itself is unaffected. **UI impact, accepted:** `/mcp-proxy` is the MCP Inspector, and `frontend.oauth.get` is called only from `apps/frontend/lib/oauth-provider.ts` via `hooks/useConnection.ts`, i.e. the Inspector pages (`mcp-inspector`, `mcp-servers/[uuid]`, `namespaces/[uuid]`). Members lose the Inspector's connect/test panels on those pages; they were already half-broken for members because the same provider's `saveTokens`/`saveClientInformation` calls go through the admin-gated `oauth.upsert`. Nothing on the production data path moves: API-key and OAuth clients reach namespaces through `routers/public-metamcp` mounted at `/metamcp`, a different router that this gate does not touch. 40 new tests across two files — the RCE gate drives the REAL `mcpProxyRouter` composition over a real socket (m365.test.ts idiom), mocking only the auth seam and the two sub-routers, and asserts the sub-router handler was never invoked, not merely that the status changed. Mutation-verified both ways: deleting the `mcpProxyRouter.use(requireAdminMcpMiddleware)` line turns 19 of 36 red ("expected 200 to be 403"); reverting `get` to `protectedProcedure` turns 2 of 4 red ("promise resolved … instead of rejecting"). Gates: `pnpm build` 4/4 forced; `pnpm check-types` 2/2 forced; backend `tsc --noEmit` exit 0; backend vitest **574 passed / 52 files** (534 / 50 on the base commit `49473c6`, +40 / +2 from this PR, nothing lost); `pnpm lint` still fails `backend#lint` (34 warnings) and `frontend#lint` (22 warnings) at `--max-warnings 0` with a warning-line fingerprint DIFF-IDENTICAL to the base commit — no warning added, none removed. | `apps/backend/src/middleware/require-admin-mcp.middleware.ts`, `apps/backend/src/routers/mcp-proxy.ts`, `apps/backend/src/routers/mcp-proxy/server.ts`, `apps/backend/src/routers/mcp-proxy/metamcp.ts`, `apps/backend/src/routers/mcp-proxy.admin-gate.test.ts`, `packages/trpc/src/routers/frontend/oauth.ts`, `apps/backend/src/trpc/oauth-get-admin-gate.test.ts`, `UMBRELLA_FORK.md` | No — both gates key on `users.role`, which is a fork-only addition (PR #68). Upstream has no RBAC, so the patch does not apply as-is; the underlying shapes (query-string `spawn()` behind a session-only check, unowned `oauth.get`) are worth reporting upstream as a hardening note rather than as this diff. |
| 2026-08-14 | [#100](https://github.com/Umbrella-IT-Group/metamcp/pull/100) | **Security-review hardening wave 2 — confirmed-safe subset (topology, stack, scope, secrets). Does NOT close FIND-001/005.** A follow-up security review surfaced a further set of findings; this ships the confirmed-safe, non-regressing subset after a `cowork-reviewer` adversarial pass (verdict: integrate-with-fixes) + foreman firsthand verification + worker red-green proofs. **(1) DCR scope** → constant `GRANTED_OAUTH_SCOPE = "mcp"` at every issue site (client-registration, authorization, token challenge `:554`, metadata, introspection fallback) + `localhost:12009` base-URL leak → `getBaseUrl`. Scope carries NO privilege (`checkOAuthAccess` authorizes on token `user_id` + endpoint ownership, never the scope string), so this is honest labelling + defense-in-depth, not an access change; migration **`0026`** flips the three OAuth-table defaults `admin`→`mcp` and rewrites legacy `admin` rows (auto-applies via docker-entrypoint's `drizzle-kit migrate`). **(2) FIND-007** tRPC stack-trace strip via `errorFormatter` in BOTH trpc instances **+ `INTERNAL_SERVER_ERROR` message mask** — the config router's procedures are `publicProcedure` with no try/catch, so an unexpected throw wrapped as ISE handed the raw pg/driver `message` (internal hostnames, SQL) to an unauthenticated caller; masked to a constant when `data.code === "INTERNAL_SERVER_ERROR"`, user-facing FORBIDDEN/NOT_FOUND/UNAUTHORIZED messages preserved (pinned by tests). **(3) Topology disclosure** — `/health/upstream` `servers[]` + `pool` now admin-gated via a soft session check (`isAdminHealthRequest`, fails closed to non-admin, never 401s so monitors keep their liveness 200); the second-door `GET /metamcp/` estate listing gated the same way (anonymous path returns the banner and no longer touches postgres at all); and live `activeSessionIds` + `idleNamespaceUuids` stripped from the unauthenticated `GET /health/sessions` (a prior fix removed a top-level `sessionIds` array but the spread of `getPoolStatus()` re-published them one level down — session-replay material). **(4) Secret redaction** in `mcp-servers` + `namespaces` serializers (env/bearer/headers/command/args/internal-url) for non-admin via `includeSecrets = role==="admin"`; `logs.get` → `adminProcedure`, and the frontend logs store's stop-condition now halts on FORBIDDEN so the admin-gate can't put every member browser in a permanent 2s 403 poll-loop. **(5)** `/health/upstream` 500 branch now logs + returns a constant (was echoing `error.message`). **NOT in this PR — CRITICAL, tracked as a dedicated fast-follow:** the DCR escalation is still reachable by a different route — `/oauth/callback` accepts an unsigned, non-session-bound `params` blob and mints an auth code for the current browser's session → login-CSRF admin auth-code injection; privilege comes from `user_id`, not scope, so the substitution above is correct but orthogonal. Fix = HMAC-signed blob + pre-login nonce cookie, real-client tested (maintainer ruling 2026-08-14). **SUPERSEDED 2026-08-14 — see the consent-screen row below:** the ruling was revised from the HMAC+nonce patch to a full RFC-standard consent screen, because signing the blob alone would have left the OTHER door open — `/oauth/authorize` itself auto-minted a code for any logged-in session with no user approval at all, which is the same escalation by a shorter path. The delivered fix closes BOTH doors and removes the `params` blob entirely rather than signing it. FIND-006 `config.getBasicAuthDisabled` left public by design (login page reads it pre-auth). **Migration coordination:** renumbered `0025`→`0026` because open PR #79 (`feat/oauth-hardening-csrf-upsert`, a DIFFERENT flow — outbound `oauth_sessions` CSRF) already claims `0025_oauth_expected_state`. `drizzle-kit migrate` applies by journal `when`; once `0026` (when 1786665600000) is live, #79's `0025` (when 1785369600000) is below-max and would be SILENTLY SKIPPED — #79 needs its `when` bumped above the then-current max before merging, not just a file renumber. Gates: backend vitest **630 pass / 2 fail** (both pre-existing `sdk-sse-keepalive`, SDK 1.30), **+12 new tests** (estate-gate real-socket, pool-status projection, ISE mask, 500 body, serializer redaction, logs stop-condition); `turbo check-types` + backend `tsc --noEmit` clean on every touched file. | `apps/backend/src/routers/oauth/{client-registration,authorization,metadata,registration,utils}.ts`, `apps/backend/src/middleware/api-key-oauth.middleware.ts`, `apps/backend/src/trpc.ts`, `packages/trpc/src/trpc.ts`, `apps/backend/src/lib/health-upstream.ts`, `apps/backend/src/index.ts`, `apps/backend/src/routers/public-metamcp.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/backend/src/db/serializers/{mcp-servers,namespaces}.serializer.ts`, `apps/backend/src/db/repositories/users.repo.ts`, `apps/backend/src/trpc/{mcp-servers,namespaces}.impl.ts`, `packages/trpc/src/routers/frontend/{logs,mcp-servers,namespaces,config,oauth-clients}.ts`, `apps/backend/src/db/schema.ts`, `apps/backend/drizzle/0026_oauth_scope_default_mcp.sql` + `meta/_journal.json`, `apps/frontend/lib/stores/logs-store.ts`, + tests, `UMBRELLA_FORK.md` | Yes (mostly) — the stack strip, ISE-message mask, `/health/upstream` + `/metamcp/` + `/health/sessions` topology gating, session-id leak, and serializer redaction all apply to upstream `metatool-ai/metamcp` unchanged; the scope substitution + `logs.get`/serializer admin-gates are fork-shaped (RBAC is fork PR #68, upstream has none). |
| 2026-08-14 | `fix/health-sessions-gate` (PR pending) | **Admin-gate the `/metamcp/health/sessions` detail — unauthenticated callers leaked live session counts + sweeper internals.** Follow-on to #100's topology work, same shape of fix as `/health/upstream` (`servers[]` + `pool`) and the `GET /metamcp/` estate listing, reached through a third door. The route stays UNAUTHENTICATED and must — external monitors probe it and a 401 would break them — so the PAYLOAD is split by role instead: every caller gets `{ status: "ok" }`, admins get the detail on top. What was public: `streamableHttpSessions.count` + `metaMcpPoolStatus.{idle,active}` + `totalActiveSessions` (the gateway's live scale and load) and the whole `publicSessionSweeper` block — `ttlSeconds`, `intervalSeconds`, `trackedSessions`, `inFlightSessions` and the reap counters, i.e. exactly how long an abandoned session survives, how often the gateway sweeps, and how many it is carrying. Together that sizes a resource-exhaustion attempt against the backend pool cap (the pool-exhaustion failure mode the connection-cap work above addresses) and times it to land between sweeps. `buildSessionsHealthPayload()` now takes a REQUIRED `isAdmin: boolean` (no default — a new call site cannot fall into the detail branch by omission) and returns `Record<string, unknown>`, built ADDITIVELY on a `{ status: "ok" }` base exactly like `buildUpstreamHealthBody`: a field added to the detail block later cannot leak by someone forgetting a redaction list, and the non-admin early return means an anonymous request never reads the session manager, the pool or the sweeper at all. The handler is now `async` and consults the existing soft `isAdminHealthRequest` (fails closed to non-admin, never 401s or throws) wrapped in a `try/catch` that falls back to the withheld body — without it a future regression in the gate would take every liveness probe down; failing closed keeps the 200 and keeps the failure direction safe. **Corrected 2026-08-14: the reason given here used to be "express 4 does not catch an async handler's rejection, so it would HANG".** The fork is express 5 (`router@2.2.0`), which DOES forward a returned promise's rejection to `next(err)`, so the uncaught outcome is a 500 rather than a hang. The `try/catch` is still right — a 500 fails a probe exactly as hard as a timeout — but the failure it prevents is the one that actually happens. **Not a re-fix of the session-id leak** — #84's `sessionIds` removal and #100's `getPoolStatus()` spread projection both stand; the ids stay projected out of the admin half too, pinned by test. Scan: `/health/sessions` was the last unauthenticated DETAIL route in `streamableHttpRouter` — the three MCP legs all carry `lookupEndpoint + authenticateApiKey + rateLimitMiddleware`, and the bare `/health` liveness routes (`index.ts`, `public-metamcp.ts`, `mcp-proxy/{metamcp,server}.ts`) publish a constant `{status, service}` with no live state. **Operational note:** any external monitor that alarmed on a COUNT from this endpoint now needs an admin session or a different source; a monitor that only checks 200/`status` is unaffected. Gates: backend vitest **640 pass / 0 fail (57 files)**, **+8 new tests** (26→34 in `streamable-http.test.ts`) — non-admin body is exactly `{status:"ok"}`, the detail sources are never even consulted for a non-admin, the admin body's full key set, and five real-socket route cases (anonymous bare 200, no count/TTL/interval/id in the anonymous body, gate consulted every request, admin full detail, gate-throws-still-200); red-green proved by hard-coding `isAdmin = true` (3 route tests red, leaked body captured). The 2 `sdk-sse-keepalive` failures noted on #100 did NOT reproduce here (2/2 pass after a clean `pnpm install --frozen-lockfile`). `tsc --noEmit` exit 0, prettier clean, `eslint --max-warnings 0` clean on both touched files. | `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.test.ts`, `UMBRELLA_FORK.md` | Yes — `/health/sessions` is unauthenticated upstream too and publishes the same counts; the fix reuses `isAdminHealthRequest`, which is fork-shaped (RBAC is fork PR #68), so upstream would need its own admin predicate but the payload split ports unchanged. |
| 2026-08-14 | branch `fix/oauth-consent-screen` (PR pending) | **OAuth consent screen — closes FIND-001/005, the CRITICAL fast-follow the #100 row defers.** This server minted authorization codes for anyone who happened to be signed in, with no consent step, through TWO doors. **(1) `GET /oauth/authorize`** treated a valid session as approval and minted on the spot for ANY registered client (`authorization.ts:150-173`, pre-fix). Registration is anonymous, so an attacker could register a client pointing at their own `redirect_uri` and phish a signed-in administrator into ONE top-level navigation; the better-auth session cookie is SameSite=Lax so the browser attached it, the code went to the attacker's URL, and their own PKCE verifier redeemed it for a token as the administrator. **(2) `GET /oauth/callback`** reached the same outcome from an unsigned base64 `params` blob that had round-tripped through the client's browser. **Fix:** `/oauth/authorize` no longer mints — it signs the request (HMAC-SHA256 over `{client_id, redirect_uri, PKCE, state, user_id, csrf, exp}`, 10-min TTL, keyed by `BETTER_AUTH_SECRET` under a dedicated context string so an `areq` can never be swapped for another artifact from the same key), sets the csrf nonce in an httpOnly `__Host-` SameSite=Lax cookie, and 302s to the consent page; new `GET /oauth/consent/info` returns display fields (client_name clamped to 100 chars, full `redirect_uri`, scope) only to the user the request was issued to, and NEVER the nonce; new `POST /oauth/authorize/decision` is the ONLY mint site, gated on four independent checks — areq signature+expiry (nothing edited in flight, notably the PKCE challenge, which nothing downstream re-derives), session user == `areq.user_id`, double-submit csrf cookie (constant-time; ALL values under the name are considered so a planted duplicate cannot deny consent, and the cookie is deliberately NOT cleared on a failed check or replaying a bad decision would cancel a victim's pending consent), and `redirect_uri` re-validated against the client record. Default-deny: only the literal `"approve"` grants. The `params` mint branch is REMOVED, not signed; the direct-code lookup branch is unchanged; the post-login return path now re-enters `/oauth/authorize` instead of a callback carrying unsigned parameters. **Page path is `/consent`, NOT `/oauth/consent`** — `next.config.js` rewrites `/oauth/:path*` to the backend, so a frontend page under that prefix is proxied away and never renders (the frontend middleware also skips i18n and its auth gate for `/oauth*`); outside the prefix it additionally picks up the middleware session check, and gets `X-Frame-Options: DENY` + `frame-ancestors 'none'` + `Referrer-Policy: no-referrer` via a new `headers()` entry (the page is the flow's trust anchor and Next.js served it with none — framing would allow a clickjacked Approve, and a referrer leak would hand the `areq` in the query string to whatever the page reaches). **Review round (`cowork-reviewer`, verdict integrate-with-fixes; 6 attack mutations re-run, NO bypass found):** the `/oauth/callback` debug "success" page interpolated `code`, `state` and `codeData.redirect_uri` into HTML unescaped — reflected AND stored (redirect_uri is raw anonymous-DCR input); CSP blocks script but sets no form-action, so an injected overlay could post to an attacker host from this origin and a CSP regression would have become a full bypass of the consent control. DELETED the page outright (dev artifact) and replaced it with a JSON error that still breaks the self-referential redirect loop it existed to stop. Also: consent decisions are rate-limited **per user_id from the verified areq, not per `req.ip`** — `trust proxy` is unset and the backend is reached through the frontend's in-container rewrite, so `req.ip` is one container-local address for every human and an IP key would be a single org-wide bucket; a 429 here uniquely strands someone who already clicked Approve. Frontend middleware now preserves `request.nextUrl.search` on its login redirect (a session lapsing mid-consent used to drop the `areq` and force a restart). The consent page shows the FULL `redirect_uri` and labels `client_name` as self-reported, to blunt a "Claude" + lookalike-host phish. **Live-client bug found and fixed before merge (operator Claude.ai test):** Approve failed with `consent rejected reason=csrf`. Root cause from server logs — the connector requested `/oauth/authorize` TWICE in the same second (two `consent requested` lines), and with ONE shared cookie name the second `Set-Cookie` REPLACED the first, so the page the user approved (request A) was compared against request B's nonce. The review had flagged this collision and it was accepted as "clear error + retry" — wrong call: real clients prefetch/retry/parallelise, so it broke on the FIRST attempt, every time. Fix = a per-request non-secret `cid` (9 random bytes, base64url) inside the signed areq, with the cookie NAME suffixed by it — `__Host-oauth_consent_csrf_<cid>` on https, `oauth_consent_csrf_<cid>` on http (the `__Host-` prefix constrains attributes, not the rest of the name, so Secure + Path=/ + no Domain still hold). `/oauth/authorize` sets the per-cid cookie; the decision endpoint DERIVES the name from the verified `areq.cid`, so concurrent flows no longer share a slot and every other guard is untouched. `cid` is required by `verifyConsentRequest`, so a token predating the split fails closed rather than resolving a cookie name of `undefined`. **Gates:** backend vitest **668 pass / 0 fail** (58 files) incl. **36 new** consent cases — among them a regression test that reproduces the operator's failure exactly: two `/oauth/authorize` calls, then approve the FIRST, with the `Cookie` header built through a browser-accurate jar (keyed by name, last write wins — a test that merely concatenated both Set-Cookie values would have passed against the broken server); red-green proven by mutation (restore the authorize auto-mint → 3 fail; drop the CSRF check → 3; drop the session binding → 2; disable the HMAC verify → 2; restore the callback params mint → 1; collapse the per-cid cookie name back to one shared name → the two-authorize regression test fails with `expected 403 not to be 403`, the operator's exact symptom); `turbo check-types` + backend `tsc --noEmit` clean; eslint clean on every touched file (backend warning count unchanged at its pre-existing 34). Note the 2 `sdk-sse-keepalive` failures the #100 row records are NOT reproducible — they were stale-`dist` phantoms; with `@repo/zod-types` built BEFORE `@repo/trpc` the suite is fully green. **DEPLOY GATE: operator real-MCP-client test** (Claude.ai connector reconnect through the new page) before prod. | `apps/backend/src/routers/oauth/{authorization,consent-token,utils}.ts`, `apps/backend/src/routers/oauth/authorization.test.ts` (new), `apps/frontend/app/[locale]/consent/page.tsx` (new), `apps/frontend/{middleware.ts,next.config.js}`, `apps/frontend/public/locales/{en,zh,ko}/auth.json`, `UMBRELLA_FORK.md` | Yes — upstream `metatool-ai/metamcp` has the identical consent-free authorize fast-path and the same unsigned callback blob, so both doors and the whole consent flow apply unchanged. The `__Host-` cookie, the per-user decision limiter and the HTML-sink deletion are equally upstreamable. |
| 2026-08-14 | branch `feat/admin-access-dashboard` (PR pending) | **Access dashboard — the accounts surface MetaMCP never had — plus a real account DISABLE.** Self-registered member accounts are invisible in the GUI, because there is no users page and no list-users procedure anywhere in MetaMCP. API keys, OAuth clients and endpoints each had an admin view; the accounts those grants belong to did not, so the only way to learn an account existed was psql. **New admin page `/[locale]/access`** (sidebar item admin-gated) with four tabs: **Users** (net-new: email, name, role, verified, disabled, created, last session refresh, and three LIVE access-path counts — unexpired sessions, unexpired OAuth tokens, active API keys *including keys whose `acts_as_user_id` is this user*), **Active OAuth tokens** (net-new: unexpired `oauth_access_tokens` joined to users + clients — metadata only), and read-only reuse of the existing **API keys** (`apiKeys.listAll`) and **OAuth clients** (`oauthClients.list`) admin views with links to their management pages. **Three deliberately distinct account tiers:** `revokeAccess` (soft — deletes sessions, OAuth access tokens and pending auth codes, deactivates every key that can act as this identity, forces the M365 delegation to `reauth_required`; account survives and CAN sign in again), `setDisabled` (**migration 0027** — `users.disabled` / `disabled_at` / `disabled_by`; locks the account out while preserving everything as evidence — the incident-response primitive), and `delete` (destructive cascade). **Disable is enforced across SIX planes and THIRTEEN call sites, not one. The four this row shipped are the ones described here; the "Final planes" note further down, plus the consent-info guard added afterwards, complete the set — login, tRPC context, OAuth authorize / consent-info / decision, OAuth code-redemption / refresh / introspect / userinfo, `/mcp-proxy`, the api-key + bearer data plane (four branches), the M365 broker, and admin health recon. (Corrected 2026-08-14: this sentence said FOUR long after the plane sweep in this same row had taken it past that; the count is the one thing an incident responder reads it for.) The four shipped here:** better-auth `databaseHooks.session.create.before` blocks new sign-ins (verified against better-auth 1.6.23's `getWithHooks`: a `false` return aborts the create, a throw propagates — this throws an `APIError` so the user gets an honest 403 rather than an opaque failure); `createContext` re-reads the flag per request and drops the user/session so an ALREADY-ISSUED cookie stops working on the very next call (load-bearing — sessions are 30 days in this fork, so blocking only new logins would leave a disabled attacker fully active for a month); and BOTH code-minting paths in `routers/oauth/authorization.ts` refuse to issue an authorization code, since either would otherwise hand a locked account a fresh 30-day MCP token. **Those two OAuth guards were RE-HOMED to land this on post-consent `umbrella`:** the consent fix (row above) deleted the two handlers they were attached to — the authorize fast path and the `/oauth/callback` `params` mint — so the same `usersRepository.isDisabled` call now sits on consent's flow instead. `GET /oauth/authorize` checks after the session user resolves and before the areq is signed, and answers a disabled account exactly as it answers a signed-out one (`/login`, no areq, no CSRF cookie, so nothing exists to POST back). `POST /oauth/authorize/decision` checks after that endpoint's four existing guards and before `generateSecureAuthCode`, returning its own 403 `access_denied` shape and deliberately NOT clearing the consent cookie, the rule every other failed check there follows. The second guard is not redundant with the first: the four guards ahead of it all still pass for someone disabled DURING the ten-minute consent TTL, who is holding a valid session, a valid areq and the matching nonce — guarding only the GET would leave that window open. **Redaction is three layers** (query projection → serializer allow-list → tRPC `.output()`): the token query never selects `access_token`/`refresh_token` at all (refresh presence is computed in SQL), and the user query selects an explicit column list. **The delete dialog now tells the truth.** Its first draft claimed only the user's own private resources were affected; a live postgres disproved that — the cascade reaches OTHER users' rows two ways (`namespaces.user_id` cascades and `endpoints.namespace_uuid` cascades from it, so B's endpoint inside A's namespace dies with A; and `api_keys.acts_as_user_id` cascades, so deleting a CI/sync identity revokes another admin's production key). A new `users.previewDelete` adminProcedure **query** returns a measured blast radius split own/other, rendered before the irreversible confirm, with the destructive button DISABLED until those counts load. **The CRITICAL bug this branch was sent back to fix:** `last_active_at` came from a raw `sql` fragment, raw fragments carry NO driver decoder, so node-postgres returned the timestamptz as the STRING `'2026-08-14 13:07:51.558+00'` and the router's `.output()` schema threw `invalid_type: expected date, received string` — `frontend.users.list` failed 100% for any account that had ever held a session, and nothing in the type system could see it (the query's TS type already claimed `Date`). Fixed with an explicit null-safe decoder on the fragment plus a coercion in the serializer, and the same audit applied to every other raw fragment in both new queries (`count(*)` is bigint → `.mapWith(Number)`; the boolean gets an explicit guard). Renamed to `last_session_refresh_at` because better-auth only touches that column on its ~weekly sliding refresh — an account active minutes ago can read as days stale, which is exactly the number that gets misread during an incident. **Root cause of that class of bug: nothing ran real SQL.** So this adds `db/repositories/access-queries.integration.test.ts`, which EXECUTES both queries against a live Postgres 16 (all 27 migrations applied) and parses the serialized rows through the real zod contracts. Gated on `TEST_DATABASE_URL` — deliberately NOT `DATABASE_URL`, because the suite TRUNCATEs and reusing the production variable would let `vitest` wipe real accounts — and `describe.skipIf` so a skipped run is visible rather than silently green. Other fixes in the same pass: every list distinguishes FAILED from EMPTY (rendering a thrown query as "No accounts found" would reproduce the exact false negative this page exists to prevent); `revokeAccess` moved from `Promise.all` to a single TRANSACTION (a partial failure previously reported "failed" while access was half-cut); the listing is capped at 500 with the true total reported as "showing N of TOTAL" so a signup flood cannot make the page unusable when it matters most; `Boolean(x)` → `x === true` on the refresh-token badge (`Boolean("false")` is true — a decode regression would have labelled every row as holding a long-lived credential); and the stale "Migration 0025" comment in `schema.ts` corrected to 0026. Gates, measured on the MERGED tree: backend vitest **738 passed / 65 files, 0 failed** (17 skipped across 1 file — the real-postgres suite, which needs `TEST_DATABASE_URL` and skips visibly rather than silently); backend and `@repo/trpc` `tsc --noEmit` exit 0; `turbo check-types` 2/2; eslint clean on the files this merge touched; drizzle journal coherent at 27 entries with `when` strictly increasing and every tag backed by a `.sql` (0026 scope-default, 0027 users_disabled). Four new cases in `authorization.test.ts` pin the re-homed guards, and BOTH were mutation-proved: delete the GET guard and a disabled account gets its consent screen back, delete the POST guard and it gets a minted code. Pre-merge the branch alone measured 690 passed / 64 files with `TEST_DATABASE_URL` set, `turbo check-types` 2/2 and `next build` clean with `/[locale]/access` present. **Final planes + known residuals.** A coverage sweep after the plane sweep above found five more surfaces that never re-read `users.disabled`. Three are now CLOSED: (1) the `/mcp-proxy` chain — `requireAdminMcpMiddleware` tests `role` and nothing else, so a DISABLED ADMIN kept the STDIO spawn route (arbitrary command execution on the gateway host) for the 30-day life of a cookie they already held; `requireEnabledMcpMiddleware` now sits at the parent router between authentication and the role gate and covers all 14 sub-routes of both sub-routers, refusing with the role gate's byte-identical 403 so the two denials are indistinguishable on the wire; (2) `/oauth/introspect` and `/oauth/userinfo` — a locked-out account's unexpired access token still reported `active: true` with `sub`, `scope` and `client_id`, and userinfo still served its identity claims; both now answer as if the token were invalid (RFC 7662 `{active:false}`, and the existing unknown-token 401) and neither deletes the row, because disable is reversible and Revoke is what deletes; (3) `isAdminHealthRequest` — a disabled admin kept the detail half of `/health/upstream`, `/health/sessions` and the `GET /metamcp` endpoint directory, i.e. the estate map, and is now treated as non-admin on the same never-throwing soft path so liveness probes keep their 200. Two are ACCEPTED, not closed: `/api/auth/*` on an already-issued cookie, where better-auth's own account routes sit outside our middleware and a disabled user can still MANAGE their own account — change-password, update-user (name/image), sign-out, and list/revoke their own sessions. **Corrected 2026-08-14: DELETE is not among them.** better-auth 1.6.23 gates `/delete-user`, `/delete-user/callback` and `/change-email` on `user.deleteUser.enabled` / `user.changeEmail.enabled` (`dist/api/routes/update-user.mjs`), and `auth.ts` sets neither — its `user` block carries only `additionalFields` — so all three answer NOT_FOUND. The residual is manage-only, and the original note overstated it: an accepted risk that names account DELETION reads far worse in a post-incident review than the one that actually exists. Self-harm only either way, no gateway resource is reachable, and closing it needs an express wrapper around the better-auth handler (deferred); and `lib/m365/mint-service.ts`, which has no `isDisabled` of its own but is reachable only through the acts-as gate closed above, making it defence-in-depth rather than a hole. Also noted while reading, NOT fixed here: `tokenRouter.post("/oauth/token")` returns `handleRefreshTokenGrant`/`handleAuthorizationCodeGrant` without `await` inside its `try`, so a rejection from either escapes that catch — pre-existing, and it fails CLOSED since no token is issued on a DB reject. **Corrected 2026-08-14: this note used to add "(express 4 does not catch an async handler's rejection)", which is wrong twice over.** The fork is on express 5 (`apps/backend/package.json` pins `express ^5.2.1`, resolving `router@2.2.0`), and `router`'s `Layer.prototype.handleRequest` attaches `ret.then(null, err => next(err))` to any promise a handler returns — so the rejection IS forwarded, and since 2026-08-14 it lands on the terminal error handler in `index.ts` as a constant 500. Fail-closed, not a hang; the distinction matters because "it hangs" is what makes an operator go looking for a deadlock that is not there. Mutation-proved: drop the `/mcp-proxy` guard and a disabled admin is served 200 on all 14 sub-routes; neuter the introspect, userinfo and health guards and the disabled account gets `active:true`, its claims, and the estate map back. | `apps/backend/drizzle/0027_users_disabled.sql` + `meta/_journal.json`, `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/{users,oauth}.repo.ts`, `apps/backend/src/db/serializers/{users,oauth-tokens}.serializer.ts`, `apps/backend/src/trpc/{users,oauth-tokens}.impl.ts`, `apps/backend/src/auth.ts`, `apps/backend/src/trpc.ts`, `apps/backend/src/routers/oauth/authorization.ts`, `apps/backend/src/routers/trpc.ts`, `packages/trpc/src/{index,router}.ts`, `packages/trpc/src/routers/frontend/{index,users,oauth-tokens}.ts`, `packages/zod-types/src/{index,users,oauth}.zod.ts`, `apps/frontend/app/[locale]/(sidebar)/access/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/layout.tsx`, `apps/frontend/lib/i18n.ts`, `apps/frontend/public/locales/*/access.json`, `apps/frontend/public/locales/en/navigation.json`, + 7 test files, `UMBRELLA_FORK.md`; plane sweep + final planes additionally `apps/backend/src/middleware/{api-key-oauth,require-admin-mcp,require-enabled-mcp}.middleware.ts`, `apps/backend/src/routers/mcp-proxy.ts`, `apps/backend/src/routers/oauth/{token,userinfo}.ts`, `apps/backend/src/routers/m365.ts`, `apps/backend/src/lib/health-upstream.ts`, `apps/backend/src/db/serializers/users.serializer.ts` + 8 more test files | Partly — the users/tokens admin surface and the delete blast-radius preview apply to upstream `metatool-ai/metamcp` in shape, but every procedure here is `adminProcedure` and `users.disabled` layers on `users.role`, both fork-only (PR #68). Upstream has no RBAC, so this does not port as-is; the driver-decode trap on raw `sql` fragments and the cross-user delete cascade are worth reporting upstream as hardening notes. |
| 2026-08-14 | branch `sec/audit-logging-phase1a` (PR pending) | **Immutable control-plane audit log — the durable security record the 2026-08-13 incident proved this gateway never had (Lane A of the Phase-1 logging design).** Before this, the only durable stores were `tool_call_audit` (data plane, hard-`DELETE` pruned every 5 min) and the in-memory Live Logs ring buffer (2000 entries, gone on restart, and clearable from the admin UI). Neither captured a single control-plane security event: **not one RBAC denial and not one refused bearer credential.** The layer that would have shown a stolen key in use — `api-key-oauth.middleware.ts` — wrote nothing at all on refusal, which is why the incident's core residual risk was invisible while it was happening. **Migration 0028** adds `audit_log` (the common envelope: `occurred_at`, `actor_type`/`actor_id`/`actor_label`/`actor_ip`/`actor_user_agent`, `action`, `target_type`/`target_id`, `outcome`, `request_id`, `http_status`, `detail` jsonb) with four indexes and a `audit_log_block_mutation()` trigger function wired to **THREE** triggers, not two: BEFORE UPDATE and BEFORE DELETE per row, **plus BEFORE TRUNCATE per statement** — row-level triggers do not fire for TRUNCATE, so blocking only UPDATE/DELETE would have left `TRUNCATE audit_log` as a one-statement path to exactly the outcome the table exists to prevent. **The trigger is the enforcement, not a `REVOKE`, and that is now measured rather than assumed:** `SELECT rolsuper FROM pg_roles WHERE rolname = current_user` returns **`t`** on the `postgres:16-alpine` image this fork runs, so the app role is the cluster bootstrap superuser and a `REVOKE UPDATE, DELETE` would be a silent no-op. **That settles the design's open decision 1: the Phase-2 `NOSUPERUSER` runtime-role split is REQUIRED for real app-level immutability, not optional hardening**; this trigger is the honest interim and stops T1/most of T2, not a superuser who deliberately runs `SET session_replication_role = 'replica'`. `audit-log.repo.ts` exposes `record()` and **deliberately nothing else** — no `pruneOlderThan`, no delete, no update, unlike its `tool_call_audit` sibling — so there is no method to be mis-wired into a cleanup interval or exposed through a tRPC procedure later. **The emitter is fire-and-forget and cannot break the request, which is the property that outranks the logging itself:** it sits on the RBAC choke point (every admin-gated mutation) and the MCP bearer path (every proxied call), so a throw escaping it would not lose a log line, it would take the product down. `emit()` returns `void` (nothing can await it into the request path), lazily imports the repo, and swallows sync throws, rejections and a missing database, logging at **debug only** — an audit sink that shouts turns a DB blip into a log flood on the hottest paths. **Audit writes get their OWN bounded connection pool** (`db/audit-db.ts`, `max: 2`, `connectionTimeoutMillis: 1000`, same `DATABASE_URL`), and this is a deploy-blocking detail rather than tidiness: every refused MCP request emits one INSERT **including the no-credential 401s, which need no credential to provoke**, and the failed-attempt rate limiter keys on `req.ip`, which is the same loopback address for every caller behind the in-container Next.js rewrite — so it is one shared bucket with no per-attacker damping. On the shared pool (pg default `max: 10`, no checkout timeout, queues forever) an unauthenticated flood would contend for the same ten connections the AUTH path uses for session lookups, `users.disabled` checks and API-key validation: **logging the attack would starve the code that refuses it.** Bounded, a queued audit INSERT now errors after ~1s into the existing swallow — a dropped row under active flood is acceptable, a starved auth path is not. **Two detectors wired, both chosen because one hook covers a whole surface.** (1) `packages/trpc/src/trpc.ts` — `protectedProcedure`'s UNAUTHORIZED throw emits `authn.denied` and `adminProcedure`'s FORBIDDEN emits `rbac.denied`, carrying the tRPC `path`, which is the only field separating "a member's browser polled a page it cannot see" from "someone walked the admin mutation surface". Caught around `requireAdmin()` rather than emitted inside it so that function stays a pure directly-unit-testable predicate, and the error is rethrown untouched so the FORBIDDEN is byte-identical. (2) **all 17 denial returns** in `api-key-oauth.middleware.ts` emit `mcp.auth.denied` / `mcp.auth.ratelimited` with `actor_ip`, `request_id`, endpoint uuid and a **sha256 + last-4 credential fingerprint — never the raw token** (same discipline migration 0019 applied to tool-call params); `actor_type`/`actor_id` name the api-key uuid or OAuth user when the credential resolved and are honestly `anonymous` when it did not. The no-credential 401s are emitted **on purpose** even though every MCP client's discovery handshake produces one — dropping them at the source would also drop unauthenticated endpoint scanning, i.e. the recon phase this incident began with — and are tagged `reason: "no_credential"` so a query excludes them in one predicate. **`logs.clear` is GONE across all six surfaces** (zod schema + type, tRPC procedure + implementations field, backend impl, frontend store method, the UI button and its confirm dialog, and 6 i18n keys x 3 locales) **plus the in-memory `metamcpLogStore.clearLogs()` it called** — leaving an unused wipe method behind is how it comes back. It only ever emptied the ring buffer, but an admin-gated wipe is still a wipe, and it was the one gesture that erased the live security view mid-incident. The two gate tests now assert the procedure **does not exist** (against the router's procedure map, not via a caller, which would fail as a plain TypeError indistinguishable from a typo) rather than that it is admin-gated. **`app.set('trust proxy', ...)` was deliberately NOT set, and the omission is the decision:** it has zero effect on audit attribution (nothing here reads `req.ip`; `actor_ip` comes from `CF-Connecting-IP`, which Cloudflare OVERWRITES per request) and would instead change `req.ip` for the three existing rate limiters to an `X-Forwarded-For` entry — a header Cloudflare APPENDS to, so the caller controls its head. The correct hop count across `client -> CF -> cloudflared -> Next.js rewrite -> express` cannot be determined without a header dump from the live gateway, and guessing high makes the brute-force limiter key on a caller-supplied string, i.e. evadable by rotating a header: strictly worse than today's single shared bucket. Reasoning is recorded in `middleware/audit-context.middleware.ts` so the next lane can settle it with one observation. **Mutation-proved, four ways, each reverted after:** delete the emitter's `.catch` and 3 tests fail with 10 unhandled rejections (process exit under Node's default `--unhandled-rejections=throw`); delete the tRPC boundary guard and a denied member receives `TRPCError: registered sink exploded` instead of FORBIDDEN; register an async sink that rejects with the `Promise.resolve(...)` normalisation removed and 2 unhandled rejections escape (`TrpcAuditSink` permits `Promise<void>`, and a `=> void` signature could never have excluded it — TypeScript assigns `() => Promise<void>` to `() => void`); hoist `actorFields(ctx.user)` out of the emit guard and a throwing getter turns the FORBIDDEN into an INTERNAL_SERVER_ERROR. A fifth mutation — point `audit-log.repo` back at the shared `db` — fails the pool-isolation assertion. The repo-failure suite additionally makes `auditLogRepository.record` reject on every call through the REAL lazy-import path and asserts both detectors still answer normally AND that `record` was actually called, so a silently-disabled emitter cannot make those assertions pass for the wrong reason. Gates: backend vitest **926 passed / 76 files, 0 failed** with `TEST_DATABASE_URL` set (904 passed / 75 files, 22 skipped without one — the real-Postgres suites skip visibly rather than silently); backend, `@repo/trpc` and `@repo/zod-types` `tsc --noEmit` exit 0; `turbo check-types --force` 2/2; eslint at baseline on every package (backend 31 pre-existing warnings, `@repo/trpc` 0, frontend 23) with zero new warnings on the added files; drizzle journal coherent at 28 entries with `idx` and `when` strictly increasing (0028's `when` `1786838400000` exceeds 0027's `1786752000000` — a too-low `when` makes drizzle SKIP the migration with no error, so a test asserts the inequality). The triggers were exercised against a real Postgres 16 with all 28 migrations applied: `DELETE`, `UPDATE` and `TRUNCATE` each returned `ERROR: audit_log is append-only; <VERB> is not permitted` and the rows survived all three. | `apps/backend/drizzle/0028_audit_log.sql` + `meta/_journal.json`, `apps/backend/src/db/{schema,audit-db}.ts`, `apps/backend/src/db/repositories/{audit-log.repo,index}.ts`, `apps/backend/src/lib/audit/{audit-emitter,trpc-denial-sink}.ts`, `apps/backend/src/middleware/{audit-context.middleware,api-key-oauth.middleware}.ts`, `apps/backend/src/{index,trpc}.ts`, `apps/backend/src/routers/trpc.ts`, `apps/backend/src/trpc/logs.impl.ts`, `apps/backend/src/lib/metamcp/log-store.ts`, `packages/trpc/src/{trpc,index}.ts`, `packages/trpc/src/routers/frontend/logs.ts`, `packages/zod-types/src/logs.zod.ts`, `apps/frontend/app/[locale]/(sidebar)/live-logs/page.tsx`, `apps/frontend/lib/stores/logs-store.ts`, `apps/frontend/public/locales/{en,ko,zh}/logs.json`, + 7 new test files and 2 updated gate tests, `UMBRELLA_FORK.md` | Partly — the append-only table, the TRUNCATE-inclusive immutability triggers, the fire-and-forget emitter on its own bounded pool, the `CF-Connecting-IP` read and the MCP-bearer denial detector all apply to upstream `metatool-ai/metamcp` unchanged, and the shared-pool starvation trap is worth reporting upstream on its own. The `rbac.denied` half does not port as-is: `adminProcedure`/`requireAdmin` are fork-only (PR #68) and upstream has no RBAC, so only the `authn.denied` hook on `protectedProcedure` has an upstream equivalent. `logs.clear` removal is a product decision upstream may not share. |
| 2026-08-14 | branch `sec/audit-logging-phase1b` (PR pending) | **Wire the SUCCESS-path emitters across auth, OAuth and the admin control plane (Lane B of the Phase-1 logging design).** Lane A recorded what was REFUSED — RBAC denials and rejected MCP bearer credentials — and nothing that was ALLOWED. So the 2026-08-13 incident's own sequence still left no durable row: a self-registered account appearing, a session minted, an anonymous client registered at `/oauth/register`, a consent granted, a 24h access token issued against a 365d refresh token. **The single loudest gap was the config toggles**, which were completely silent: an admin session could re-open self-registration and the only surviving evidence was the config row's `updated_at`. **45 emission points carrying 44 distinct action verbs, all fire-and-forget through Lane A's existing `emit()`** — no new pool, no new table, no migration. **Auth (7 sites):** the `/api/auth` relay in `index.ts` is wrapped to read better-auth's verdict, emitting `auth.login.success` / `auth.login.failure` / `auth.logout`, and `databaseHooks` in `auth.ts` emit `auth.signup`, `auth.signup.denied`, `session.create` and `session.revoke`. **Both halves are deliberate, not redundant:** the relay wrap is scoped to `sign-in/email` ONLY, because that is the one sub-path where the HTTP status genuinely IS the credential verdict — a 200 from `sign-in/social` means "here is a redirect URL", and filing it as a login would put a successful-login row in the table for a flow that has authenticated nobody. SSO logins are recorded by `session.create`, which is path-independent; failed sign-ins are recorded by the wrap, because a refused sign-in creates no session and `session.create.after` never fires for it. `user.create.after`, `session.create.after` and `session.delete.after` were confirmed present and per-row in better-auth 1.6.23 by reading `db/with-hooks.mjs` (both `deleteWithHooks` and `deleteManyWithHooks` loop the `after` hook over every removed entity), not assumed from the docs. **Hook rows are attributable because the relay threads two internal headers** (`x-audit-request-id`, `x-audit-client-ip`) onto the web `Request` handed to `auth.handler()` — better-auth's hook context can see that Request and nothing from Express, so without them every signup and session row would carry a null `request_id` and could not be joined to the `auth.login.*` row from the same HTTP call. **A caller cannot forge them, and getting that right needed the DELETE half:** the relay copies the caller's headers into that bag first, so a client sending `x-audit-client-ip` has already planted a value — `stampAuditHeaders()` therefore `set`s when it has a value and `delete`s when it does not, because the no-`CF-Connecting-IP` case (direct-to-origin, bypassed tunnel) is exactly the one where a skipped write would leave the forgery standing and it would be read back as evidence. Two tests pin both halves. **OAuth (10 sites):** `oauth.authorize.grant` / `.denied` at `POST /oauth/authorize/decision` — the consent screen shipped in [#102](https://github.com/Umbrella-IT-Group/metamcp/pull/102) (`ac5896a`), so this is the CONSENT DECISION, correcting the design's §1.6 premise that no consent step exists; the grant is emitted after `setAuthCode` commits, the three denials carry `reason: csrf` / `disabled` / `user_denied`. `oauth.token.issue` / `.refresh` beside `logTokenIssued` in `token.ts`, `oauth.token.revoke`, `oauth.token.introspect` on the disabled-account refusal, and `oauth.dcr.register` at the unauthenticated `POST /oauth/register` — an anonymous write that can add a client a signed-in human is later asked to approve, and whose `client_name` is whatever the caller typed, which is how a consent screen ends up saying "Claude". **Which branches of `/oauth/introspect` and `/oauth/revoke` emit is a decision, made per branch, because both are unauthenticated and neither carries a rate limiter** (unlike `/oauth/token` and `/oauth/register`, which have `rateLimitToken`). An UNKNOWN token emits nothing on either — RFC 7009 requires revoke to answer 200 to garbage, so a row there would record a string the caller invented, permanently. A REVOKE of a real token emits, and is self-limiting: revocation DELETES the row, so a replay finds nothing. An INTROSPECT reporting ACTIVE deliberately emits NOTHING — introspection does not consume the token, so unlike revoke it is unbounded, and whoever holds one stolen credential (the 2026-08-13 scenario exactly) could replay it into permanent rows forever; `oauth.token.issue` already recorded that credential under the same fingerprint. An introspect refused because the ACCOUNT IS DISABLED does emit: it needs a real token belonging to an account an administrator has already locked, and a relying party still asking about it is what an incident responder wants to see. Bounding the active branch properly means mounting a rate limiter on a public endpoint — deferred by operator ruling, see the end of this entry. **Admin (28 sites):** `config.signup_disabled.set` / `.sso_signup_disabled.set` / `.basic_auth_disabled.set` / `.session_lifetime.set` / `config.set` each carry the actor and **old AND new value** — the new value alone cannot answer "did this change anything?", which is the first question asked of a toggle during a review — read through a guarded `previousValue()` helper so a failed before-picture read degrades the row to `old_value: null` rather than refusing the administrator's change. The generic `setConfig` escape hatch is audited too, because `key` is the full `ConfigKey` enum and can reach `DISABLE_SIGNUP` without the named setter. **The four MCP tuning setters deliberately do NOT emit** (documented in `config.impl.ts`): they change upstream retry behaviour, not who may reach this gateway, and they are what an operator touches routinely while debugging a flaky backend. CRUD breadcrumbs on the success path of api-keys (`create`/`update`/**`revoke`, its own verb when `is_active: false`**, so "which credentials were killed, by whom" is greppable rather than buried in renames)`/delete`, oauth-clients, endpoints, mcp-servers (incl. `bulk_import`), namespaces (incl. `server_status` / `tool_status` / `tool_overrides`) and tools; plus **all three account tiers in `users.impl.ts`** — `user.disabled.set` / `user.enabled.set` (two verbs rather than one with a boolean, because reversing a containment decision has to be greppable on its own), `user.access.revoked`, and `user.delete`. Those last two are the operations an administrator performs WHILE containing an incident, and they tear their targets down with RAW DRIZZLE rather than through better-auth, so no `databaseHooks` sink sees them — without an emitter in the impl the table would have recorded deleting one API key but not wiping an entire identity. Both carry **counts only**: `user.access.revoked` gets `sessions_revoked` / `oauth_tokens_revoked` / `auth_codes_revoked` / `api_keys_revoked` / `m365_tokens_revoked`, `user.delete` gets the nine cascade counts INCLUDING the cross-user reach (`other_users_endpoints`, `other_users_api_keys`) that an incident review asks about first, and each carries one clamped `target_email` — for `user.delete` that email is the only surviving link between the id in `target_id` and a human, since every FK into `users` is `ON DELETE CASCADE`. The values these two operations touch ARE the credential set (session rows, OAuth access and refresh tokens, authorization codes, API keys, M365 delegations), and none of it is recorded: a token copied into an append-only table would outlive the revocation meant to kill it. `tools.create` / `tools.sync` emit **only when the catalog actually changed** — both are called on every UI refresh, and the no-op branches would be an admin-triggered stream of rows recording nothing. **NO CALLER-SUPPLIED FIELD IS UNBOUNDED, which matters as much as the secret discipline and is easier to miss:** `audit_log` has DELETE/TRUNCATE triggers and deliberately no prune path, so a row's SIZE is as permanent as its contents — which makes any un-clamped request-body value a write-amplification primitive. The JSON body limit is 50mb, `/oauth/register` takes no credential at all, and `rateLimitToken` keys on `req.ip`, which is the same loopback address for every caller behind the in-container rewrite (i.e. one global bucket). So `clampAuditText` / `clampAuditTextList` bound both the element length AND the array length of `redirect_uris` and `grant_types` on the DCR and admin client-create paths (with a `redirect_uri_count` so truncation is recorded rather than hidden), the attempted email on a failed login is clamped to RFC 5321's 320 characters, and bulk-import server names are clamped too. A test feeds 40 URIs of 2000 characters each through the anonymous endpoint and asserts the whole row stays under 8KB; removing the clamp fails it. **NO SECRET REACHES THE TABLE, and this is the property that outranks the logging:** `audit_log` is append-only with triggers refusing UPDATE/DELETE/TRUNCATE, so a credential written into it can never be taken back out. API-key rows carry uuid/name/scope and never `result.key` (emitted before the serialiser, which is the only thing between there and a response body holding the full key); MCP-server rows carry name/type and never `bearerToken`, `env` or `headers`; OAuth-client rows carry `has_client_secret: boolean`; token rows carry a `credentialFingerprint` (sha256 + last-4) and never the token — the same shape Lane A's MCP-bearer detector records on refusal, so one credential can be followed from mint to misuse across the two by hash; session rows carry the session `id`, never its `token`; the login-failure row carries the ATTEMPTED email (an identifier, the whole value of the row) and never the password. **Actor threading:** `auditActor(ctx)` in `packages/trpc/src/trpc.ts` flattens the session user plus Lane A's `BaseContext.audit` into one bundle passed to the 28 mutating router call sites, matching the existing `ctx.user.id` idiom. It **cannot throw**, and that is load-bearing rather than defensive: it is evaluated as an ARGUMENT to the implementation call, i.e. strictly before the mutation runs, so a throwing property read on `ctx.user` (typed `any`, reached from JSON) would abort the write itself — the audit path deciding whether an admin's toggle takes effect. A test drives a `Proxy` whose `email` getter throws and asserts the toggle still succeeds with a null actor. The three new mapping modules (`admin-event.ts`, `auth-relay-audit.ts`, `auth-hook-audit.ts`) are extracted rather than inlined for the same reason Lane A extracted `trpc-denial-sink.ts`: importing `index.ts` boots the whole server and importing `auth.ts` boots better-auth plus a pg pool, so an emitter defined inside either could only be tested by testing the whole stack. **Gates:** backend vitest **993 passed / 80 files, 0 failed, 0 skipped** against a real Postgres 16 with all 28 migrations applied (971 passed / 22 skipped without `TEST_DATABASE_URL`) — 67 new tests in 4 files, each group asserting the row's action/outcome/actor/target, the ABSENCE of every credential the request handled, that a failure or no-op path emits NOTHING, and that a throwing sink and a rejecting sink both leave the operation intact. **Three mutation proofs, each reverted:** adding `bearerToken`/`env`/`headers` back into `mcpserver.create`'s detail fails the vendor-credential assertion, removing the `redirect_uris` clamp fails the DCR size assertion, and spreading the target user record into `user.access.revoked`'s detail — the shape a careless refactor produces — fails the counts-only assertion. **`emitAdminEvent` labels an ABSENT actor bundle `actor_type: "system"`, not `user`** — `lib/metamcp/metamcp-proxy.ts` calls `toolsImplementations.sync` directly during a proxied MCP `tools/list` with no session and no request context, and a `user` row with five null columns there would put a phantom administrator in the table for something a background refresh did; an actor bundle that EXISTS but holds nulls stays `user`, because a request really was made by someone whose identity could not be read. **Every path that destroys a session today is now recorded somewhere** — through better-auth it is `session.revoke`, through the admin surface it is `user.access.revoked` / `user.delete` — and the two comments that describe that split (`auth-hook-audit.ts`, `auth.ts`) say so, along with the thing to check when a new teardown path is added: one that uses neither route would be silent. `session.revoke` still cannot distinguish a deliberate kill from an expiry sweep at the verb level, so it carries `already_expired` in `detail` as an inference rather than pretending better-auth handed it a reason code. **DEFERRED by operator ruling, not oversight:** `/oauth/introspect` and `/oauth/revoke` remain unauthenticated with no rate limiter — the active-introspect row is gone so this change adds no amplifier, but bounding those endpoints is a behaviour change for existing resource servers and belongs to a lane that can verify against live traffic. Backend and `@repo/trpc` `tsc --noEmit` exit 0, `turbo check-types --force` 2/2; **End-to-end probed against a live Postgres 16** (throwaway script, not committed): the real mapping helpers driven through the real emitter and repository landed five rows — `auth.login.failure` carrying the attempted email, `auth.signup.denied`, `session.create`, `config.signup_disabled.set` with `old_value: true / new_value: false`, and an actor-less `tools.sync` stored as `actor_type = system` — and a substring search of the returned rows for the submitted password and the session token came back clean on both. eslint **backend 31 → 29** warnings (the two removed are an incidental `userId`-unused pair in `mcp-servers.impl.ts` that stopped being the trailing argument), `@repo/trpc` 0 → 0, frontend 23 unchanged — zero new warnings. One pre-existing gate test (`access-admin-gate.test.ts`) was updated in the same change to assert the third `setDisabled` argument rather than loosened. | `apps/backend/src/lib/audit/{admin-event,auth-relay-audit,auth-hook-audit,audit-emitter}.ts`, `apps/backend/src/{index,auth}.ts`, `apps/backend/src/routers/oauth/{authorization,token,registration}.ts`, `apps/backend/src/trpc/{config,api-keys,oauth-clients,endpoints,mcp-servers,namespaces,tools,users}.impl.ts`, `packages/trpc/src/{trpc,index}.ts`, `packages/trpc/src/routers/frontend/{config,api-keys,oauth-clients,endpoints,mcp-servers,namespaces,tools,users}.ts`, + 4 new test files and 1 updated gate test, `UMBRELLA_FORK.md` | Partly — the auth relay wrap, the better-auth `databaseHooks` emitters, the OAuth token/DCR rows and the credential-fingerprint discipline all apply to upstream `metatool-ai/metamcp` unchanged. The consent-decision rows do not: `POST /oauth/authorize/decision` is fork-only (PR #102), and upstream still mints on the authorize GET. `user.disabled.set` and the admin CRUD actor threading depend on fork-only surfaces (the users router, `adminProcedure`), and the config-toggle rows depend on the fork's RBAC gating of those setters. |
| 2026-08-14 | branch `sec/oauth-hardening-v3` (PR pending) | **Lock dynamic client registration to a redirect_uri allowlist (security review FIND-023, HIGH) and stop serving OAuth discovery for endpoints that do not use OAuth (pre-empts upstream [#277](https://github.com/metatool-ai/metamcp/issues/277)).** **(1) FIND-023.** `POST /oauth/register` is anonymous and validated only the SCHEME, so the 2026-08-14 security review got a 201 for `https://evil.com/callback`, the lookalike `https://your-gateway.example.com.evil.com`, the userinfo trick `https://your-gateway.example.com@evil.com` (real host evil.com, reads as the gateway's own host), and `http://localhost:12009` — the gateway's own internal listener. With the consent screen (PR #102) in place none of those mints a code by itself, but each is a client whose plausible `client_name` a signed-in human can be phished into approving, with the code landing on the attacker's host. New pure `isAllowedRedirectUri` in `routers/oauth/utils.ts` enforces six rules on EVERY entry: WHATWG-parseable; `https` for non-loopback and `http` only for loopback (generalises the old `javascript:`/`data:` special case to every other scheme); no userinfo; no fragment (RFC 6749 §3.1.2); loopback = EXACT hostname `localhost`/`127.0.0.1`/`::1` (never prefix or suffix — `localhost.evil.com` and `evil.localhost` are both refused) on any port except `GATEWAY_INTERNAL_PORT`; and an EXACT-match host allowlist for non-loopback https, default `claude.ai`, `claude.com`, `anthropic.com`, replaceable via `DCR_REDIRECT_URI_ALLOWED_HOSTS`. **Default-on, and safe because the live data says so:** of the 65 client rows registered before this change, every legitimate redirect_uri was loopback or one of the two Claude callbacks — every other host was a probe. Exact match, not suffix: suffix matching is precisely what makes `your-gateway.example.com.evil.com` look legitimate, and it would also hand every `*.claude.ai` subdomain a valid callback. **Scope, deliberately:** registration only. `validateRedirectUri` is left in place on `GET /oauth/authorize` and the 65 stored rows are NOT re-validated or mutated — retroactive enforcement is a separate, evidence-gated decision, and deleting the test clients is the cleanup that pairs with this. Because `buildClientRegistration` is shared, the allowlist also covers the admin UI's create-client dialog; an admin who needs another host changes the env, which leaves a deployment-visible record. The 400 `invalid_redirect_uri` contract is unchanged apart from a trailing parenthesised reason, added because a default-on allowlist otherwise refuses a legitimate new client with an error that never mentions the host. **(2) #277.** Upstream serves `/.well-known/oauth-authorization-server` and `/.well-known/oauth-protected-resource` unconditionally; Claude Code v2.1.85 does RFC 9728 discovery BEFORE connecting and, finding OAuth metadata, abandons the configured bearer token — so an API-key-only endpoint re-prompts for auth every session. Both documents now resolve what the caller asked about: the RFC 9728 §3.1 path-inserted form (`/.well-known/oauth-protected-resource/metamcp/<endpoint>/mcp`, newly ROUTED — it previously fell through to Express's HTML 404) answers per-endpoint on `enable_oauth`, and the bare form answers on the new `endpointsRepository.hasOAuthEnabledEndpoint()`, i.e. whether this deployment runs an authorization server at all. Fails closed on any other suffix; refusals are one identical `no-store` 404 whether the endpoint is OAuth-disabled or absent, so discovery is not an endpoint-name oracle. **Pre-deploy check:** the bare-path gate means `SELECT count(*) FROM endpoints WHERE enable_oauth = true` must be >= 1 on any deployment whose clients use OAuth — it is on ours, and that is what keeps the live Claude.ai connectors discovering. **Tests:** 56 new assertions across `redirect-uri-allowlist.test.ts` and `metadata.test.ts`, including the full case list pinned to the RULE that refuses each one, the legit-client list (loopback any port + both Claude callbacks) pinned as ACCEPTED, the env override replacing rather than extending, and a drift guard that reads `app.listen()` back out of `index.ts` to keep `GATEWAY_INTERNAL_PORT` honest. 15 mutants killed, including allowlist-removed, loopback-match weakened to startsWith/endsWith/includes, unknown-path-falls-back-to-unscoped, and gate-removed. | `apps/backend/src/routers/oauth/{utils,client-registration,metadata}.ts`, `apps/backend/src/db/repositories/endpoints.repo.ts`, `example.env`, + 2 new test files and 3 updated test files, `UMBRELLA_FORK.md` | Yes, both halves. The `.well-known` gate is upstream issue #277 answered directly and applies unchanged. The redirect_uri validator applies too, though upstream would want the allowlist DEFAULT-OFF (empty default = today's behaviour, opt in via the env) rather than Claude-only, since the default encodes this fork's expected client population, not theirs. |
| 2026-08-14 | branch `sec/rr4-cors-hardening` (PR pending) | **Scope the OAuth CORS policy to OAuth paths, give `/api/auth` a deliberate allowlisted CORS policy of its own, and replace the reflected-origin CORS on `/metamcp/*` with that same allowlist.** **(1) Cross-router policy bleed.** `oauthRouter` is mounted UNPREFIXED (`app.use(oauthRouter)`) so `/.well-known/*` can sit at the root, and it declared `cors({ origin: "*", credentials: true })` as router-level middleware — which on an unprefixed router runs for EVERY request the app receives. The wildcard policy written for anonymous OAuth discovery was therefore applied to `/api/auth/*` responses and, because the cors package answers preflights itself, to the OPTIONS leg of every other route in the app including `/trpc/*`. Not exploitable as it stood — the value was the literal `*`, which no browser honours alongside credentials — but it is a policy nobody chose for those routes landing on the ones that answer with the session cookie. Now guarded to the two prefixes this router actually serves (`/oauth`, `/.well-known`, matched whole-segment so `/oauthanything` does not qualify), with `credentials` dropped: `*` plus credentials grants nothing in any browser, and every browser-driven leg of the OAuth flow reaches the backend same-origin through the frontend's rewrites. Discovery, token, register and userinfo keep their wildcard grant — MCP clients this deployment has never seen read those paths, so no allowlist can be built for them, and breaking that takes every connector down. **(2) `/api/auth`.** The relay had no CORS policy of its own and answered with whatever a neighbour left behind. It now gets an explicit one: origin resolved against a shared allowlist reading the same two environment variables better-auth's own `trustedOrigins` reads (`APP_URL` plus `EXTRA_TRUSTED_ORIGINS`), though NOT the same list — better-auth keeps nine hardcoded loopback origins trusted in production and treats `APP_URL` as its `baseURL` rather than a list member, whereas this one admits loopback ONLY when `APP_URL` is itself loopback, i.e. it is strictly stricter on a deployed gateway. Only http(s) origins are eligible: WHATWG gives every non-special scheme the SAME opaque origin, serialised as the string `null`, so without a scheme check one `moz-extension://…` entry in `EXTRA_TRUSTED_ORIGINS` would have answered every extension, packaged-app and sandboxed-iframe caller `Access-Control-Allow-Origin: null` beside credentials, `credentials: true`, and either a specific echoed origin or NO `Access-Control-Allow-Origin` at all. Every inherited `Access-Control-*` header is cleared before that policy runs (not just origin and credentials — `-Expose-Headers` and `-Allow-Methods` are policy too), a `Vary: Origin` is set even on the grant-less answer so a shared cache cannot replay it to the trusted origin, and CORS headers are filtered out of the header set the relay copies back from better-auth so an upstream default can never silently replace the policy. The relay itself moved out of `index.ts` into `routers/auth-relay.ts`: `index.ts` calls `app.listen()` at module scope and so cannot be imported, and behaviour that cannot be imported cannot be tested. **(5) The other two credentialed routers.** `/trpc/*` and `/mcp-proxy/*` were both still on a raw `origin: process.env.APP_URL`, which sets that fixed string as the grant for every caller and — if `APP_URL` is ever unset or empty — is falsy, which the cors package answers with `*` beside `credentials: true`. Both now use the shared resolver, so all four credentialed surfaces share one definition of trusted. **(3) `/metamcp/*`.** Used `origin: true` — the caller's own `Origin` reflected straight into `Access-Control-Allow-Origin`, next to `credentials: true`, which is the pairing that lets any site a victim visits read a credentialed response from this host. Not reachable today because those routes authenticate on `X-API-Key` rather than the session cookie; fixed anyway, because a reflected origin beside credentials must not be one route change away from mattering. Now on the same allowlist; a browser-based third-party client that needs cross-origin access belongs in `EXTRA_TRUSTED_ORIGINS`. **(4) `/api/auth` 500 body.** The relay's catch echoed `details: error.message` to an unauthenticated caller, and that relay reaches better-auth, the drizzle adapter and postgres, so the message is routinely a driver error naming internal hosts, tables and columns. It now answers with the `INTERNAL_ERROR_BODY` constant the terminal handler already uses, detail to the server log only. Checked at the same time and deliberately NOT changed: better-auth returns no stack trace to the client on any path — better-call answers a non-`APIError` with a null-bodied 500 and `console.error`s the stack to the log, and an `APIError` serialises `{ message, code }` and nothing else. **Tests:** 96 tests in a new `routers/cors-policy.test.ts` driving the real routers AND the real relay over a real socket, with only `auth.handler` standing in. The no-wildcard-and-no-reflection-with-credentials invariant is swept unconditionally across all ten surfaces and against four hostile origin classes (untrusted https, browser-extension, packaged-app, `file://`); a deliberately hostile middleware sets `Access-Control-*` headers ahead of the `/api/auth` policy so the clearing belt cannot become untestable dead code; the relay's header filter and masked error body are asserted against a better-auth stand-in that returns a CORS grant and one that throws. The allowlist's own cases cover case and trailing-slash normalisation, the userinfo and backslash lookalikes, port mismatch, suffix-lookalike host, scheme mismatch, every non-special scheme, and loopback gated on the deployment being loopback. A source-level drift guard reads every `cors()` call site in the backend and fails if any credentialed one drifts off the shared resolver — which is what covers the two routers too heavy to mount, and what catches a NEW credentialed router added later. **Mutation-verified, 10 of 10 killed:** removing the scheme check, the clearing belt, the `Vary`, the relay's header filter, the relay's error mask, the OAuth path guard, or reverting either of the four routers to its old origin option each fails at least one test. | `apps/backend/src/lib/cors-policy.ts` (new), `apps/backend/src/routers/auth-relay.ts` (new, extracted from `index.ts`), `apps/backend/src/index.ts`, `apps/backend/src/routers/{oauth/index,public-metamcp,trpc,mcp-proxy}.ts`, `example.env`, + 1 new test file, `UMBRELLA_FORK.md` | Yes, all four. None of it is Umbrella-specific: the bleed, the reflected origin and the error-detail echo are all upstream shapes, and the allowlist keys off env vars upstream already defines. |

**Upstream PRs filed back from this fork:**

| Date | Upstream PR | What |
|---|---|---|
| 2026-05-07 | [metatool-ai/metamcp#286](https://github.com/metatool-ai/metamcp/pull/286) | Better-auth session env-var fallback. Branched off plain `main`, no dependency on #276. **MERGED upstream 2026-06-14** (`3b4f36d`) in the ai-dev revival wave. |
| 2026-06-11 | [metatool-ai/metamcp#310](https://github.com/metatool-ai/metamcp/pull/310) | Ports fork PR #28 onto upstream main: cascade `invalidateServerConnection` (upstream had none), `requestWithSessionRecovery` helper, all four aggregate list handlers wired with retry + DEGRADED tripwire. Includes #293's detectors as its base commit (helper depends on them) — if #293 merges first this rebases clean; if this merges, close #293. **CLOSED without merge (verified 2026-07-21, `gh pr view 310`)** — superseded once #293 landed. The cascade content already ships in `umbrella` (fork PR #28) and is present on upstream `ai-dev`, so nothing is lost; local/remote branch `upstream-pr/list-handler-recovery` pruned 2026-07-21. |
| 2026-05-14 | [metatool-ai/metamcp#293](https://github.com/metatool-ai/metamcp/pull/293) | Adds the recovery-error detectors (`isBackendSessionLostError` + `isBackendTransportLostError` + `isRecoverableBackendError`) that fork PRs #12 + #13 ship. Standalone file contribution — no `metamcp-proxy.ts` rewiring; the recovery callsite is in companion upstream PR #283. Either order works. **MERGED upstream 2026-06-14** (`b73829d`) in the ai-dev revival wave. |

## How to update the fork against upstream

```bash
git fetch upstream
git checkout main
git rebase upstream/main
git push origin main

# Now reconcile umbrella against the new main.
git checkout umbrella
git rebase main
# Resolve conflicts. Drop any of our cherry-picks that upstream has merged
# (the cherry-pick log above tells you which).
git push --force-with-lease origin umbrella
```

Cadence: monthly minimum, or whenever a real upstream commit lands that we want.

## Build + deploy

`umbrella` is built by `.github/workflows/umbrella-build.yml`. Push events to `umbrella` build **and publish** — login, tag `ghcr.io/umbrella-it-group/metamcp:<sha>` + `:latest`, push, and attest all run. Pull-request events into `umbrella` run the same job as a **check only**: `docker/login-action`, `docker/metadata-action`, and `attest-build-provenance` are skipped, `build-push-action` runs with `push: true` only on the push path (`push: false` on PRs), proving the Dockerfile still builds without publishing or touching the registry. (This PR-check path only exists as of PR #63, 2026-07-13 — before that fix the job's `if` matched `push` only, so the `pull_request` trigger existed but silently never fired; see the cherry-pick log below.) Pinned in `Umbrella-MCP-Server/metamcp/compose.fragment.yml`. Watchtower (with `scope=umbrella` label) auto-updates the running container within ~5 min of an image push.

To deploy a hotfix:

1. Branch off `umbrella`, commit, push, open PR against `umbrella`, get review, squash-merge.
2. CI builds `:latest` automatically.
3. Watchtower polls every 5 min; container restart picks up the new image.
4. If you need it instantly, mint a break-glass session and run `docker compose up -d --no-deps metamcp` from `mcp-host-prod`.

### Repo visibility (operator standing decision, 2026-07-21)

This repo is **PUBLIC**, deliberately. GitHub Actions minutes are billed
per-job on private repos but free on public ones — flipping to public on
2026-07-21 took this fork's ~253 min/mo of Actions usage to $0. Flipping
back to private re-arms roughly that same ~250-400 min/mo of billing (the
range accounts for cherry-pick/dependency-sweep months running heavier
than quiet months). Any future call to make this repo private again should
weigh that cost explicitly, not flip it as a routine hygiene pass — check
here first.

## Upstreaming our work

When we write a patch that's not Umbrella-specific (env-var fallbacks, bug fixes, etc.):

1. Branch off `main` (not `umbrella`) so the patch is clean against upstream.
2. Open PR against `metatool-ai/metamcp`.
3. Once merged upstream, drop our private cherry-pick during the next rebase.

The cherry-pick log doubles as our own upstream-PR backlog — anything in "Our own patches" is a candidate.

## Deferred / future work

- **Port the SDK 1.30.0 stdio ReadBuffer 10MB cap into the fork's vendored copy** (`apps/backend/src/lib/stdio-transport/shared.ts`, uncapped `append()`): the pool fronts STDIO backends whose output we do not control, and the vendored transport bypasses the SDK cap entirely. Surfaced by the #95 review 2026-08-13.
### MCP spec 2026-07-28 catch-up (the "sessionless rewrite"), parked 2026-08-13

Source: a 2026-08-13 research session (~500K tokens) that read the MCP spec revision dated 2026-07-28 end to end against this fork and the wider Umbrella MCP platform. Operator ruling that night, verbatim: park Phase 2-3 with the ideas in the repo, revisit at a later date (probably next year), after double-checking our systems will support it. Two phases, filed as the paired counterpart to `Umbrella-MCP-Server` `docs/TASKLIST.md`, "MetaMCP fork backlog" section (`MCP-SPEC-2026-07-28-PHASE2` / `MCP-SPEC-2026-07-28-PHASE3`), where the full writeup lives. This is the pointer, not a duplicate; read that entry before starting anything below.

**Phase 2, gated on three named triggers (verify each, then re-verify the operator's own systems-support gate before starting):**

1. TypeScript `@modelcontextprotocol/sdk` v2 goes STABLE. Currently 2.0.0 beta, itself a package split (core/client/server/...) rather than a drop-in bump. This fork pins v1 today: `apps/backend/package.json` and `apps/frontend/package.json` both carry `1.30.0` (the v1 line was established by fork PR #64's zod v4 migration at `1.29.0`, currency-bumped by PR #95).
2. `fastmcp` 4.0 goes STABLE. Currently 4.0.0b2. The 4.x line serves sessionless alongside the handshake era (dual support), which is the property every backend MCP depends on existing before this bump touches anything.
3. claude.ai connectors actually negotiate protocol version 2026-07-28. Per Anthropic's docs fetched 2026-08-13, connector support today goes only through 2025-11-25.

Key facts a 2027 resumer needs on this fork specifically:

- The gateway must stay dual-era (serving both the pre-rewrite handshake protocol and the new sessionless one) for as long as any Claude surface we serve, meaning every claude.ai connector today, still speaks handshake-era. No flag day exists in the spec that forces a cutover.
- This fork carries zero hardcoded `protocolVersion` anywhere; the SDK negotiates it. That removes one migration step but not the one below.
- `apps/backend/src/lib/metamcp/transport-recovery-hydration.ts` is the number one migration risk. It reaches into SDK-private internals to hydrate lazily-recovered StreamableHTTP transports (the fix that made PRs #15/#22/#23/#24 actually work). Any SDK major bump needs this file re-verified line by line against the new internals before merge; do not assume compatibility.
- Backends across the platform are pure tools-over-POST (205 tools fleet-wide, no sampling, no elicitation, no resources), so the migration's real risk concentrates in this gateway's session/transport plumbing, not in the per-vendor MCP servers it fronts.
- No deprecation clock forces this migration. Legacy servers keep serving dual-era clients indefinitely under the spec. The 12-month deprecation runway announced 2026-07-28 covers only HTTP+SSE transport, the Roots / Sampling / Logging capabilities, and RFC 7591 Dynamic Client Registration (see Phase 3 below), none of which is `protocolVersion` itself.

**Canary path, not a fresh design question.** Per the operator's standing 2026-08-04 ruling (`Exploitacious/Personal-MCP-Server` `README.md:185-188`), the personal MCP instance is the designated fork canary for this exact upgrade: prove it there first, single-operator, low blast radius, before this fork or any Umbrella production backend touches it.

**Phase 3, opportunistic, no date, pick up individually:**

- **Tasks extension (`io.modelcontextprotocol/tasks`).** Rolling out across Claude products. Watch for it reaching a stable client surface before scoping gateway support.
- **Elicitation.** Client-gated, asymmetrically, today: Claude Code supports form and URL modes; claude.ai connectors do not. Revisit when claude.ai adds support, not before.
- **MCP Apps.** No current consumer need identified. Track only.
- **CIMD migration off deprecated RFC 7591 DCR.** claude.ai still uses DCR today; keep both until Anthropic actually moves claude.ai off it. This is the one item on an actual clock: a 12-month deprecation runway was announced 2026-07-28.
- **Enterprise Managed Auth extension.** No current consumer need identified. Track only.

Discovery evidence and the full research trail live in the operator's 2026-08-13 COWORK session memory, not copied into either repo. Pull that before re-deriving any of the above from scratch.

## Disclaimer

The `LICENSE` file is unchanged from upstream (MIT). Upstream copyright notice is preserved. Our fork-specific changes are clearly marked in commit messages and in this file's logs.
| 2026-07-10 | [#44](https://github.com/Umbrella-IT-Group/metamcp/pull/44) | **M365 delegated-token broker** — the gateway-side linchpin of the per-user Microsoft 365 MCP program (design doc: Umbrella-MCP-Server `docs/M365_DELEGATED_MCP_DESIGN.md` §4-5). Four surfaces. (1) *Enrollment routes* `GET /m365/enroll` → Entra auth-code+PKCE → `GET /m365/callback` (+ `/m365/status`, `POST /m365/disconnect`, TEMP `GET /m365/me` Graph echo), better-auth session-gated, state⇄user bound (callback requires the SAME signed-in user that started the flow); `next.config.js` gains the `/m365/:path*` rewrite (same requirement as `/health`). (2) *Token custody* — new `m365_user_tokens` table (migration `0018`, idempotent) holding AES-256-GCM envelopes only (`lib/m365/crypto.ts`, KEK from the SOPS-vaulted `M365_TOKEN_KEK`, AAD-bound header, per-envelope `kek_id` for rotation); deliberately NOT better-auth's plaintext `accounts` table; row cascades with user deletion. (3) *Mint service* (`lib/m365/mint-service.ts`) — per-user AT cache (60s expiry buffer), per-user single-flight (Entra rotates RTs on redemption; concurrent redemption kills the grant), atomic rotate-and-persist with in-memory pending-rotation recovery when the persist write fails, typed failure taxonomy (`credential_missing/expired/revoked`, `mfa_required`, `not_configured`, `mint_failed`) mapped from Entra error_codes, one JSON audit line per mint (no token material). DB-free module graph via type-only import + lazy repo resolution (same doctrine as `consumer-identity-resolver.ts`). (4) *Per-request injection* — `streamable-http.ts` wraps all three `transport.handleRequest` dispatch sites in an `AsyncLocalStorage` user context (OAuth consumers only); `client.ts` passes a custom transport `fetch` (`lib/m365/injected-fetch.ts`) for servers named in `M365_INJECTED_SERVER_NAMES` (default `m365`) that strips any inbound credential and stamps `Authorization: Bearer <minted Graph AT>` + `X-Umbrella-User-Id` per REQUEST. Request-scoped injection is the only design that survives the pool's idle-warmup handoff, per-server cap-reuse and global params cache without cross-user token leakage — connect-time headers are provably unsafe here. No context (idle warmup, health ping, API-key consumer, OpenAPI bridge) → NO Authorization header at all, fail-closed. `metamcp-proxy.ts` catches typed broker errors in the tools/call handler → structured `isError` result carrying the enroll URL + best-effort SEP-1036 URL-mode elicitation (fire-and-forget; SDK 1.16 predates URL mode so it's sent raw and swallowed on rejection). Boots CLEANLY with no M365 env (routes answer 503 `not_configured`, injection inert) so the image deploys before the Entra app exists. 38 new vitest cases across crypto (tamper/AAD/wrong-KEK), mint (single-flight, rotation-persist-failure recovery, error taxonomy), the injection invariant (never forwards inbound credential, no-context → no header, typed propagation, ALS through async hops) and the error→result mapping, plus review-driven additions — 14 route-level integration tests on the enrollment surface (XSS escaping + no-script CSP, single-use state, session⇄state anti-grafting binding, envelope-not-plaintext custody, PKCE verifier passthrough) and 4 transport-wiring cases pinning byte-identical construction for non-injected servers; 222 suite-wide. Review (cowork-reviewer, integrate-with-fixes): reflected XSS on the pre-auth `/m365/callback` error branch CONFIRMED and fixed (every dynamic value HTML-escaped via `escapeHtml`, `sendHtml` stamps `Content-Security-Policy: default-src 'none'`, and the router mounts the same `securityHeaders` middleware as `oauthRouter`); re-enrollment that switches Microsoft accounts now emits an `m365_enroll_account_switch` audit event (oid comparison was previously claimed-but-not-implemented). RFC 8707 `resource`-binding on the MetaMCP-issued token side was VERIFIED ABSENT in the fork's OAuth impl and deliberately left unenforced this PR (sole resource server; hard enforcement risks breaking live Claude.ai/Claude Code consumers) — tracked as a follow-up in Umbrella-MCP-Server `docs/TASKLIST.md`. | `apps/backend/src/lib/m365/*` (new), `apps/backend/src/routers/m365.ts` (new), `apps/backend/src/db/repositories/m365-tokens.repo.ts` (new), `apps/backend/drizzle/0018_m365_user_tokens.sql` (new), `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/index.ts`, `apps/backend/src/index.ts`, `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/frontend/next.config.js`, `turbo.json`, `UMBRELLA_FORK.md` | No — Umbrella-specific delegated-auth program (upstream has no per-user backend-credential concept). |
| 2026-07-10 | [#45](https://github.com/Umbrella-IT-Group/metamcp/pull/45) | **Skip the i18n middleware for `/m365/*`** — deploy-verify on PR #44 found the frontend's locale middleware 307-redirecting `/m365/status` → `/en/m365/status` BEFORE the `next.config.js` rewrite could proxy it to the backend (the `/health` path has the same skip for the same reason, fork PR #8 lineage). Entra redirects to the EXACT registered callback URI, so the locale redirect would break enrollment outright. Adds `/m365` to both the in-function skip list and the `config.matcher` regex. | `apps/frontend/middleware.ts`, `UMBRELLA_FORK.md` | No — pairs with the Umbrella-specific #44 broker. |
| 2026-07-10 | [#47](https://github.com/Umbrella-IT-Group/metamcp/pull/47) | **Remove the TEMP `GET /m365/me` mint-and-echo route** — PR #44 shipped it as a pilot-only Graph echo (mint a delegated AT, call `/me`, dump display name + UPN) so enrollment could be verified before the m365 MCP was wired. Token-custody review signed + pilot gate cleared (Umbrella-MCP-Server #377), so the debugging surface goes away; `/m365/status` remains the health/enrollment check. | `apps/backend/src/routers/m365.ts` | No — pairs with the Umbrella-specific #44 broker. |
| 2026-07-11 | [#48](https://github.com/Umbrella-IT-Group/metamcp/pull/48) | **Dependabot criticals sweep** — better-auth `^1.4.18`→`^1.6.23` (CVE-2026-53512 refresh-token-replay critical + 6H/1M/1L; the vulnerable `oidcProvider()`/`mcp()` plugins are unused here — we only use `genericOAuth` + drizzle adapter, and the 1.7-beta genericOAuth rewrite does NOT ship in 1.6.x stable), drizzle-orm `^0.44.2`→`^0.45.2` (2 highs + better-auth 1.6 peer), shell-quote `^1.8.3`→`^1.8.4` (CVE-2026-9277 critical), vitest/coverage-v8 `^4.0.0`→`^4.1.0` (CVE-2026-47429 critical, dev-only), drizzle-kit + @better-fetch/fetch peer alignment. Clears 13 alerts incl. all 3 criticals; supersedes dependabot PR #46. Known deferred peers: better-call wants zod@4 (Janhouse line), next-runtime-env wants next@14. Gate: 222/222 vitest + full turbo build. | `apps/backend/package.json`, `apps/frontend/package.json`, `pnpm-lock.yaml` | No — dependency hygiene on our line. |
| 2026-07-11 | [#49](https://github.com/Umbrella-IT-Group/metamcp/pull/49), [#50](https://github.com/Umbrella-IT-Group/metamcp/pull/50) | **Dependabot PRs merged after local gate** — next `15.5.12`→`15.5.18` (16 alerts, same-minor patch line) and turbo `2.5.5`→`2.9.14` (2 alerts, dev tool; drove the gating build itself). Each gated locally (install + full turbo build) before merge; #50 rebased over #48/#49 lockfile first. | `package.json`, `apps/frontend/package.json`, `pnpm-lock.yaml` | No — dependency hygiene. |
| 2026-07-11 | [#53](https://github.com/Umbrella-IT-Group/metamcp/pull/53) | **Transitive-alert overrides sweep** — root `pnpm.overrides`, scoped per major line (no cross-major forcing), clearing every remaining dependabot alert except the deferred `@modelcontextprotocol/sdk` 1.16→1.26 jump (9 alerts; conflict-heavy Janhouse-line upgrade touching the proxy core — gets its own reviewed lane). Runtime chains: qs, path-to-regexp, lodash, js-cookie, tar, minimatch, js-yaml, brace-expansion, picomatch, defu, glob, ajv, prismjs, postcss. Dev/build chains: vite `^7.3.5` (needed lockfile surgery — pnpm kept a stale auto-installed-peer snapshot at 7.2.7 violating vitest's own peer range through install AND dedupe; dropped the blocks, re-resolved 7.3.6), rollup, esbuild (drizzle-kit's legacy `@esbuild-kit` chain, dev-only), flatted, @eslint/plugin-kit. Plus `pnpm -r update` direct-range refreshes (@trpc/server 11.18.0). Gate: 222/222 vitest + full turbo build. | `package.json`, `pnpm-lock.yaml`, `apps/*/package.json`, `packages/trpc/package.json` | No — dependency hygiene. |
| 2026-07-11 | [#56](https://github.com/Umbrella-IT-Group/metamcp/pull/56) | **Umbrella tab favicon + auto-create-server default off** — `app/icon.png` (256×256 Umbrella Bug brandmark, same asset as the PR #7 sidebar branding; Next 15 emits the icon link automatically) and the endpoint create/edit forms' `createMcpServer` default flipped `true`→`false` at all three sites (the loopback `<endpoint>-endpoint` server row is noise for the expose-to-external-clients pattern; opt-in remains). Third quick-win candidate (Janhouse tool-name-truncation cherry-pick) assessed and deferred INTO the SDK 1.16→1.26 lane: no clamping exists in fork code, longest prod tool name ~32 chars vs the >60 trigger. | `apps/frontend/app/icon.png`, `apps/frontend/app/[locale]/(sidebar)/endpoints/page.tsx` | Checkbox default: maybe (as a config flag); favicon: no. |
| 2026-07-11 | [#57](https://github.com/Umbrella-IT-Group/metamcp/pull/57) | **Persistent tool-call audit log** (Umbrella-MCP-Server TASKLIST SPEC) — new `tool_call_audit` table (migration `0019`, idempotent; indexes on called_at/tool_name/client_name) written fire-and-forget by the PR #38/#39 auditing middleware: consumer identity, namespace, session, server, tool, sha256 `params_hash` (raw args NEVER stored — they can carry passwords), success/error_code (JSON-RPC code or error class), latency_ms. Middleware module graph stays DB-free (lazy repo import + `setAuditRecorderForTesting` seam — same doctrine as `consumer-identity-resolver.ts`); an audit-write failure never fails or delays the tool call. Retention: pruned on the oauth cleanup interval, `TOOL_AUDIT_RETENTION_DAYS` (default 90, ≤0 retains forever). Deviation from SPEC: `client_name` (resolved api-key name / OAuth email) instead of raw `user_id` — api-key consumers have no user row. Admin query UI deferred (SQL + Live Logs cover the read path). 7 new vitest cases; 229 suite-wide. | `apps/backend/drizzle/0019_tool_call_audit.sql`, `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/tool-call-audit.repo.ts` (new), `apps/backend/src/db/repositories/index.ts`, `apps/backend/src/lib/metamcp/metamcp-middleware/auditing.functional.ts` (+ new test file), `apps/backend/src/routers/oauth/index.ts`, `UMBRELLA_FORK.md` | Yes — generic auditability gap upstream shares; pairs with the #38/#39 middleware if those are upstreamed. |
| 2026-07-11 | [#58](https://github.com/Umbrella-IT-Group/metamcp/pull/58) | **Fix the image-build breaker: glob the next proxy-timeout patch path** — the Dockerfile's proxy-request `sed` (30s→600s for long MCP tool calls through the frontend rewrite) hardcoded next's pnpm store path (`next@15.5.12_react-dom@…`); PR #49's next 15.5.18 bump invalidated it, `sed` exited 2, and the #50/#53/#56 image builds all failed — prod silently stayed on the #48 image (Watchtower had nothing new to pull). Now resolves the file pair via `find -name proxy-request.js -path '*/next/dist/*'`, patches every match, and fails the build loudly if <2 files match so the patch can never silently stop applying (in either direction). | `Dockerfile` | No — the timeout patch itself is fork-specific. |
| 2026-07-13 | [#63](https://github.com/Umbrella-IT-Group/metamcp/pull/63) | **Fix dead `pull_request` CI trigger on `umbrella-build.yml`** — the workflow declared a `pull_request` trigger on branch `umbrella`, but its only job was gated `if: github.event_name == 'push' && github.ref == 'refs/heads/umbrella'`, a condition that can never be true for a `pull_request` event — the trigger existed but the job silently never fired, so no PR ever ran a build check. This is the mechanism behind Dependabot PR #54's build-breaking bump reaching as far as it did unseen (see #64's row below): the workflow *looked* like it covered PRs but didn't. Fix: the job now runs on both events; only the registry-touching steps (`docker/login-action`, `docker/metadata-action`, `attest-build-provenance`, and `push: true` on `build-push-action`) stay push-gated via their own `if`, so a PR run proves the Dockerfile still builds without publishing anything. `paths:` filtering unchanged for both triggers. Verified live on itself: `gh pr checks 63` → `build-and-push pass 2m27s`; the job's step list confirms `docker/login-action`/`docker/metadata-action`/`attest-build-provenance` all show skipped (`-`) while checkout/buildx/build-push ran and passed. `actionlint` clean before and after. `gh pr view 63` → `"mergeable":"MERGEABLE","mergeStateStatus":"CLEAN"`. Note for the operator: the first PR-triggered build has no `cache-to` write (by design — a PR build never writes the shared `metamcp-umbrella` GHA cache scope it doesn't own), so it may run slower than a push-triggered build; later PR builds still get `cache-from` reads against whatever the push path already wrote. `workflow_dispatch` was already unreachable before this change too (same job-level `if`) — untouched here, flagged as a separate follow-up if manual dispatch is meant to work. Merged 2026-07-13T16:54:44Z (`5dbc9f7`). | `.github/workflows/umbrella-build.yml` | No — Umbrella-specific CI/infra housekeeping (mirrors the fix pattern of #41/#42). |
| 2026-07-13 | [#64](https://github.com/Umbrella-IT-Group/metamcp/pull/64) | **zod v3→v4 + `@modelcontextprotocol/sdk` 1.16.0→1.29.0 — clears all 9 open HIGH Dependabot alerts** — the SDK's schemas carry zod-4 internal markers while its public generics are checked against the local zod, so the SDK bump only type-checks once the whole monorepo (`@repo/zod-types`, `@repo/trpc`, backend, frontend) is also on zod `^4.4.3`. All 9 HIGH alerts are `@modelcontextprotocol/sdk` (3 GHSAs × 3 manifests — GHSA-w48q-cv73-mx4w/GHSA-8r9q-7v3j-jr4g/GHSA-345p-7cg4-v4c7, patched at 1.24.0/1.25.2/1.26.0 respectively; 1.29.0 clears all three). This is the dedicated lane Dependabot PR **#54 was CLOSED as unsafe to merge (2026-07-13)** to wait for — #54's close comment: bumping the SDK alone breaks `next build` (189 new type errors across 9 SDK-importing files, reproduced identically at 1.29.0) because the SDK's zod-v4-marked schemas aren't assignable against the repo's then-pinned zod 3.25.64; the fix needed the zod migration as a prerequisite, not a same-lane patch. This PR is that migration — our own, not a blind cherry-pick of the `Janhouse/metamcp` fork's zod-v4 line (studied for breaking-change patterns only; our fork had diverged enough — e.g. it never type-checked its backend — that it never hit the pgEnum/session-recovery-hydration issues below). zod 4 breaking changes handled: `z.record(value)` → `z.record(z.string(), value)` (single-arg overload removed; applied to env/headers/tool-schema maps in `@repo/zod-types`, semantics preserved); `.Enum`/`.Values` accessors removed in favor of `.enum`/`.values` (111 sites — runtime-verified as a real fix, not cosmetic: v4's `.Enum` is `undefined`, the old code would throw); `z.enum(...).options` widened from a literal tuple to `string[]`, breaking drizzle `pgEnum`'s literal-union inference — fixed at the source with `as const` tuples (`MCP_SERVER_TYPES`/`_STATUSES`/`_ERROR_STATUSES`) feeding both the zod enum and the drizzle `pgEnum` from one source of truth; `ZodError.issues` shape changes in `validation-utils.ts` (`invalid_string` → `invalid_format`, `too_small`/`too_big` dropped `.type`). MCP SDK 1.29 breaking changes handled: `ClientCapabilities` now strict (client advertises `{}`, wire-equivalent); `ContentBlock` is a discriminated union (narrow by `type` before reading `.text`/`.blob`); `StreamableHTTPServerTransport` now wraps an inner `_webStandardTransport` — the lazy session-recovery hydration from PR #27 (and its startup contract check) is ported to the inner transport, with the no-mock test rewritten against 1.29's `Request → Response \| undefined` `validateSession`. `pnpm run build --force` (turbo) exit 0, 4/4 tasks; `check-types` exit 0; backend `tsc --noEmit` 0 errors; backend vitest **230 passed, 18 files**. `pnpm run lint` (`--max-warnings 0`) is a known pre-existing repo-level failure (frontend 22 / backend 36 warnings on `umbrella` before this branch, per `umbrella-build.yml`'s own comment) — verified via stash that this PR introduces zero new warnings; not a gate per that workflow, and `check-types` going green incidentally clears the pre-existing `trpc.ts` type error the workflow's own comment references. Separately and independently of this PR: `.github/dependabot.yml` (security-update grouping by ecosystem, PR #60) was **OPEN as of 2026-07-13** when this PR was authored, explicitly gated in its own PR body on an operator-approved maintenance window — untouched by this pass; #60 subsequently merged 2026-07-13T16:55:38Z (`955950d`). Merged 2026-07-13T16:54:48Z (`3060bb8`). | `apps/backend/package.json`, `apps/backend/src/db/repositories/mcp-servers.repo.ts`, `apps/backend/src/db/schema.ts`, `apps/backend/src/lib/bootstrap.service.ts`, `apps/backend/src/lib/config.service.ts`, `apps/backend/src/lib/metamcp/client.test.ts`, `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/fetch-metamcp.ts`, `apps/backend/src/lib/metamcp/server-error-tracker.ts`, `apps/backend/src/lib/metamcp/transport-recovery-hydration.test.ts`, `apps/backend/src/lib/metamcp/transport-recovery-hydration.ts`, `apps/backend/src/routers/mcp-proxy/server.ts`, `apps/backend/src/routers/public-metamcp/openapi/tool-execution.ts`, `apps/backend/src/trpc/mcp-servers.impl.ts`, `apps/frontend/app/[locale]/(sidebar)/mcp-inspector/components/inspector/inspector-prompts.tsx`, `apps/frontend/app/[locale]/(sidebar)/mcp-inspector/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/mcp-servers/[uuid]/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/mcp-servers/export-import-buttons.tsx`, `apps/frontend/app/[locale]/(sidebar)/mcp-servers/mcp-servers-list.tsx`, `apps/frontend/app/[locale]/(sidebar)/mcp-servers/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/namespaces/[uuid]/components/enhanced-namespace-tools-table.tsx`, `apps/frontend/app/[locale]/(sidebar)/namespaces/[uuid]/components/namespace-servers-table.tsx`, `apps/frontend/app/[locale]/(sidebar)/namespaces/[uuid]/components/namespace-tool-management.tsx`, `apps/frontend/app/[locale]/(sidebar)/namespaces/[uuid]/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/search/components/CardGrid.tsx`, `apps/frontend/components/edit-mcp-server.tsx`, `apps/frontend/hooks/useConnection.ts`, `apps/frontend/lib/validation-utils.ts`, `apps/frontend/package.json`, `packages/trpc/package.json`, `packages/zod-types/package.json`, `packages/zod-types/src/mcp-servers.zod.ts`, `packages/zod-types/src/metamcp.zod.ts`, `packages/zod-types/src/namespaces.zod.ts`, `packages/zod-types/src/tools.zod.ts`, `pnpm-lock.yaml` | Yes — the SDK/zod dependency line is upstreamable in principle (upstream will hit the same GHSAs), though upstream is unattended enough this may never get reviewed. |
| 2026-07-13 | [#62](https://github.com/Umbrella-IT-Group/metamcp/pull/62) | **Per-server "Reconnect Server" — flushes the pooled tool list without a gateway restart (parts 1+2 of the no-restart refresh brief; part 3 deferred, see below)** — MetaMCP re-lists tools live on every `tools/list`, but over one upstream connection per server the pool keeps warm forever; idle sessions are only health-pinged, never recycled while healthy, so an upstream rename/add keeps serving its connect-time list indefinitely (the DB resync is additionally gated behind `toolsSyncCache.hasChanged`, which never trips on a live rename). Before this PR the only full flush was a gateway restart (`cleanupAll` + `GATEWAY_BOOT_ID`). New `mcpServers.reconnect` tRPC mutation runs the FULL `invalidateServerConnection` cascade (every active AND idle slot for the `serverUuid`, fires PR #19's `list_changed` subscribers) — not the update path's `invalidateIdleSession`, which only rebuilds the idle spare and is why the existing PATCH path can't bust rename-rot — then resets the STDIO error gate and refreshes idle MetaMCP + OpenAPI aggregations and tool-override caches. New "Reconnect Server" button (confirm dialog + toast) beside Refresh Tools on the single-server tool-management surface; Refresh Tools itself is left as-is (it re-lists over the SAME pooled connection, so it cannot bust rename-rot — the new button is the actual flush). Owner-or-public trust boundary matches update/delete. `pnpm run build` exit 0 (4/4 turbo tasks); backend vitest **236 passed, 19 files** (7 new `mcp-servers.reconnect.impl.test.ts` cases: full-cascade fan-out, no-namespace path, public-server allow, foreign-owner deny + no cascade, not-found + no cascade, primary-cascade failure surfaced not swallowed, namespace fan-out best-effort). **Part 3 (auto-detect via a timer-driven idle-connection re-list + hash-compare, no manual click) is DEFERRED, not shipped** — binding constraint: the stated failure mechanism is that the warm pooled connection serves its connect-time tool list; if that is literally true, re-listing over the SAME idle connection returns the same stale list, `toolsSyncCache`'s hash never changes, and the auto-detector is a no-op against the exact problem it targets. Idle sessions also have no `list_changed` subscribers (they attach on `getSession`), so an auto-fired `invalidateServerConnection` against a purely-idle server has no one to fan out to. Falsifier: a live experiment where an upstream renames a tool WITHOUT dropping its connection, and a fresh `tools/list` over the retained idle connection returns the new name, would confirm the detector can work — needs a real upstream on the live gateway, out of scope for this lane. Parts 1+2 fully kill the manual-restart ritual for a given server, which is the lane's core DONE criterion. Merged 2026-07-13T16:55:30Z (`a053b9f`). | `apps/backend/src/trpc/mcp-servers.impl.ts`, `apps/backend/src/trpc/mcp-servers.reconnect.impl.test.ts` (new), `apps/frontend/app/[locale]/(sidebar)/mcp-servers/[uuid]/components/tool-management.tsx`, `apps/frontend/public/locales/en/mcp-servers.json`, `packages/trpc/src/routers/frontend/mcp-servers.ts`, `packages/zod-types/src/mcp-servers.zod.ts` | Yes — capability declaration/flush semantics are upstreamable; the frontend button is fork-shaped only in that it surfaces existing pool internals. |
| 2026-07-13 | [#61](https://github.com/Umbrella-IT-Group/metamcp/pull/61) | **Bounded warmup fallback for `tools/call` reconnect-window 404** — closes a routing gap the invalidate-and-retry cascade (PR #13/#16/#28) doesn't cover: `McpServerPool.invalidateServerConnection` tears the pooled client down and fires `list_changed` **before** the replacement connection exists, and a call landing in that gap falls through the dynamic-find loop's `if (session)` guard without the owning server's name ever being checked — `clientForTool` stays unset, the handler throws `"Unknown tool: ..."`, and the OpenAPI bridge maps that to a literal HTTP 404 for a tool that is real (the transient 404 the n8n pipeline already retry-hardens around). Fix, added to both `metamcp-proxy.ts` and the OpenAPI bridge's independent copy, right before the existing throw: (1) last-known-good routing via `namespacesRepository.findServersForNamespaceToolName`, keyed off the DB `toolsTable` populated by the last successful `tools/list` sync, independent of whether a live session exists; (2) if a match is found and still in the namespace's active/non-error server set, a bounded warmup race (new `acquireSessionWithBoundedWarmup` in `tool-call-warmup.ts` — one `getSession` attempt raced against `MCP_TOOL_CALL_RECONNECT_WARMUP_TIMEOUT`, default 5000ms, one retry — worst-case ~10s added latency, does not touch `client.ts`'s own longer reconnect backoff); (3) no match, or the warmup doesn't produce a session — falls through to the existing `"Unknown tool"` throw unchanged. Tools that never existed anywhere skip the wait entirely (zero added latency); a server removed from the namespace has no `params` to open a session with (also zero added latency); a genuinely dead but still-member upstream is bounded at ~10s, never an unbounded hang. `pnpm run build` exit 0 (4/4 turbo tasks); `check-types` exit 0; backend `tsc --noEmit` 0 errors (no `check-types` script on `apps/backend`, run directly since it's the live gateway); backend vitest **234 passed, 19 files** (5 new `tool-call-warmup.test.ts` cases: fast-path resolve, bounded retry, bounded exhaustion returns `undefined` without hanging, custom `attempts` override, defensive reject-handling). `pnpm run lint` (`--max-warnings 0`) fails at 36 pre-existing backend warnings verified identical against untouched `origin/umbrella` (none in a file this PR touches) — not fixed, out of scope for this lane. Merged 2026-07-13T16:55:34Z (`00ab46b`). | `packages/zod-types/src/config.zod.ts`, `apps/backend/src/lib/config.service.ts`, `apps/backend/src/lib/metamcp/tool-call-warmup.ts` (new), `apps/backend/src/lib/metamcp/tool-call-warmup.test.ts` (new), `apps/backend/src/db/repositories/namespaces.repo.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/routers/public-metamcp/openapi/handlers.ts` | Yes — the routing gap exists upstream too; pairs with the PR #13/#16/#28 recovery-cascade line. |
| 2026-07-13 | [#60](https://github.com/Umbrella-IT-Group/metamcp/pull/60) | **Group Dependabot security-update PRs by ecosystem** — first `.github/dependabot.yml` in the repo (none existed; security-update alerts already open PRs without one — it's a Security-settings toggle, not file-driven). Adds `npm` (root `/`, covers the whole pnpm workspace via the single `pnpm-lock.yaml`) and `github-actions` (root `/`, 7 pinned actions) ecosystems, each with a `groups.security-updates` block (`applies-to: security-updates`, `patterns: ["*"]`) so a wave of alerts lands as one PR instead of one-per-package — PRs #48/#49/#50/#53 were five sequential PRs clearing a single wave. Scheduled version-update PRs (`interval: weekly`) are left ungrouped, unchanged from default. Companion investigation (ticket T20260710.0012, not a PR — see `sec/dependabot-metamcp-20260713`, not pushed): the 9 currently-open HIGH `@modelcontextprotocol/sdk` alerts (GHSA-w48q-cv73-mx4w / GHSA-8r9q-7v3j-jr4g / GHSA-345p-7cg4-v4c7) need >=1.26.0 to clear; bumping to 1.26.0 (and 1.29.0, latest, tested identically) breaks `next build` — SDK-exported Zod schemas like `EmptyResultSchema` no longer satisfy the SDK's own `ZodType<any, ZodTypeDef, any>` generic bound. Matches this file's own `Janhouse/metamcp` note ("zod 3→4, MCP SDK 1.16→1.29 — conflict-heavy, take last"); the SDK bump needs that migration as a prerequisite. Dependabot PR #54 left open (identical break). Merged 2026-07-13T16:55:38Z (`955950d`). | `.github/dependabot.yml` (new), `UMBRELLA_FORK.md` | No — dependency-hygiene process config, Umbrella-specific tooling choice. |
| 2026-07-14 | [#68](https://github.com/Umbrella-IT-Group/metamcp/pull/68) | **RBAC (`users.role`) + API-key governance.** Merged 2026-07-14T22:05Z (`1677d36`), squash, after a security-review round (member public-key mutation BLOCKER fixed; config write surface gated). PROD-VERIFIED same day: alex@=admin, 8 members, last_used_at stamping live. First PR of the MetaMCP RBAC wave; closes the "every authenticated user is a full admin" gap (today all frontend mutations sit on `protectedProcedure`). (1) New `users.role` (`text NOT NULL default 'member'`, `'admin' | 'member'`; migration `0020`, hand-written idempotent per fork convention, seeds `alex@umbrellaitgroup.com` → `admin` via idempotent `UPDATE` by email — **fresh-install note**: on a virgin database the seed UPDATE matches zero rows, so NO ADMIN EXISTS until someone is promoted manually after sign-up; prod is unaffected, the alex@ row already exists there) surfaced into the better-auth session via `user.additionalFields` with `input: false` (no self-escalation; sessions read fresh from DB per request so demotion is immediate). (2) `adminProcedure` = `protectedProcedure` + `role==='admin'` (FORBIDDEN otherwise; unauth still UNAUTHORIZED first) plus an extracted pure `requireAdmin` guard, gating: create/update/delete of MCP servers, namespaces, endpoints; the three namespace **curation** mutations `updateServerStatus`/`updateToolStatus`/`updateToolOverrides` (same "destructive update" class — they change which servers/tools an agent sees); `tools.create`/`tools.sync` (writes to the shared tools catalog); `logs.clear`; `oauth.upsert` (upstream MCP-server OAuth credential writes); the entire global config write surface (`setSignupDisabled`/`setSsoSignupDisabled`/`setBasicAuthDisabled`/`setSessionLifetime`/`setConfig`/the MCP timeout+attempt setters — reads keep their existing access level); the api-key admin listing; and public-key minting. `mcpServers.bulkImport` gated too (a create path that would otherwise bypass the create gate). Deliberately LEFT member-accessible (operational nudges, no config mutation, each commented in-line so a future sweep doesn't "fix" them): `namespaces.refreshTools` and `mcpServers.reconnect` — verified by code inspection that `refreshTools` calls `toolsRepository.bulkUpsert` directly from `namespaces.impl.ts`, never routing through the now-gated `tools.create`/`tools.sync` procedures, so gating those has no effect on it. Members keep read surfaces + minting **private** keys + managing their own; the public MCP request path + better-auth flows are untouched. (3) New nullable `api_keys.last_used_at` (migration `0021`) stamped fire-and-forget + throttled to 15 min by `validateApiKey` (pure decision in `api-keys.last-used.ts`; never awaited, never rejects — the hot public-endpoint auth path gains no latency and no failure mode; existing Tara/n8n/Claude consumers behavior-identical bar the additive stamp). (4) Dormant `findAll()` wired into an admin-only `listAll` tRPC query (LEFT JOIN users → owner email or `null`=public, created, last-used, active) that returns a non-reversible key **prefix only**, never the raw secret — deliberately different from the member-facing `list`, which continues returning the FULL key value for public ('everyone') keys (that's the "copy the shared key to configure your client" feature; a public key is already distributed to every consumer, so masking it there would only reduce usefulness, not exposure — private keys are never exposed this way). (5) `updateAsAdmin`/`deleteAsAdmin` bypass the ownership WHERE so admins revoke/disable/delete ANY key including public ones; members stay owner-scoped. **BLOCKER fix from independent security review (2026-07-14, same day)**: the member-scoped `update()`/`delete()` WHERE originally read `or(eq(user_id, userId), isNull(user_id))` — the `isNull` branch also matched PUBLIC keys, so any member holding a public key's uuid (visible via their own `list`) could deactivate or DELETE it. Narrowed to `eq(user_id, userId)` only; a public key is now mutable exclusively through `updateAsAdmin`/`deleteAsAdmin`. Proven with a structural regression test that spies on drizzle-orm's real `or`/`isNull` and asserts neither is invoked by the member path. Mint path also rejects a member assigning a key to another user's id (ownership spoofing), not just the public case. (6) Frontend API Keys page reads the session role → admin-only "All API Keys" section (owner/created/last-used/active/revoke, sourced from the gated `listAll` which members never fire) + hides the 'everyone' mint option for non-admins; no separate admin nav route (the role-gated section is the admin surface). Gates (final, post-review-fixes): `pnpm run build` (turbo) 4/4; backend `tsc --noEmit` 0; `check-types` 2/2; backend vitest **279 passed, 27 files** (37 new across 8 test files); lint introduces zero net new warnings (`@repo/trpc`/`@repo/zod-types` fully clean; backend's true `origin/umbrella` baseline, freshly re-verified via a disposable worktree, is 34 — not the "36" this file previously quoted, which was stale — and this branch matches it exactly, none in touched files). | `apps/backend/src/db/schema.ts`, `apps/backend/src/auth.ts`, `apps/backend/drizzle/0020_users_role.sql` (new), `apps/backend/drizzle/0021_api_keys_last_used_at.sql` (new), `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/repositories/api-keys.repo.ts`, `apps/backend/src/db/repositories/api-keys.repo.member-scope.test.ts` (new), `apps/backend/src/db/repositories/api-keys.last-used.ts` (new), `apps/backend/src/db/repositories/api-keys.last-used.test.ts` (new), `apps/backend/src/db/serializers/api-keys.serializer.ts`, `apps/backend/src/trpc/api-keys.impl.ts`, `apps/backend/src/trpc/api-keys.impl.test.ts` (new), `apps/backend/src/trpc/admin-procedure.test.ts` (new), `apps/backend/src/trpc/namespaces-curation-admin.test.ts` (new), `apps/backend/src/trpc/config-admin-gate.test.ts` (new), `apps/backend/src/trpc/admin-gate-sweep.test.ts` (new), `packages/trpc/src/trpc.ts`, `packages/trpc/src/index.ts`, `packages/trpc/src/routers/frontend/index.ts`, `packages/trpc/src/routers/frontend/api-keys.ts`, `packages/trpc/src/routers/frontend/mcp-servers.ts`, `packages/trpc/src/routers/frontend/namespaces.ts`, `packages/trpc/src/routers/frontend/endpoints.ts`, `packages/trpc/src/routers/frontend/config.ts`, `packages/trpc/src/routers/frontend/tools.ts`, `packages/trpc/src/routers/frontend/logs.ts`, `packages/trpc/src/routers/frontend/oauth.ts`, `packages/zod-types/src/api-keys.zod.ts`, `apps/frontend/app/[locale]/(sidebar)/api-keys/page.tsx`, `UMBRELLA_FORK.md` | Partly — `adminProcedure`, the config-write/curation gating pattern, and `last_used_at` telemetry are generic gaps upstream shares; the `member`/`admin` policy + operator seeding are Umbrella-specific. |
| 2026-07-14 | [#69](https://github.com/Umbrella-IT-Group/metamcp/pull/69) | **Full-definition tool hashing — schema/description-only changes now resync (no-reboot wave, Track A3)** — `ToolsSyncCache.hashTools()` hashed tool NAMES only, on the false premise (in its own docstring) that names uniquely identify a tool per server. A backend that reworks a tool's `inputSchema` or `description` while keeping the same name produced an identical hash, so `hasChanged()` returned false, the DB was never resynced, and the change never propagated to clients — only a name add/remove ever triggered a sync (verified by toy harness on fastmcp 3.4.2 + SDK 1.29.0, plus an independent adversarial audit: same nameHash, different full-def hash). Now hashes the full `{name, description, inputSchema}` of every tool, canonicalized with recursively-sorted JSON key order and tools sorted by name (array order and inputSchema key order are both normalized out). Both callers pass full tool objects instead of names: `metamcp-proxy.ts`'s `tools/list` DB-sync hash check and `tools.impl.ts`'s `sync()` check. Public cache API surface unchanged except the tool-list param is now `ToolDefinition[]` not `string[]`. **Migration:** first deploy flips every cached hash once (name-hash → full-def hash) → a one-time full resync per server on the first `tools/list` after deploy — cheap, expected; steady state unchanged. `tools-sync-cache.test.ts` rewritten for full objects + change-detection matrix (schema-only flips, description-only flips, array-order no-flip, inputSchema-key-order no-flip, name change flips, undefined-desc ≡ null): 26 cases. Gates: backend vitest **248 passed (20 files)**, backend `tsc --noEmit` exit 0, `pnpm run build` 4/4 turbo tasks, ESLint `--max-warnings 0` clean on touched files. Merged 2026-07-14T22:08Z (`e0bbbe9`), squash. Track A2 (#70) merged on top. Track A2's periodic sweep (branch `feat-tools-sweep`) is stacked on this branch and must merge after it. | `apps/backend/src/lib/metamcp/tools-sync-cache.ts`, `apps/backend/src/lib/metamcp/tools-sync-cache.test.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/trpc/tools.impl.ts`, `UMBRELLA_FORK.md` | Yes — the name-only-hash bug exists upstream too; clean bug fix with no Umbrella-specific coupling. |
| 2026-07-14 | [#70](https://github.com/Umbrella-IT-Group/metamcp/pull/70) | **Periodic full-definition tools/list sweep — no-reboot tool updates (no-reboot wave, Track A2)** — the pull half of the no-reboot pair. Prod backends deliver tool updates as container REPLACES that kill the process (fastmcp `add_tool`/`remove_tool` have no notify hook); on replace the SDK standalone GET stream — the only push channel — dies and its auto-reconnect exhausts before the replacement container boots, so no `tools/list_changed` push survives an update. This adds a pool-level periodic PULL sweep in `mcp-server-pool.ts`: every `TOOLS_SWEEP_INTERVAL_SECONDS` (env, default 60, `0` disables — no timer scheduled), for each backend that ALREADY has a live pooled connection (active preferred since those carry the `listChangedSubscribers`; idle is a valid detection fallback — never opens a NEW connection just to sweep), it re-lists tools over that existing connection (paginated to mirror `metamcp-proxy`'s loop), computes the Track A3 full-definition hash. On drift it logs one INFO line (`tool definitions changed for <server>, resyncing`) and runs the SAME cascade a transport drop uses — `invalidateServerConnection` → PR #19 `list_changed` fan-out → consumer re-list → DB resync — with NO new propagation mechanism. **Sweep-owned baseline (added in the review round, see below):** the sweep compares against a baseline it owns itself (`toolsSweepLastHash`, per serverUuid) — NOT `toolsSyncCache`. Seeds silently on first observation, updates on every tick, invalidates only when the new hash differs from the sweep's OWN prior observation — `toolsSyncCache` is used only as a pure hashing utility (`hashTools`), never its stateful `hasChanged`/`update` surface. This makes the invalidate fire exactly once per real change regardless of whether anything downstream reacts. Guard rails: one sweep in flight at a time (overlapping tick skipped); per-server failures (tools/list OR the invalidate cascade) logged at debug and never abort the loop or crash the timer; bounded 10s per-request timeout; a non-advancing `nextCursor` stops pagination immediately (WARN) and `TOOLS_SWEEP_MAX_PAGES` (50) backstops any other infinite-pagination shape (WARN); timer cleared on pool dispose (`cleanupAll`); `toolsSweepLastHash` pruned on server deregistration (same category as the existing `lastErrorProbeAt`/`activePingFailures` cleanup); runs as an ordinary JSON-RPC request over the shared transport so it multiplexes with in-flight tool calls. `TOOLS_SWEEP_INTERVAL_SECONDS` added to `turbo.json`'s env allowlist. **Review round (2026-07-14, foreman verdict MERGE-AFTER-FIXES):** three findings on the first cut, which compared the sweep directly against `toolsSyncCache`. Finding 1 (high): an idle-only server has zero `listChangedSubscribers`, so the fan-out reaches no one and `toolsSyncCache` never updates downstream — comparing against that frozen cache re-detected the SAME already-handled change every ~60s tick FOREVER (invalidate → health-check timer recreates the idle connection → re-detect), on exactly the Watchtower-replace scenario this PR targets. Finding 2 (medium): `tools.impl.sync` overwrites `toolsSyncCache` with the namespace-FILTERED tool set whenever `filterOutOverrideTools` drops a tool, while the sweep always lists the UNFILTERED set — permanent false-drift even for servers with active consumers. Fix (closes 1+2): the sweep-owned baseline described above — owning a separate map makes both failure modes structurally impossible. Finding 3 (low): `listToolsForSweep`'s pagination had no cursor-advance guard; a backend returning a constant `nextCursor` would loop forever, and since `toolsSweepInProgress` only clears in the sweep's `finally`, that would wedge the sweep permanently. Fix: the non-advancing-cursor + page-cap guards described above. `mcp-server-pool.test.ts` sweep suite: 10 cases (rewritten for two-tick seed-then-observe semantics) incl. the three review-requested regressions — (a) multi-tick idle-only/zero-subscribers: one real change → exactly ONE invalidate, quiet after; (b) baseline independence: a stale/filtered `toolsSyncCache` entry doesn't fire when the listed unfiltered set is unchanged across ticks; (c) non-advancing cursor trips the guard and the in-flight lock releases for the next tick — plus a hard-page-cap test. Gates: backend vitest **260 passed (20 files)**, backend `tsc --noEmit` exit 0, `pnpm run build` 4/4 turbo tasks, ESLint `--max-warnings 0` introduces zero new warnings on touched files (the 3 remaining `no-explicit-any` on `backgroundIdleSessionsByNamespace` are pre-existing on `umbrella`, verified). **Base: `feat-tools-fulldef-hash` (PR #69), NOT `umbrella` — stacked; MERGE AFTER #69** and do not squash-merge #69 with `--delete-branch` while this PR still targets it (auto-closes it): retarget to `umbrella` after #69 lands, or merge #69 first without deleting its branch. Merged 2026-07-14T22:10Z (`a36498c`), squash, after a review-fix round (sweep-owned drift baseline; exactly-once invalidation). | `apps/backend/src/lib/metamcp/mcp-server-pool.ts`, `apps/backend/src/lib/metamcp/mcp-server-pool.test.ts`, `turbo.json`, `UMBRELLA_FORK.md` | Yes — the no-push-survives-update gap exists upstream too; pairs with the PR #16/#19/#20 invalidation-cascade line and Track A3 (#69). |
| 2026-07-14 | [#71](https://github.com/Umbrella-IT-Group/metamcp/pull/71) | **Non-retryable m365 broker errors on cold connect + `closing` guard + actionable connect-error logs (Track A5).** Fixes three cold-connect defects in `connectMetaMcpClient`, all confirmed against prod logs. (1) The per-user m365 broker mints a Graph token on EVERY request including the backend `initialize` handshake, so a fresh connect for an unlinked user throws `M365BrokerError` (`credential_missing`) — a deterministic identity rejection. The retry loop caught it as any error and retried 3× (the "Connect attempt N/3 failed" storm) then resolved `undefined`; the enrollment elicitation (`buildM365BrokerErrorResult`) only ran on the WARM tool-call path, so a cold-connecting unlinked user never saw the enroll URL (a teammate hit exactly this). Now: detect `isM365BrokerError` in the catch → one attempt, no retry, single INFO line (identity state, not a server fault). **Enrollment delivery mechanism (the design-latitude call):** the cold connect runs inside the consumer's request ALS context (`runWithM365UserContext` wraps `transport.handleRequest`), so the broker error is user-attributable — but it can't simply be re-thrown: the pool's connect path swallows it to `undefined` (`mcp-server-pool.ts` is off-limits, owned by another lane) and the proxy's dynamic-find catch-and-continue must keep routing non-m365 tools. So connect LATCHES the error into a mutable field on the request-scoped `M365UserContext` (`recordConnectBrokerFailure`, `lib/m365/request-context.ts`); the outer `tools/call` handler in `metamcp-proxy.ts` — the same handler that already translates warm-path broker errors — drains it (`takeConnectBrokerFailure`) when the call otherwise failed and answers with `buildM365BrokerErrorResult` (structured `isError` + enroll_url + SEP-1036 elicitation), the SAME surface as the warm path. Delivery is gated on the requested tool's prefix matching the latched server name, so an incidental m365 probe while routing an unrelated tool can't hijack that tool's error. Pool contract unchanged (connect still resolves `undefined`) — zero blast radius outside three files. `tools/list` cleanly omits the unconnectable backend (can't carry an `isError` tool result); the prompt reaches the user on tool invocation. (2) The failed-connect cleanup closed the transport WITHOUT setting the `closing` guard, logging its own intentional close as "Transport closed unexpectedly (backend drop)" twice per attempt — now `closing` is hoisted to loop scope and set in the catch, and the drop handlers additionally require `connectedAt !== undefined` (handshake completed) before logging a drop, so a never-established transport is a connect-time failure (surfaced by the connect-attempt log), not a fake drop; real established-then-dropped detection (watchtower/container-replace) preserved, now stamped with connection age (`established 2h ago` vs `never-connected`). (3) New `lib/metamcp/connect-error.ts` recursively unwraps undici's nested `.cause` and `AggregateError.errors` (cycle-safe) to the leaf syscall/code/address — logs `connect ECONNREFUSED 172.18.0.13:3000` instead of `fetch failed`/`terminated` — reused at the connect-failure log and both transport drop-log sites; no new INFO lines for healthy operation. Tests: `connect-error.test.ts` (undici nested cause, AggregateError, plain/non-Error, cycles, age), `request-context.test.ts` (latch record/take, fail-open, no cross-request leak, end-to-end same-as-warm enrollment result), `client-connect.test.ts` (broker → one attempt + latch + no drop/attempt logs + no `onTransportDrop`; transient → 3 retries + unwrapped cause; closing guard both directions). Gates: backend `vitest` **318 passed, 30 files**, backend `tsc --noEmit` 0 errors, `eslint --max-warnings 0` clean on touched files, backend `tsup` build clean (full `pnpm build` 4/4 verified pre-rebase; diff is backend-only). Branched off `origin/umbrella`, rebased onto current `umbrella` (incl. #68/#69/#70) — clean 3-way. `mcp-server-pool.ts` and `tools-sync-cache.ts` untouched. Merged 2026-07-14T23:27Z (`7af16aa`), squash, after a review-fix round (mint_failed stays retryable; fallback glue extracted + tested). | `apps/backend/src/lib/metamcp/client.ts`, `apps/backend/src/lib/metamcp/metamcp-proxy.ts`, `apps/backend/src/lib/m365/request-context.ts`, `apps/backend/src/lib/metamcp/connect-error.ts` (new), `apps/backend/src/lib/metamcp/connect-error.test.ts` (new), `apps/backend/src/lib/metamcp/client-connect.test.ts` (new), `apps/backend/src/lib/m365/request-context.test.ts` (new), `UMBRELLA_FORK.md` | No — Umbrella-specific delegated-auth program (builds on #44's per-user broker; upstream has no per-user backend-credential concept). |
| 2026-07-14 | [#72](https://github.com/Umbrella-IT-Group/metamcp/pull/72) | **Public-session TTL sweeper — structural fix for pool-cap saturation (pool-cap wave, Track A4 / METAMCP-POOL-1)** — public endpoint (API-key/OAuth) StreamableHTTP sessions are created per request stream and only torn down on a client-sent `DELETE`; most clients never send it, so sessions accumulate indefinitely (prod: 241 → 1363 sessions in 17h). Each holds backend pool connections; once the global backend pool hits `MAX_TOTAL_CONNECTIONS` every new connect LRU-evicts a LIVE connection (`evictOneForCapacity`), surfacing as transient `"Failed to re-initialize session ... after backend session loss"` tool failures (Hermes/autotask; 278 cap-refusals / 148 evictions in 25h). The 2026-07-14 `100 → 400` cap raise (`compose.fragment.yml`) only bought headroom. The existing age-based `SessionLifetimeManagerImpl` cleanup timer keys off `configService.getSessionLifetime()`, which is null in prod BY DESIGN (persistent sessions never expire), so it never fires — this reaps on a DIFFERENT axis: last-request IDLE time. New isolated module `apps/backend/src/routers/public-metamcp/public-session-sweeper.ts` (`PublicSessionSweeper`), wired into `streamable-http.ts` (the file that owns the public-session registry `sessionManager` and the `cleanupSession` DELETE path). Tracks last-activity per session in a cheap in-memory map (stamped on every request via `markInFlight`/`markSettled` around the dispatch + `beginTracking` at session creation); a periodic sweeper reaps sessions idle beyond `PUBLIC_SESSION_TTL_SECONDS` (default 86400/24h — generous so a long-idle-but-real consumer like Hermes/Tara survives a workday) every `SESSION_SWEEP_INTERVAL_SECONDS` (default 300; 0 disables either knob, no timer) by running `reapIdleSession` — a ROW-PRESERVING teardown variant, NOT the same path a client DELETE uses (see Review round below). A reaped session's consumer reconnects transparently on its next request via the fork's lazy session-recovery path (rebuilds the transport from `mcp_sessions` — verified live 2026-07-14). Never reaps a session with an in-flight request (a long tool call, or an open standalone GET stream, that outlives the TTL is live use, not idleness — guarded on an in-flight counter incremented around the dispatch). Conventions mirror PR #70's tools sweep: env-tunable interval, a re-entrancy guard (`sweepInProgress`, cleared in `finally`), cleanup on dispose (`stop()`, wired into `index.ts`'s `gracefulShutdown`), WARN/INFO discipline — one INFO line per sweep ONLY when it reaps (`reaped N idle public session(s) (ttl Xs), released ~M connections`, where M = a best-effort backend active-connection-count delta across the reap batch), debug otherwise. Reap counters (`totalSweeps`/`totalReaped`/`totalConnectionsReleased`/`lastSweepAt`/…) exposed additively on the `/health/sessions` payload. `PUBLIC_SESSION_TTL_SECONDS` + `SESSION_SWEEP_INTERVAL_SECONDS` added to `turbo.json`'s env allowlist. **Scope: StreamableHTTP only** — SSE public sessions already self-reap on socket close (`res.on("close")` in `sse.ts`), so they are not the accumulation vector; the shared `SessionLifetimeManagerImpl` and the age-based `sessionLifetime` path are untouched. **Review round (2026-07-14, foreman verdict MERGE-AFTER-FIXES):** one MAJOR + five smaller findings on the first cut. MAJOR: the sweep reaped via `cleanupSession`, which ALSO deletes the `mcp_sessions` row — a reaped session's next request then 404'd instead of lazy-recovering, and while spec-conformant SDK clients handle that cleanly (re-`initialize`), the Anthropic/claude.ai connector wraps the 404 as `-32600 "Anthropic Proxy: Invalid content from server"` and stays broken until a manual `/mcp reconnect` — the exact failure mode PR #22/#23's capability-hash refusal narrowing exists to avoid. Fix: `cleanupSessionInternal` now takes a `deleteRow` flag; `cleanupSession` (client DELETE, age-based cleanup timer) stays `deleteRow: true`; new `reapIdleSession` (the sweep's own reap path) is `deleteRow: false` — the row survives so `recoverPersistedSession` rebuilds the transport transparently. Accepted tradeoff: reaped rows linger in `mcp_sessions` until the `MCP_SESSION_TTL_DAYS` pruner (default 7d) catches them — rows are tiny and the principal hash still gates recovery, so this is a storage non-issue; a dedicated shorter purge for sweep-reaped rows specifically is a named follow-up, not this PR. Five smaller fixes: (2) `sweepOnce` now reaps candidates SEQUENTIALLY with a recheck (`in-flight == 0 && still idle beyond TTL`) evaluated fresh immediately before each individual reap, not just at snapshot time, so a request landing while an earlier candidate's reap awaits real I/O can't have a later candidate's transport closed under it (tradeoff: a large first-sweep backlog reaps slower than a parallel-fire batch would — correctness over throughput). (3) `touch()`/`markInFlight()`/`markSettled()` are now guarded no-ops for an untracked session, so a trailing call landing after a concurrent `forget()` (client DELETE racing an in-flight request) can't resurrect a zombie tracking entry; new `beginTracking()` is the one unconditional seed, used at session creation and — new — inside `recoverPersistedSession`, so a recovered session resumes TTL tracking instead of falling out of the sweep forever. (4) `index.ts`'s `gracefulShutdown` now also stops the sweeper timer. (5) the reap log line now reads `released ~N` — an explicit best-effort marker, not an exact per-session count. (6) documented the half-open-TCP blind spot on `dispatchTracked`'s doc comment: a client that dies without a clean FIN/RST keeps the in-flight guard held forever for an open standalone GET stream, so that session is never reaped even though nothing is really listening — accepted, not fixed here; `SO_KEEPALIVE` / an SSE heartbeat named as the out-of-scope follow-up. `public-session-sweeper.test.ts`: **40 cases** (was 24 — all originals updated for the guarded touch/markInFlight/markSettled API, seeded via `beginTracking`; +2 for the sequential recheck-before-reap race; +6 for the touch/markInFlight/markSettled no-op-when-untracked guard and `beginTracking` reseed). New `streamable-http.test.ts`: **8 cases** — `reapIdleSession` vs `cleanupSession` row-preservation contract; `recoverPersistedSession` succeeding against a row left in exactly the state a sweep reap leaves it (+ not-found/auth-mismatch contrast cases); `dispatchTracked`'s in-flight wiring held for a simulated open GET stream and released once it settles (+ a throw-releases-in-flight case). Gates (final, post-review-fixes): `pnpm build` 4/4 turbo tasks, backend `tsc --noEmit` exit 0, backend vitest **337 passed (29 files)** incl. all 48 new/updated cases, ESLint `--max-warnings 0` zero net-new warnings (one pre-existing `no-process-exit` on `index.ts`, verified identical via `git stash` against the pre-fix file — same line, not introduced here). Merged 2026-07-14T23:27Z (`e05941d`), squash, after a review-fix round (row-preserving reap keeps lazy recovery transparent). | `apps/backend/src/routers/public-metamcp/public-session-sweeper.ts` (new), `apps/backend/src/routers/public-metamcp/public-session-sweeper.test.ts` (new), `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.test.ts` (new), `apps/backend/src/index.ts`, `turbo.json`, `UMBRELLA_FORK.md` | Yes — the DELETE-only-teardown / pool-accumulation gap exists upstream too; clean addition with no Umbrella-specific coupling (pairs with the #276/#260 pool-cap + session-timestamp line). |
| 2026-07-28 | [#84](https://github.com/Umbrella-IT-Group/metamcp/pull/84) | **Endpoint-scoped API keys + `require_scoped_api_key` endpoint gate.** MERGED 2026-07-29 (squash `b0b67a1`); DEPLOYED to prod 2026-07-29 in the #85 image build (`a35db2a`, `/health/upstream` 13/13 healthy — migration `0023` applied, proven transitively by `0024` accepting an acts-as mint on top). Closes the "every API key is gateway-wide" gap: `validateApiKey` is a flat key lookup with no endpoint predicate, so any key reaches every `enable_api_key_auth` endpoint. (1) New nullable `api_keys.endpoint_uuid` FK → `endpoints.uuid` `ON DELETE CASCADE` + index (migration `0023_endpoint_scoped_api_keys`, hand-written idempotent per fork convention, modeled on 0021): non-NULL binds a key to exactly ONE endpoint — `checkApiKeyAccess` (the gate both `authenticateApiKey` branches funnel through; covers streamable-http, SSE, and the OpenAPI bridge) denies it everywhere else. NULL = legacy/unscoped, deliberately GRANDFATHERED per operator ruling: the pre-existing keys keep reaching every endpoint, no forced migration. (2) Mint path: `endpoint_uuid` XOR explicit `all_endpoints: true` REQUIRED (zod `superRefine` on the create form/request schemas + a mirrored impl guard for schema-bypassing callers) — a silent default to global is impossible; scope selection is admin-only in the existing FORBIDDEN-before-write RBAC style (net effect with the mandatory-scope rule: member key minting is closed — pinned by test); the scoped endpoint must exist (NOT_FOUND). `all_endpoints: true` stores NULL. Scope is IMMUTABLE BY OMISSION — neither `UpdateApiKeyRequestSchema` nor the repo update field sets carry `endpoint_uuid` (zod strips a smuggled field; pinned by test); re-scope = delete + re-mint. The internal auto-mint in `endpoints.impl.ts` (createMcpServer convenience) now scopes its key to the endpoint it is minted for; `BOOTSTRAP_API_KEYS` raw inserts stay NULL-scope by design (the grandfathered class). (3) New `endpoints.require_scoped_api_key` (`boolean NOT NULL DEFAULT false`, same migration): when set, `checkApiKeyAccess` denies unscoped keys on that endpoint — the opt-out that lets a sensitive endpoint (e.g. m365 before its `enable_api_key_auth` flip in the follow-up PR) refuse grandfathered gateway-wide keys; default false = zero behavior change. (4) UI: key-create dialog gains a REQUIRED endpoint picker with an admin-only "All endpoints (Global)" option; member + admin key lists gain a Scope column (endpoint name; amber "All endpoints" badge for NULL); endpoint create/edit dialogs expose the require-scoped toggle inside the API-key auth section; strings en + mirrored ko/zh. Sequencing note for the m365 follow-up: flip a sensitive endpoint's `enable_api_key_auth` only AFTER its consumer's key is scoped, and set `require_scoped_api_key` on it first. **Migration-ordering note (READ BEFORE MERGING EITHER PR):** held PR #79 also carries a `0023_*` migration. The SECOND PR to land must do BOTH on rebase, not just the first — renumber alone is a silent-skip trap: (a) rename its SQL file + `_journal.json` `tag` to `0024_*`, AND (b) bump that journal entry's `when` (folderMillis) STRICTLY ABOVE the first lander's `0023` `when`. Drizzle (`drizzle-orm/pg-core/dialect.js`) applies only journal entries whose `when` exceeds the max already-applied `when`, so a `0024` that keeps an older `when` is SILENTLY SKIPPED — no error, the table is just never created in prod. This PR's `0023` uses `when: 1785196800000`; a second-landing renumber must exceed that, and after any rebase re-verify the renumbered entry's `when` is the largest in `entries[]`. **Review round 1 (2026-07-29):** two HIGH session findings + one HIGH bootstrap + MED/LOW hardening fixed on this branch before merge — (1) endpoint-binding on the in-memory session lookup (streamable-http + SSE + DELETE guard): a session id is now served only on the endpoint it was created against (`SessionBinding` + `bindingMatches`, mirroring `recoverPersistedSession`'s cross-namespace predicate), closing cross-endpoint session replay; (2) `/health/sessions` (unauthenticated) no longer publishes live session ids — counts + sweeper stats only; (3) the `BOOTSTRAP_RECREATE_USER` + `preserveApiKeysOnRecreate` path now preserves each key's endpoint scope as uuid + endpoint NAME and restores AFTER `bootstrapEndpoints`, re-resolving name → the recreated endpoint's CURRENT uuid (the user delete cascades user-owned endpoints away and they return with fresh uuids, so the preserved uuid is FK-invalid by restore time) — scope survives recreate via NAME re-resolution, not uuid; a scoped key whose endpoint no longer exists is dropped LOUDLY (skipped + counted, never NULL-promoted to gateway-wide) and the restore log reports restored/skipped counts instead of blanket success; (4) `require_scoped_api_key` documented as API-KEY-ONLY (does not gate OAuth — see `checkOAuthAccess`) in UI copy + the tRPC field comment; (5) the createMcpServer auto-mint now reuses only a key that authenticates on the new endpoint (exact-scope, else unscoped) else mints a scoped one; (6) `validate` drops `endpoint_uuid` from its response (key-oracle narrowing); (7) member Create-API-Key UI disabled with an explanatory line (minting is admin-only); (8) endpoint-delete dialog warns bound keys are CASCADE-destroyed. Gates (post-review-round-1): backend vitest **426 passed (43 files)** incl. `api-key-access.test.ts` (13 cases) + extended `api-keys.impl.test.ts` (scope RBAC, xor guard, stores-NULL, NOT_FOUND, update-schema strip) + new `session-lifetime-manager.test.ts` (bindingMatches predicate + binding storage), `streamable-http.test.ts` getBoundSession mismatch-404 + health-payload-no-ids, `bootstrap.preserve.test.ts` (scope survives recreate via name re-resolution; endpoint-gone → skip, never NULL-promote; counts correct), `endpoints.impl.pick-key.test.ts` (auto-mint reuse); `pnpm turbo build` 4/4; `check-types` 2/2; prettier clean on touched code; zero net-new ESLint warnings (backend/frontend max-warnings-0 baselines fail pre-existing on `umbrella`, re-verified via stash runs). **Review round 2 (2026-07-29):** re-review verdict FIX-FIRST — round 1's session-binding work confirmed closed, but its bootstrap fix was FK-broken on the common path: `db.delete(users)` cascades user-owned endpoints away (`endpoints_user_id_users_id_fk ON DELETE cascade`) and `bootstrapEndpoints` recreates them with fresh uuids, so restoring the preserved `endpoint_uuid` violated the `api_keys` FK — swallowed by the catch, with an unconditional "✓ Restored" log (silent credential LOSS reported as success). Fixed: preserve also captures the endpoint NAME (left join); the restore is deferred until after `bootstrapEndpoints` and re-resolves name → current uuid via the pure `planPreservedApiKeyRestores`; a key whose endpoint no longer exists is SKIPPED loudly (never NULL-promoted to gateway-wide) and the log reports restored/skipped/failed counts. Also round 2: the DELETE guard's persisted branch now funnels through `bindingMatches` (single security predicate, no drift); the SSE `/message` guard and the streamable DELETE guard (incl. `findById` branch + lookup-failure fallback) extracted into exported pure resolvers (`resolveSseMessageSession`, `resolveDeletableSession`) with unit tests — match / cross-endpoint / absent / DB-error-treated-as-absent / indistinguishable 404 shaping; the three remaining `getSessionIds()` request-path log dumps removed (count-only, one demoted to debug); the api-keys page resolves the session role (`roleLoaded` + `.catch`) before claiming key-create is admin-only (neutral disabled button until then). Gates (post-round-2): backend vitest **441 passed (44 files)** (`sse.test.ts` new, +7 DELETE-guard cases, preserve tests rewritten around the planner); `turbo build` 4/4; `check-types` 2/2; prettier + `eslint --max-warnings 0` clean on all touched files. | `apps/backend/drizzle/0023_endpoint_scoped_api_keys.sql` (new), `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/api-keys.repo.ts`, `apps/backend/src/db/repositories/endpoints.repo.ts`, `apps/backend/src/db/serializers/api-keys.serializer.ts`, `apps/backend/src/db/serializers/endpoints.serializer.ts`, `apps/backend/src/middleware/api-key-oauth.middleware.ts`, `apps/backend/src/middleware/api-key-access.test.ts` (new), `apps/backend/src/trpc/api-keys.impl.ts`, `apps/backend/src/trpc/api-keys.impl.test.ts`, `apps/backend/src/trpc/endpoints.impl.ts`, `packages/zod-types/src/api-keys.zod.ts`, `packages/zod-types/src/endpoints.zod.ts`, `apps/frontend/app/[locale]/(sidebar)/api-keys/page.tsx`, `apps/frontend/app/[locale]/(sidebar)/endpoints/page.tsx`, `apps/frontend/components/edit-endpoint.tsx`, `apps/frontend/public/locales/{en,ko,zh}/{api-keys,endpoints,validation}.json`, `UMBRELLA_FORK.md`; **review round 1 also touched:** `apps/backend/src/lib/session-lifetime-manager.ts` (+`.test.ts` new), `apps/backend/src/routers/public-metamcp/streamable-http.ts` (+`.test.ts`), `apps/backend/src/routers/public-metamcp/sse.ts`, `apps/backend/src/lib/bootstrap.service.ts` (+`bootstrap.preserve.test.ts` new), `apps/backend/src/trpc/endpoints.impl.pick-key.test.ts` (new), `packages/trpc/src/routers/frontend/api-keys.ts`, `apps/frontend/app/[locale]/(sidebar)/endpoints/endpoints-list.tsx`; **review round 2 also touched:** `apps/backend/src/routers/public-metamcp/sse.test.ts` (new) | Partly — the endpoint-scope column + `checkApiKeyAccess` binding and the `require_scoped_api_key` gate are generic upstream gaps; the admin-only mint policy + grandfathering ruling are Umbrella-specific. |
| 2026-07-29 | [#85](https://github.com/Umbrella-IT-Group/metamcp/pull/85) | **API-key-bound identity (acts-as) for m365 delegated injection.** MERGED 2026-07-29 (squash `a35db2a`); DEPLOYED to prod 2026-07-29 ~06:07Z (push build run 30426814937 success; acts-as mint accepted on prod → migration `0024` live; first consumer: the sever endpoint's scoped+bound key, 16 tools verified end-to-end from LXC 205). Makes an API key an EXPLICIT, admin-controlled identity source for the per-user Graph-token injection — the "one teammate's key exercises another identity" hazard `docs/mcp-auth.md` warns about, shipped safely because it is PAIRED with #84's endpoint scoping. (1) New nullable `api_keys.acts_as_user_id` `text` FK → `users.id` `ON DELETE CASCADE` + index (migration `0024_api_key_acts_as_identity`, hand-written idempotent; journal `when` 1785283200000 > 0023's 1785196800000 per the migration-ordering note above — re-verify largest-in-entries after any rebase). NULL (every existing key) = no identity, injection keeps fail-closing. (2) The ONE streamable-http m365 context gate (`handleRequestWithUserContext`) gains an `api_key` branch: `authMethod === "api_key" && apiKeyActsAsUserId` → `{ userId }`; OAuth branch unchanged and takes precedence. `validateApiKey` projects the binding; `authenticateApiKey` stamps `apiKeyActsAsUserId` on BOTH its api-key branches. (3) Mint policy (zod superRefine + mirrored impl guard for schema-bypassing callers): binding is admin-only (FORBIDDEN-before-write), REQUIRES a non-null `endpoint_uuid` (rejected with `all_endpoints: true` or missing scope — an identity-bound key must never be gateway-wide; this pairing IS the safety argument), target user must exist (friendly NOT_FOUND via new minimal `usersRepository.findById`; deliberately no list-users tRPC). Creation-only / IMMUTABLE BY OMISSION from every update schema + repo update method (zod strips a smuggled field; pinned by test). (4) Fail-closed by design and PINNED BY TEST: unbound keys inject nothing (a key never "acts as its creator"); SSE + OpenAPI bridge have NO m365 context wiring (source-level absence test); `validate` still discloses neither scope nor binding. (5) Audit: `resolveClientIdentity` labels a bound key `key-name (as user-email)` — a string extension of the #39/#57 identity label, same shape for all consumers. **AUDIT LABEL-SHAPE NOTE for downstream query owners:** `tool_call_audit.client_name` (and Live Logs `clientName`) now carries `key (as email)` for identity-bound keys — an external dashboard grouping by EXACT `client_name` sees a bound key's label change the moment it is bound (and again within ~60s of an acted-as email change), and "every call acting as X" requires a `LIKE '%(as x@y)%'` match, not equality. A structured `acts_as_user_id` column on `tool_call_audit` is the right long-term shape — deliberately NOT added in #85 (schema churn on a hot audit table deserves its own change); noted here as a future line. Round-2 behavior: the immutable binding (name + acts_as id) is cached per key uuid while the mutable email is composed per call behind a 60s TTL, and a DB error reading the key row labels the caller `api-key <short> (identity unresolved)` UNCACHED — never a bare label indistinguishable from a no-identity key. (6) UI: admin-only "Acts as user" free-text input (better-auth user id) in the create dialog, enabled ONLY when a specific endpoint is selected AND ownership is a specific user, cleared on both the all-endpoints switch and the 'everyone' ownership switch (free text stays because the ownership select carries no concrete user ids and there is deliberately no list-users tRPC — the backend ownership invariant enforces regardless); identity badge in member (short id) + admin (email, via aliased second users join) key lists; strings en/ko/zh. **Review round 2 (2026-07-29), all findings fixed:** (a) OWNERSHIP INVARIANT [high] — an identity-bound key must be OWNED by the identity it exercises: FORBIDDEN in the impl (before any DB read) when the effective owner ≠ `acts_as_user_id`, mirrored in both zod superRefines (`ownerMismatch`, en/ko/zh), killing the public-('everyone')-owner combination (public keys' raw values are listed to every member — a public identity-bound key would be a fleet-distributed delegated Graph credential) and the foreign-owner one; admin-owned acting-as-self and owned-by-the-acted-as-user still pass. (b) UI gating [high] as in (6). (c) RUNTIME pairing re-check [medium] — both `authenticateApiKey` branches stamp via `resolveActsAsUserId()`: acts-as honored only when `endpoint_uuid` is also non-null, so an unscoped-but-bound row written outside the app (psql/admin_cli) can never become a gateway-wide identity key. (d) STRUCTURAL invariant [medium] — migration 0024 adds `api_keys_acts_as_requires_scope` CHECK (`acts_as_user_id IS NULL OR endpoint_uuid IS NOT NULL`), DO-block-guarded for idempotency; mirrored in schema.ts via drizzle `check()`. (e) BOOTSTRAP preserve/restore [medium] — the recreate cycle now captures `acts_as_user_id` + the acted-as EMAIL and re-resolves email → current user id at restore (sign-up mints a fresh id, same reasoning as the endpoint-name pair); an unresolvable identity SKIPS the key loudly (never restored unbound = silent degradation); capture widened to `or(user_id, acts_as_user_id)` so foreign-owned/public keys bound to the recreated user (killed by the acts_as CASCADE) are counted in a loud warn instead of dying silently. (f)+(g) resolver label fixes [low] as in (5). Sequencing unchanged from #84: bind + scope the consumer's key FIRST, flip the m365 endpoint's `enable_api_key_auth` LAST, with `require_scoped_api_key` set. Gates (round 2): backend vitest **492 passed (46 files)** incl. context-gate matrix via `dispatchTracked` with the real AsyncLocalStorage, RBAC/pairing/NOT_FOUND matrix, immutability strips, `validateApiKey` projection pins; `turbo build` 4/4; `check-types` 2/2; `eslint --max-warnings 0` + prettier clean on all touched files. | `apps/backend/drizzle/0024_api_key_acts_as_identity.sql` (new), `apps/backend/drizzle/meta/_journal.json`, `apps/backend/src/db/schema.ts`, `apps/backend/src/db/repositories/api-keys.repo.ts`, `apps/backend/src/db/repositories/users.repo.ts` (new), `apps/backend/src/db/repositories/index.ts`, `apps/backend/src/db/serializers/api-keys.serializer.ts`, `apps/backend/src/middleware/api-key-oauth.middleware.ts`, `apps/backend/src/routers/public-metamcp/streamable-http.ts` (+`.test.ts`), `apps/backend/src/lib/metamcp/consumer-identity-resolver.ts` (+`.test.ts`), `apps/backend/src/lib/bootstrap.service.ts`, `apps/backend/src/lib/bootstrap.preserve.test.ts`, `apps/backend/src/lib/bootstrap.order.test.ts`, `apps/backend/src/middleware/api-key-access.test.ts`, `apps/backend/src/trpc/api-keys.impl.ts` (+`.test.ts`), `apps/backend/src/db/repositories/api-keys.repo.member-scope.test.ts`, `packages/zod-types/src/api-keys.zod.ts`, `apps/frontend/app/[locale]/(sidebar)/api-keys/page.tsx`, `apps/frontend/public/locales/{en,ko,zh}/{api-keys,validation}.json` | Partly — the acts-as column + context-gate branch + identity-requires-scope invariant are a generic upstream capability; the m365-specific injection pairing and admin-only policy are Umbrella-specific. |
| 2026-07-29 | [#85](https://github.com/Umbrella-IT-Group/metamcp/pull/85) | **PR #84 residual sweep (5 follow-up-grade findings from the #84 final review, separate `fix(followup)` commits on the same PR).** (a) `recoverPersistedSession` routed through `bindingMatches` — the endpoint-binding predicate now exists exactly once file-wide (was a third open-coded copy that could drift). (b) New `bootstrap.order.test.ts`: module-mocked WIRING tests driving the real `initializeEnvironmentConfiguration` against a chain-stub db — pins that the deferred api-key restore insert is issued AFTER the endpoints insert with the name-re-resolved FRESH uuid and that `ensureUser` issues no api_keys insert (the two #84 review blockers both lived in this wiring, which the pure planner tests could not see). (c) `bootstrapApiKeys` log truth: a config-declared key colliding with a pending preserved-key restore no longer logs its fresh mask as the live credential (the restore's `onConflictDoUpdate` overwrites it moments later) — line amended to name the pending restore; DB writes unchanged. (d) `ensureUser` early returns (sign-up failed / user missing after sign-up) now warn loudly with the preserved-key COUNT + NAMES (never values) — previously N credentials died behind a generic sign-up warning. (e) api-keys page: a failed role fetch renders an error line + Retry instead of a permanently `aria-busy` create button (still fail-closed; strings en/ko/zh). | `apps/backend/src/routers/public-metamcp/streamable-http.ts`, `apps/backend/src/lib/bootstrap.service.ts`, `apps/backend/src/lib/bootstrap.order.test.ts` (new), `apps/frontend/app/[locale]/(sidebar)/api-keys/page.tsx`, `apps/frontend/public/locales/{en,ko,zh}/api-keys.json` | Partly — (a)–(d) fix generic upstream-shaped bootstrap/session code; (e) is fork UI. |
